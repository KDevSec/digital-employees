# 预审与 demo 对照·协同编排设计合并后状态刷新

| 项 | 值 |
|----|-----|
| 日期 | 2026-08-26 |
| 性质 | 过程记录——对 [设计预审记录-2026-08-25](设计预审记录-2026-08-25.md)（PR-001~032）与 [1.0demo员工创建对照与对齐建议-2026-08-26](1.0demo员工创建对照与对齐建议-2026-08-26.md)（DC/TC）在 **D-039~D-048 合并 main（7d2a657）之后**的逐条状态刷新；不改原两份文档结论，只标注现状 |
| 触发 | [协同编排设计 v0.1](../plans/2026-08-26-协同编排-design.md) 评审通过合并（🟢，12 项裁决 → D-041~D-048，**D-037 被修订**）+ 补记批次 D-039（Inno Setup 真安装包）/ D-040（品牌 workbench → devzero，`~/.devzero`）+ I0-5 合流（T9~T13，品牌正式名「研发零处数字员工终端」入 CLAUDE.md §4） |
| 基准变化 | 预审基准（main @ 9496b7b）**不含**：MCP-server-Bun spike 报告、S4 员工落地方案 spike 报告（均 2026-08-25 晚提交）、编排设计、D-039/D-040——本文据此补增量 |

---

## 1 总览

新决策批次对两份落档的影响分四类：

| 类别 | 条目 |
|------|------|
| ✅ 已解决（原问题被承接/消解） | PR-017、PR-018、PR-027；TC-1、TC-3、TC-4 |
| ✅ 已裁决（方向有锚，待回写设计文档） | DC-05（档位）、PR-016（数据源方向） |
| 🔺 加重 / 前提变化 | PR-013/014（回写清单扩大）、PR-001（裁决内容变化）、PR-002/PR-005（加重）、PR-023（**前提被 S4 重构**）、PR-024（底座扩为三家） |
| ➖ 仍开放（不受影响） | PR-003、PR-004（半）、PR-006~012、PR-015、PR-019~022、PR-025~026、PR-028~032、DC-01/04/06/07/08/09 |

**核心结论**：M2 三份详细设计中**员工业务与底座适配两份的修订量显著扩大**——不止预审发现的 D-036/037 未回灌（PR-013/014），还要吸收 D-039~D-048 的目录品牌化、E-13 改道、E-11 扩编、S4 落地模型。I0-3（🟡→🟢）评审前需要一份合并的修订清单（§4）。

## 2 预审 PR 条目状态刷新

| PR | 原判 | 现状 | 依据 |
|----|------|------|------|
| PR-001 skill 来源缺支 | 🔴 | 🔴 **裁决内容变化**：D-047 裁定 E-13 V0.1 主路径 = **本地 zip 上传**（AgentHub 拉取后移 V0.2+）——skillEntrySchema 的 V0.1 必需支变为 `template`（内置 7 员工素材）+ `local`（zip 上传），`agenthub` 支降为 V0.2 预留 | D-047、编排设计 §2 裁决 8 |
| PR-002 schema 丢字段群 | 🔴 | 🔴 **加重**：① E-11 模板库按 7 员工扩编（D-044），7 个预置包全要过 schema——字段缺口被 7 倍放大；② **新分歧**：S4 采纳的讨论稿包形态是 `employee.yml + persona.md + skills/`（demo 预览树渲染的正是它），而详细设计是 `manifest.yml + AGENTS.md`；编排设计 §4.1 只钉了「persona 模板格式沿 1.0」，**EmployeeSpec 的文件形态（manifest+AGENTS.md vs employee.yml+persona.md）未钉死**——I0-4 契约冻结前必须定 | D-044、S4 报告 §1.1、编排设计 §4.1/§7.1 |
| PR-003 base_identity 死锁 | 🔴 | 🔴 不变，且 D-043 三层模型给了更清晰的解法方向：员工身份归属事实在 Deployment（spec×host×home），manifest 的 base_identity 应改 optional 或移除 | D-043 |
| PR-004 UPP 骨架/upp_version | 🟡 | 🟡 半变：`commands` 缺口仍在（DC-01）；`engine_version` 的相关性上升（引擎已进 V0.1，D-041） | — |
| PR-005 connectors 表达力 | 🟡→🔴 | 🔴 **加重**：引擎连接器实证形态即 url 型 `{"type":"http","url":"http://127.0.0.1:19980/mcp"}`（D-042，落工作区 .mcp.json，虽不经 manifest.connectors，但 http 型 MCP 条目已是 V0.1 现实需求）；员工自带连接器的 sse/url 型需求不变（DC-02） | D-042、spike 实证 |
| PR-006 tier_map 命名/键名 | 🟡 | 🟡 **词表有锚**：Q7 裁「表内 model_tier 档位名沿 1.0 五档名（评审安全档/设计档/探索档/编码档/执行档）」；字段名二选一仍未定 | 编排设计 §15 Q7 |
| PR-013 F-02 看板旧口径 | 🔴 | 🔴 **回写清单扩大**（见 §4） | — |
| PR-014 无引擎口径过时 | 🔴 | 🔴 同上；且 D-041 LLM 主控取消——「主控员工」概念整个消失，员工状态数据源 = 编排任务事件（§13 L4 回写③）口径更明确 | D-041 |
| PR-016 统计四格 | 🟡 | ✅ 方向有锚：D-046/§13 L4——员工状态机工作维迁移源 = 编排任务事件；tab① 统计接不接 events 待员工业务修订时落 | D-046、编排设计 §13 |
| PR-017 内置 team 通路 | 🔴 | ✅ **解决**：§4.3 交付时序（预置资产就位 → 首次演示 install 7 spec 经 adapt() 落位）+ §10 三层模型 + §13 回写 | D-043/D-044 |
| PR-018 mcp.json 引擎工具注入 | 🟡 | ✅ **解决且方案变更**：引擎连接器落**任务工作区** `.mcp.json`（编排器写，发起任务时落位），**不进员工包**——§10「.mcp.json 分工：安装器装员工（身份/技能）；编排器落引擎连接器，在 registry 会合」 | D-042/D-043、编排设计 §10 |
| PR-023 skills 落位无命名空间 | 🔴 | 🔴 **前提被 S4 重构**：身份锚定为 per-host adapter 属性（CC=配置域 config-domain 最稳、Qoder=项目层 project-file 基线、launcher 兜底），员工 home = `~/digital-staff/<host>/<id>/` 独立 config 域——原落位表（项目级 `.codebuddy/skills/` 等）与**当值/停靠区/duty.json 整个模块的前提变化**（CC 侧「身份互斥天然满足」；Qoder 侧项目层锚的互斥语义待 L2 重写）。**底座适配详细设计需按 S4 + D-043 大改**（§4 缺口 B-4） | D-043、S4 报告 §1.4/§2 |
| PR-024 profile 缺 provides | 🟡 | 🔴 **扩容**：底座矩阵改 **CC/CB/Qoder 三家并列一等公民**（D-043，事实修正 D-029 的 V0.1=CB+Qoder）——capability-profile 要三份且 provides 清单（L2 可达判定输入）更必需 | D-043 |
| PR-027 orchestration 落位复核 | 🟡 | ✅ **消解**：驱动器在 service 内读表，run 级表快照进任务账本（`table.snapshot.yml`），不落位工作区的结论成立；「引擎自读包目录」的旧语境整体翻篇 | D-045 |
| 其余（PR-007~012/015/019~022/025~026/028~032） | — | ➖ 不受本批次影响，仍开放（其中 PR-026 事务日志崩溃恢复被编排设计 §5.1「驱动器无状态可恢复」与 S4 事务化八条进一步佐证为必修项） | — |

## 3 demo 对照 DC/TC 条目状态刷新

| 条目 | 原状态 | 现状 |
|------|--------|------|
| TC-1 team 实体（搁置） | 搁置 | ✅ **设计确认搁置正确**：编排设计 §3.2「team 在 2.0 是组合关系不是交付单元（node-table 引用员工 + 被引用员工集合）；V0.2 群组再正式化团队资产」+ §14 演进表——与 TC-1 建议选项 a 同构 |
| TC-2 8 人花名册 | 搁置 | ✅ **已裁决变体**：内置 team = **7 员工**（D-044，按 1.0 新 demo 花名册；测试工程师不预置——不在五阶段链；req-architect 拆分为 req-clarifier + sys-engineer）；demo 虚构三安全员转正为真实预置员工（sec-compliance/sec-design/sec-code，带 skill 素材）。**E-11 模板库从 2 个扩到 7 个**——员工业务 §4.1「仅 2 个内置模板」被推翻（§4 缺口 B-2） |
| TC-3 安全体系 | 搁置 | ✅ **已裁决引擎化**：五阶段 12 节点（准入-需求-设计-开发-准出），三角色位全部引擎化（主责=action.emp、评审位/安全位=gate.reviewer），「安全不降档」由拓扑保证——demo「台账与引擎两套说辞」的问题被真正解决 |
| TC-4 主控形态（不可全搁） | 半搁置 | ✅ **超预期解决**：D-041 LLM 主控整个取消——「编排=代码（确定性驱动器），智能=LLM（员工/评审会话），兜底=人」；CLAUDE.md §2-1 无冲突（驱动器是 service 内代码模块，非 agent）；1.0「主控内联」经验的 2.0 等价物即 L0 手动驱动层 |
| DC-01 Command 类 | 🔴 待裁 | ➖ 仍待裁（编排设计未涉及 manifest commands；DC-01 建议不变：schema 补字段、向导不做 UI） |
| DC-02 sse/url 型 | 🔴 待裁 | ➖ 仍待裁且加重（并入 PR-005，见上） |
| DC-03 skill 三来源 | 🔴 待裁 | 🔺 **变化**：AgentHub 支后移（D-047）——「资产平台拉取」演示高光 V0.1 不再上演，V0.1 叙事改为 zip 上传（sec-scan 两 zip 为首批素材）；「AgentHub」命名统一问题（DC-09）同步降优先级到 V0.2 |
| DC-04 avatar/brief | 🔴 待裁 | ➖ 仍待裁；persona 模板格式已钉（1.0 TPL「# 角色\n一句话职责」+ S4 授权措辞——编排设计 §4.1），brief 有落点雏形，avatar 仍缺 |
| DC-05 档位 UI 全隐 | 🔴 新冲突 | ✅ **基本已裁决**：D-046「模型选择从员工级移任务级（同一员工不同任务不同档，比 1.0 更灵活**且与其 v2 去档位方向一致**）」+ 四层解析链（节点 model_tier > 任务级 > 员工 manifest tier-map > 底座默认）——**员工创建向导去档位 UI** 有明确锚，剩回写动作（员工新建设计 §3.5 / 员工业务 §4.1 Step4 / tab⑨） |
| DC-06 身份文件形态 | 🟡 演示素材 | 🔺 **升级为设计分歧**：S4/讨论稿的包形态 = `employee.yml + persona.md + skills/`（demo 预览树来源），与详细设计的 `manifest.yml + AGENTS.md` 未钉死（同 PR-002②）——I0-4 冻结前必裁 |
| DC-07 底座选择时机 | 维持 2.0 | ✅ 确认维持：底座选择在发起任务时（表底座无关）；「向导底座多选 = 安装目标集合声明」仍是安装期语义 |
| DC-08 三轻类映射 | 🟡 | ➖ 不变（维持 2.0 口径） |
| DC-09 命名统一 | ⚪ | 🔺 **品牌层已另裁**：正式名「研发零处数字员工终端」（CLAUDE.md §4，DevZero 退出 Web 正式名）；标识符 `~/.devzero`（D-040）——**员工业务/底座适配详细设计全文的 `~/.ieidev-workbench/` 目录口径已过时**；「数字资产平台/AgentHub」统一问题随 D-047 降为 V0.2 |

## 4 新增回写缺口（预审基准不含 S4/spike/D-039/040/编排设计的增量）

> 以下按文档归组，供 I0-3 评审（🟡→🟢）与各线 design 文档直接消费。

### B-1｜员工业务与前端详细设计（修订清单扩大）

1. 目录前缀全文替换：`~/.ieidev-workbench/` → `~/.devzero/`（D-040）；数据流图同步（employees/ templates/ installs/ bases/ drafts）
2. E-13 整章改道（D-047）：§6 AgentHub 对接 V0.1 主路径 → 本地 zip 上传（解包 → frontmatter 校验 → 锁版本进包），AgentHub 客户端/代理端点/浮层交互后移 V0.2；§7 的 `/api/skills/*` 端点相应调整；§10 E-13 验收口径重写
3. E-11 模板库扩编（D-044）：2 模板 → 7 员工（req-architect 拆分为 req-clarifier + sys-engineer，+ reviewer-expert + 三安全员）；三安全 skill 素材来源（reference-projects/安全skill/ zip + secretgate）与 sec-scan-code 的 .venv 处理（装员工 home 不进包）
4. 员工包 AGENTS.md SOP 模板（§13 L1 回写③）：干完必报三件套（advance/handoffWrite/dispatchDone）+「引擎工具不可用 = 工作台未运行 → 停止推进并上报，不得绕路」健壮性指令
5. persona 模板格式（§13 回写④）：1.0 TPL 样式 + S4 授权框架措辞
6. PR-013/014 原项：F-02 看板入口解隐 + 无引擎口径回灌（D-036/041）
7. 发起任务操作面（D-046）虽归 L5 看板线，但员工业务 §7 API 表与引擎 API 面（11 操作）的边界建议加一节指针

### B-2｜员工安装与底座适配详细设计（S4 重构级修订）

1. **员工 home 模型**：`~/.ieidev-workbench/homes/<employee_id>/` → `~/digital-staff/<host>/<id>/`（D-043 三层模型 EmployeeSpec→Deployment→Session）；「落地物 = 编译产物 + manifest+hash 漂移检测」（讨论稿/S1 拼图）
2. **身份锚 per-host**：CC=配置域（CLAUDE_CONFIG_DIR，S4 V1 3/3 实证 + skills 同域）、Qoder=项目层（S1 落位表基线不动）、launcher 旗标兜底（V5）——§3 落位表按 host 重写；adapters/ 新增 CC 档案（三底座并列，D-043）
3. **当值模块（§6/§1 停靠区/duty.json）前提重估**：CC 侧独立 config 域天然互斥；Qoder 侧项目层锚的互斥/共存语义待重写；B-07 验收口径相应调整
4. **adapter 契约扩展**：`launch(deployment, workdir, prompt, permission, model, effort)` + `listModels(base)` + `identity_anchor` 声明（§13 L2 回写②）
5. Windows spawn 坑清单（编排设计 §12.1 六坑）并入 B-03/执行器实现注意事项
6. 目录品牌化（installs/ bases/ 等的 `~/.devzero/` 前缀）
7. 原有 PR-023~032 开放项在新模型下重新对表（落位表重写时一并解决 PR-023；PR-026 崩溃恢复被「驱动器无状态 + S4 事务化八条」双重佐证为必修）

### B-3｜shared-protocol（schema 定稿清单，I0-4 冻结前）

1. PR-001 变体：skillEntry V0.1 支 = `template` + `local`（zip 上传为 local 主路径），`agenthub` 支留 V0.2 预留位
2. PR-002 变体：**先钉 EmployeeSpec 文件形态**（manifest.yml+AGENTS.md vs employee.yml+persona.md——S4 讨论稿与详细设计的分歧），再补字段群（identity/capabilities/department/avatar/engine_version…）
3. PR-003：base_identity 按 D-043 改 optional/移除
4. PR-005：connectors 补 type/args/url（http 型已是 V0.1 现实）
5. PR-006：tier_map 字段名定稿 + 值词表沿 1.0 五档名（Q7）
6. PR-004/008：commands 字段（DC-01）、capability 词表权威化（L2 可达判定三份 profile 共用）
7. 兼容性新项：manifest schema 与 node-table schema 的边界（model_tier 档位名、emp/reviewer 引用员工 id——registry 校验在发起时，schema 层不耦合）

### B-4｜跨文档

- 服务本体详细设计：`/mcp` 受控例外口（hono-adapter 单文件，registry 纪律不变）+ SSE 双通道（/api/stream 与 /api/engine/stream 分立，I3 评估合流）+ 端点总表补员（预审 X-1 仍成立）
- 功能点清单 I3 回写清单再加：D-040 品牌口径、D-047 E-13 改道、D-044 E-11 扩编、D-043 底座三家
- 白皮书：员工 home 路径、`~/.ieidev-workbench/` 全部旧口径（§10.3 仓库结构 + 各处路径示例）

## 5 更新版「I1 开工前必裁/必修」清单

| # | 事项 | 优先级变化 |
|---|------|-----------|
| 1 | EmployeeSpec 文件形态钉死（PR-002②/DC-06 升级）——**新增，且是一切 schema 修订的前置** | 🔴 新进 |
| 2 | PR-001 变体：skillEntry V0.1 = template + local(zip) | 🔴 内容变化 |
| 3 | PR-003：base_identity optional | 🔴 不变（D-043 给了方向） |
| 4 | PR-005：connectors url/args 型 | 🟡→🔴 |
| 5 | 员工业务详细设计修订（B-1 清单 7 项，含原 PR-013/014） | 🔴 扩大 |
| 6 | 底座适配详细设计 S4 重构（B-2 清单，含 PR-023 重解、当值模块重估、三底座档案） | 🔴 扩大——L2 线 design 文档开工前完成 |
| 7 | ~~PR-017/018/027~~ | ✅ 销项 |
| 8 | DC-05 档位：员工向导去档位 UI 回写（D-046 已锚） | 🔴→🟡（方向已定，剩回写） |

**给 I0-3 评审的建议**：M2 三份详细设计不宜按原样转 🟢——建议先按 §4 清单完成两份修订（员工业务 7 项、底座适配 S4 重构），shared-protocol 按 B-3 定稿后，再各自评审转绿；或按线拆开：shared-protocol 随 I0-4 契约冻结走、员工业务随 L1 线 design 走、底座适配随 L2 线 design 走（后者与路线图「各线开工前产出 design+plan 文档对」的惯例一致）。

---

*本文为状态刷新记录，不构成裁决；新增缺口 B-1~B-4 待用户确认后由各文档修订/各线 design 文档承接。*
