# 员工安装与底座适配详细设计 v0.2（S4 三层模型重构版）

| 项 | 值 |
|----|-----|
| 版本 | v0.2（**S4 重构级**：三层对象模型替换 v0.1 的「(base, scope) 直装」模型） |
| 日期 | 2026-08-27 |
| 状态 | 🟢 设计骨架用户裁决通过（2026-08-27），全文待评审 |
| 覆盖 | [功能点清单](../概要设计/套件工作台V0.1功能点清单-2026-08-23.md) **B-01（CodeBuddy 落位）/ B-02（Qoder 落位）/ B-03（安装执行器）/ B-04（安装报告）/ B-05（卸载）/ B-06（底座在场探测）/ B-07（作用域与当值→语义改写，见 §12）** |
| 输入 | [S4 Spike 报告](../../research/S4-MVP员工落地方案对比与Linux多底座Spike报告-2026-08-25.md)（三层模型/身份锚裁决/踩坑 P-9~P-14）· [S1 Spike 报告 §8](../../research/S1员工上岗最小闭环Spike报告-2026-08-24.md)（事务化八条）· [协同编排设计 §10/§12/§13](../../plans/2026-08-26-协同编排-design.md)（L2 接口/Windows spawn 四坑/回写清单）· [设计预审记录 PR-023~032](../设计预审记录-2026-08-25.md)（i0-prereview 分支）· **[shared-protocol v0.2 + 员工模板设计](../../plans/2026-08-26-员工模板-design.md)（i0-prereview 分支评审中——manifest 八类真源，§2 消费面对齐）** · D-034/D-043/D-045/D-046/D-048 · [v0.1](../../archive/design/详细设计/员工安装与底座适配详细设计-v0.1.md)（含 §11 真机验证记录，落位事实全部继承） |
| 裁决批次 | **D-059**（本设计定稿：三底座统一 config-domain 身份锚 + 当值/停靠区/duty.json 退役，修订 D-043 Qoder 锚层与 D-013 当值模型） |
| 底座范围 | **CC / CB / Qoder 三底座并列**（D-043；用户裁决 2026-08-27「三 adapter 全开发——都跑过 spike、都是 CC 系插件系统」） |

> **v0.1 → v0.2 变更总表**（读者速览）
>
> | 维度 | v0.1 | v0.2（本设计） |
> |---|---|---|
> | 对象模型 | (base × scope) 直装：员工装进「员工驻地 / 项目工作区」两级 scope | **三层**：EmployeeSpec → Deployment（spec×host×home）→ Session |
> | 身份锚 | 每底座落位表写死（CB=CODEBUDDY.md / Qoder=AGENTS.md 项目层） | **adapter 档案字段 `identity_anchor`**，三底座统一 **config-domain**（D-059），project-file 为回退档 |
> | 当值/停靠区 | duty.json + installs/parked/ + 切换事务 | **整体退役**（每员工独立 config 域天然互斥；D-013 修订） |
> | 员工 home | `~/.ieidev-workbench/homes/<id>/`（scope 之一） | `~/digital-staff/<host>/<id>/`（**家**：config 域 + memory + sessions，身份锚所在） |
> | 安装产物 | 落位文件散布底座约定目录 | 集中在员工 home（config 域内），**工作区零身份文件** |
> | 启动 | （无 launch 概念） | **launch() 契约**（L3 spawn runner 消费；D-041/D-048） |
> | registry | 无（roster 为派生视图） | `~/digital-staff/registry.json` Deployment 台账（L4 花名册同源） |
> | MCP 共存 | merge 进底座全局/项目配置（PR-025 悬置） | 员工连接器落 config 域隔离；引擎连接器由编排器落任务工作区 `.mcp.json`（D-042/§4.5） |
> | 标识前缀 | `_ieidev`（沿 1.0） | **`_devzero` / devzero-* / .devzero-manifest.json**（D-040 品牌修订统一） |

---

## 1 三层模型（总纲）

```
EmployeeSpec（底座无关，员工包 = 唯一事实源；E-12 管线产物 / 模板库）
   │  adapt(spec, host) —— 幂等：建 home + config 域（身份编译/skills/MCP/auth）+ manifest
   ▼
Deployment（spec × host × home = 一行 registry 台账）
   │  launch(deployment, workdir, prompt, permission, model, effort) —— 纯构造 LaunchSpec
   ▼
Session（一次会话 = 一位员工的一个工作实例；spawn 归 L3 spawn runner）
```

**核心语义**（S4 §3.1 采纳 + D-043）：

1. **安装 = 员工上岗**：`adapt()` 建立员工在该底座的全部运行时资产（config 域）；安装后底座自行加载，工作台不驻留底座进程。
2. **身份与工作区分离**：身份/技能锚在员工 home 的 config 域；任务产物在共享工作区；会话 = 「带着工具箱去项目现场」（cwd=workdir 启动 CLI，config 域注入身份）。
3. **落地物 = 编译产物，永不手改**：config 域内一切文件由 adapter 生成；升级=重跑 adapt；漂移=对 manifest hash；卸载=按 manifest 清单删。
4. **多员工并发无冲突**：每员工独立 config 域，身份互斥铁律（S4 3.2 跨底座 4/4 实证）在域层面天然满足——同一 workdir 任意时刻可 spawn 任意已安装员工，无需切换事务。

**模块结构**（workbench-service 绿地新增）：

```
workbench/workbench-service/src/
├── installs/                  安装域（三层模型实现）
│   ├── spec/                  EmployeeSpec schema（zod）+ parser 接口（§2）
│   ├── registry/              Deployment 台账读写（§3.3）
│   ├── executor/              事务化安装执行器（§6）
│   ├── report/                安装报告 + manifest（§7）
│   ├── uninstall/             反向清理（§8）
│   └── launch/                LaunchSpec 构造 + spawn 辅助（§4.3）
├── bases/                     底座域：在场探测 + 版本断言（§9）
└── adapters/                  每底座档案 + transforms（§5）
    ├── contract.ts            adapter 契约类型（§4）
    ├── claude-code/           profile.yml + transforms/
    ├── codebuddy/             profile.yml + transforms/
    └── qoder/                 profile.yml + transforms/
routes：server/routes/installs.ts + bases.ts（I0-5 分域注册，鉴权「会话+激活」）
```

---

## 2 EmployeeSpec 与 parser 接口

**包结构真源已定稿**（i0-prereview 分支，评审中；2026-08-27 确认可参考）：[shared-protocol 详细设计 v0.2](../详细设计/shared-protocol详细设计-v0.2.md)（八类 manifest）+ [员工模板设计](../../plans/2026-08-26-员工模板-design.md)（七模板已物化为活样例 `workbench/templates/`）。**员工包与模板同构**：

```
<package>/                 # 员工包 = 安装单元（E-12 管线产物）
  manifest.yml             # 八类 v0.2（元数据 10 项 / 八件套 1+7 / 管理面 3 段）
  AGENTS.md                # 指令真相源（六段式渲染产物，含 S4 授权措辞）
  skills/<slug>/SKILL.md   # Agent Skills 标准包（与 manifest.skills 清单一致）
  hooks/hooks.json         # 红线编译产物（E-12 构建期从 redlines 声明编译，compiled=true 的规则）
  commands/ knowledge/ orchestration/    # 约定路径（V0.1 预留 / 引擎账本消费，不落位）
```

**消费方式**：复用 `@devzero/shared-protocol` 包（`workbench/shared-protocol/`，L1 线交付物）导出面——`manifestSchema / skillEntrySchema / deriveRequires / deriveCapabilities`——**本线不重复定义 schema**（PR-008 capability 词表漂移的根治）。**状态注记（2026-08-27 修正）**：i0-prereview 合流 main（`f8f78ac`）的只有**设计文档**（shared-protocol详细设计-v0.2）与**模板物料**（workbench/templates/）——**包本体代码由 L1 线 A 阶段实现中**（l1-create 分支 21 任务之首）。本线开发期以 parser 接口桩 + fixture 先行 TDD（fixture 按 manifest v0.2 手工构造）；**L1 A 阶段合流 main 后执行切换**（依赖边界：`installs/spec/` 独立模块，切换点单一）。

```ts
// installs/spec/types.ts —— L2 消费面（从员工包解析；字段与 manifest v0.2 对齐）
interface EmployeeSpec {
  id: string                    // manifest.id（纯 id；employeeId(id,org) 拼装归平台交互 V0.2+）
  display: string
  version: string               // manifest.version
  instructions: string          // AGENTS.md 全文（身份编译源——identity copy 基线）
  skills: SkillEntry[]          // manifest.skills（name/version/source_type，D-047 两支）
  requires: { level: 'L0'|'L1'|'L2', capabilities: string[] }
                                // level = 落盘唯一协商输入；capabilities = deriveRequires() 派生（不落盘）
  connectors: ConnectorRef[]    // stdio/http 双型（§4.5 MCP 落域源；D-042 url 型同引擎连接器形态）
  tier: string                  // constraints.tier 五档单值（launch effort/model 映射输入，D-046 第四层）
  hooksFile?: string            // 包内 hooks/hooks.json 路径（存在时 = adapt merge 源）
}

interface EmployeeSpecParser { parse(packageRoot: string): Promise<EmployeeSpec> }   // 接口；实现待 shared-protocol 合入 main
```

- **协商输入**：`requires.level`（落盘）+ `deriveRequires()` 推导 capabilities（shared-protocol §2.3 派生表：skills 非空 +skill-def；runbook/个人表 +bash-exec+slash-command；个人表 +subagent-dispatch）——negotiator 不自建词表。
- **安装期包校验**（消费 shared-protocol 校验语义）：skills/ 目录与 manifest 清单一致；compiled redlines → hooks/hooks.json 必须存在（跨字段校验 4——一等安装期错误）。
- **persona 措辞**：S4 §3.4 授权框架措辞已由模板设计 §4 六段式「我是谁」段落实（AGENTS.md 渲染含「本角色为用户配置的数字员工岗位」），本线无需二次加工。

---

## 3 Deployment：home 布局与 registry

### 3.1 员工 home（家；S4 §3.3）

```
~/digital-staff/<host>/<employee-id>/          # <host> ∈ {claude-code, codebuddy, qoder}
  config/            # 隔离配置域（adapt 产物全部在此；§5 各底座布局）
  memory/            # 跨会话长期记忆（AGENTS.md 声明「你的记忆在 ./memory/」）
  sessions/          # 会话台账 {workdir, task, host_session_id, started_at}
  reports/           # 安装报告 install-<ts>.json（§7）
  .devzero-manifest.json   # 版本 + 文件清单 + hash（漂移/卸载数据基础）
  .transaction/      # 事务日志（安装期；§6.4）
```

- 同一员工装多底座 = 多个 home（`<host>` 维度分目录），registry 每底座一行。
- **持久用户路径**，禁用 /tmp（P-13：codex 拒绝临时目录建 helper；S3 互证「/tmp 丢文件」）。
- 命名前缀统一 **devzero**（D-040）：`.devzero-manifest.json`、merge 撤销标记 `_devzero`、溯源注释 `generated by devzero`。S4 讨论稿的 `.workbench-manifest.json` 为旧品牌名，本设计按 D-040 修订。

### 3.2 registry 台账（D-043：L4 花名册与编排 Deployment 台账统一口径一个真源）

```
~/digital-staff/registry.json
{
  "registry_version": 1,
  "deployments": [{
    "employee_id": "frontend-dev",
    "spec_version": "0.1.0",
    "base": "qoder",                          // host id
    "home": "C:/Users/liyong/digital-staff/qoder/frontend-dev",
    "status": "installed",                    // installing|installed|upgrading|broken|uninstalling
    "identity_anchor": "config-domain",       // §4.2 档案快照（实测修正后回写）
    "base_version": "1.1.26",                 // 安装时实测版本（P-8：CLI 静默自升级，断言留空间）
    "installed_at": "…", "last_launch_at": "…",
    "manifest_path": "<home>/.devzero-manifest.json"
  }]
}
```

- 写入时机：`adapt()` 事务内（status 先 installing → 完成转 installed）；`launch()` 后更新 last_launch_at。
- 崩溃恢复依据：非终态行 = 未完成事务（§6.4）。
- **职责边界**：员工身份真相源 = 员工包（`employees/<id>/`，E 系列管线）；安装状态真相源 = registry。两层真源不同域，不冲突（继承 v0.1「roster.json 不设」裁决）。

---

## 4 adapter 契约（三动词）

```ts
// adapters/contract.ts
interface BaseAdapter {
  readonly profile: BaseProfile                       // §5 档案（含 identity_anchor）
  adapt(spec: EmployeeSpec, opts: { home: string }): Promise<AdaptResult>      // 幂等建域（§6 执行器驱动）
  launch(input: LaunchInput): LaunchSpec               // 纯构造（§4.3）
  listModels(base: { version: string }): Promise<ModelInfo[]>   // D-046 数据源
}
interface LaunchInput {
  deployment: DeploymentRef                            // home + base + 身份内容
  workdir: string                                      // 任务工作区（cwd）
  prompt: string                                       // 任务指令（D-046：节点 prompt 模板插值结果）
  permission?: PermissionMode                          // 会话权限模式预配（D-042 spike §3.4）
  model?: string                                       // 四层解析链落定的具体模型
  effort?: string                                      // 抽象档位 → 底座旗标（映射收敛 adapter，D-046）
}
```

### 4.1 adapt(spec, host)——幂等建域

每次调用对同一 (spec, host) **幂等**：校验 manifest hash → 未变更则跳过（unchanged）；版本变更则升级（重跑编译）；域内产物缺失/漂移则修复（自愈）。产出：config 域全套（身份文件 + skills + MCP 配置 + 凭证置备）+ manifest + registry 行。

### 4.2 身份锚：三底座统一 config-domain（D-059 裁决）

**裁决与证据链**（避免重复分析，结论以此为准）：

- D-043 原文「CC=config-domain / Qoder=project-file / launcher 兜底」——本次修订：**三底座统一 config-domain 为主路径**。
- 依据①：qodercli = CC 架构复刻（D-034 旗标面同构实证：--print/--cwd/--setting-sources/--plugin-dir/--config-dir）；CC 的 config 域身份+skills 全链已实证（S4 V1 3/3、V6）。
- 依据②：**S1 方案 B「config 根身份脆弱」的真实语义 = 竞争场景双输**（config 域身份 × 项目层身份并存时 2/2 双弃用，V-X1c）——**纯净场景（config 域唯一身份）S1 从未测过、从未被否定**；且该竞争边界三底座均未实测（S4 V1 测试目录无用户身份文件），非 Qoder 特有风险。
- 依据③：用户裁决 2026-08-27「按理说 Qoder 和 CC 一样」+ 统一后架构收益：当值/停靠区/duty.json 整体退役、多员工可并发 spawn 同一 workdir（project-file 覆盖式做不到）。
- **待验与回退 → 已验（2026-08-27 M2 实测定案）**：Qoder config 域身份注入 **3/3 通过** + skills 同域加载双证（枚举+显式触发）+ 竞争边界双底座（CC/Qoder）实测互补叠加——**config-domain 主路径成立，回退档（project-file）不启用，`identity_anchor` 档案值维持不变（零代码改动）**。[L2 安装线真机验证报告](../../research/L2安装线真机验证报告-2026-08-27.md) §1/§3；CB 列待 429/502 解除补验。

锚层档案字段（`adapters/<base>/profile.yml`）：

```yaml
identity_anchor: config-domain        # config-domain | project-file | launcher-flag（兜底档）
```

### 4.3 launch()——LaunchSpec 构造（Windows 四坑对策在此层）

**launch 不 spawn 进程**：返回 `{command, args[], env{}, cwd}` 纯构造值（表驱动可测）；进程生命周期管理（超时 kill/孤儿回收/续跑）归 L3 spawn runner（D-048 分工）。L2 提供两个 spawn 辅助：

1. **prompt 文件中转**：prompt 写入 `<workdir>/.devzero/prompt-<ts>.md`，命令行以 `@file` 或 stdin 引用——规避 npm .CMD 垫片多行 prompt 首个 `\n` 截断坑（协同编排 §12.1 坑1，已选文件中转方案）。**2026-08-27 CC 实测**：坑1 复现实锤（`cmd /c` 垫片下多行 prompt 只剩开头引号）；`@file` 与 stdin 两形态均完整透传——**prompt 通道必须走文件中转，args 直传仅在 spawn 不经 shell 时安全**（`transforms/launch.ts` 现实现把 prompt 全文压 args 且 promptFile 仅作观测——若 L3 spawn runner 用 shell 包装（win32）形态即踩坑，传输通道升级归 L2/L3 接口任务，报告 §7.3）。
2. **垫片解析**：Windows 下探测 `.CMD` 垫片并解析出真实 node/js 入口直连（坑1 根治）；spawn 后产物自检（坑4 python Store 占位符类假成功的通用对策）。**2026-08-27 B-06 实测**：探测链路 `shell: win32` 包装器真实 spawn 三底座版本解析全部正确（报告 §7）。

各底座 LaunchSpec 映射（`transforms/launch.ts`；**Windows 实测 2026-08-27 全收口**——[L2 安装线真机验证报告](../../research/L2安装线真机验证报告-2026-08-27.md) §2）：

| 抽象参数 | Claude Code | CodeBuddy | Qoder（qodercli） |
|---|---|---|---|
| 命令 | `claude` | `codebuddy` | `qodercli` |
| config 域 | env `CLAUDE_CONFIG_DIR=<home>/config` ✅实测（**无 --config-dir 旗标**；身份 3/3 + debug 路径证据） | env `CODEBUDDY_CONFIG_DIR=<home>/config`（**无旗标**；S4 CB-0 结构实证，注入面 502 未测） | **`--config-dir <home>/config` ✅实测**（身份/skills/登录态全链域内生效，1.1.32） |
| cwd | workdir | workdir | workdir |
| prompt | `-p` + **prompt 文件中转（stdin）** ✅（`@file` 展开语义亦实证可用） | 同 CC ⏳（另有 `--system-prompt-file` CB 特有） | `--print` + **stdin ✅实测**（**@file 是附件语义非展开语义**——内容可达但指令弱化，不作为跨底座通道）——坑1 双底座实锤：args 直传多行在 shell 包装（win32）spawn 下必截断 |
| permission | `--permission-mode <mode>` ✅语义实测（值域 acceptEdits/auto/bypassPermissions/manual/dontAsk/plan；**不传时 -p 会话无写类工具**——编排驱动必须显式传） | 同名旗标，值域 acceptEdits/bypassPermissions/default/plan/dontAsk/auto ⏳ | 同名旗标 ✅实测（值域**下划线风格** default/accept_edits/bypass_permissions/dont_ask/auto——**映射层需值转换**） |
| model | `--model <model>` ✅（json `modelUsage` 实锤生效；别名经网关映射） | `--model`（--help 内置 13 模型 id 清单） | `-m, --model` ✅实测（不可用模型自动 fallback 并提示） |
| effort | `--effort <level>` ✅存在（low/medium/high/xhigh/max；语义经网关不可观测） | `--effort`（minimal/low/medium/high/xhigh/max——**值域多 minimal**） | **`--reasoning-effort`** ✅实测存在（旗标名不同） |

> **三底座旗标面差异实测要点**：permission-mode 两套值域风格（CC/CB 驼峰 × Qoder 下划线）且枚举集各有出入；effort 旗标名与值域三底座三个样——**adapter 映射表逐底座显式声明，禁止同构假设直传**。`transforms/launch.ts` 现实现 `--effort` 直传对 qodercli 将撞 `--reasoning-effort` 命名差（回写注记，改码归 L2 开发任务）。**prompt 中转统一走 stdin**（双底座唯一共同可靠通道，报告 §2.2/P-16/P-20）。

> 回退档（project-file 锚）launch 行为：workdir 落薄壳身份文件（AGENTS.md `@import` 根内路径 + 溯源注释，S1 V-X3：引用目标必须在 scope 根内），覆盖式落位（后 launch 覆盖先 launch，互斥单值天然满足）；skills 同步落 workdir 命名空间目录（§5.4 回退落位）。

### 4.4 listModels(base) 与模型档位配置（D-062）

数据源 = 底座配置（**实测 2026-08-27**：CC **无 CLI 模型清单命令**——`claude model list` 不存在（位置参数兜底会把它当 prompt 吞掉，白耗调用），实现走静态 tier-map（/api/bases/claude-code/models 即五档静态表）；CB `--model --help` 文本内置 13 模型 id 清单可解析；**qodercli `--list-models` 实测输出真实清单**（Lite/Qwen3.8-Max/Qwen3.8-Flash/Qwen3.7-Max/Qwen3.7-Plus）——**静态 tier-map 与实测漂移实锤**（现静态表 qoder-max 实测 not available、缺 Qwen3.8 系）：tier-map 按 `--list-models` 实测校准并考虑升级为实测数据源（报告 §2.3/P-21）），返回 `ModelInfo[]{id, label, tier?}`；tier-map（沿用 1.0 五档语义：评审安全档/设计档/探索档/编码档/执行档，D-046/Q7）将抽象档位映射到具体模型 id。发起表单四层解析链：节点 model_tier > 任务级选择 > 员工 manifest 档位 > 底座默认（D-046，解析在编排侧，L2 只提供 listModels 与 tier-map 查询）。

**模型档位配置化（D-062，2026-08-27 用户裁决）**：tier->模型映射从内置静态表升级为**底座级用户配置**，解析优先级 = 用户配置 > 内置默认（校准后静态表）：

- **存储**：`~/.devzero/bases/tier-config.json`（工作台 profile 目录 bases 域内聚；形态 `{ [base]: { 五档: model_id } }`，只存用户覆盖值，未覆盖档位落内置默认）；
- **接口**：`GET /api/bases/:id/tier-config` 返回合并后五档映射（含来源注记）；`PUT /api/bases/:id/tier-config` 覆盖写（zod 校验：五档键齐全、值非空字符串；不做候选白名单强校验--CC 网关别名形态下合法 id 不止静态表五个，前端下拉候选 = models 端点返回）；
- **models 端点**：`GET /api/bases/:id/models` 改返回**合并配置后**的五档展平（配置生效即在此体现）；
- **内置默认表校准**（qoder 按实测 `--list-models`，P-21）：评审安全档/探索档 -> Qwen3.8-Max、设计档 -> Qwen3.7-Max、编码档 -> Qwen3.7-Plus、执行档 -> Lite（原 qoder-max/pro/lite 废除--max 实测 not available）；
- **配置面**：「底座与环境」页面 per-底座五档编辑（I0-5 预留版面，本线填充）；任务发起表单不出现具体模型 id，档位默认「随底座」（D-046 四层链第四层 = 底座配置 > 内置默认）。
- **暂不做**：qoder `--list-models` 实测源动态拉取（长线项--缓存/fallback 策略待真机排期，配置面候选先吃 models 端点静态+配置合并结果）。

### 4.5 MCP 落位分工（D-042/D-043 消解 PR-025）

| MCP 内容 | 落位 | 职责方 |
|---|---|---|
| 引擎连接器（url 型 `http://127.0.0.1:19980/mcp`） | 任务工作区 `.mcp.json` | **编排器**（L3，非本线） |
| 员工自带连接器（spec.connectors） | **员工 config 域内**（各底座域内 MCP 约定位置 ⏳ M2 实测核） | 本线 adapt() |
| 用户自己的连接器 | 用户底座全局配置 | 用户（不触碰） |

员工连接器落 config 域 → 随域隔离，PR-025 的「多员工 MCP 共存上下文污染」在主路径下消解（每员工会话只加载自己域的连接器）。AGENTS.md SOP 配套指令（回写 L1）：「引擎工具不可用 = 工作台未运行 → 停止推进并上报，不得绕路」（D-042 spike ④：模型会自作主张降级）。

---

## 5 三底座档案

### 5.1 档案总表（`adapters/<base>/profile.yml`；**探测修正覆盖静态声明——B-7 不盲信**）

| 项 | Claude Code | CodeBuddy | Qoder（qodercli） |
|---|---|---|---|
| identity_anchor | **config-domain** ✅ Windows 3/3（2026-08-27 实测，S4 V1 Linux 复证） | **config-domain**（⏳ 429/502 解除后复验：S4 CB-1 实测 config 域 CLAUDE.md 未加载半生效） | **config-domain** ✅ **实测通过（2026-08-27，D-059 命门 3/3 + skills 双证；回退档不启用）** |
| 域内身份文件 | `<config>/CLAUDE.md` ✅（Windows 3/3；注意 **CC 同时原生加载 workdir AGENTS.md**——③ 竞争边界实测为互补叠加，见 §5.3 注） | `<config>/CODEBUDDY.md`（v0.1 实证 CB 唯一身份源 ×16）⏳域内约定复验 | **`<config>/AGENTS.md` ✅实测**（纯净场景身份注入 3/3；S1 V-X1「常驻」复证） |
| skills 落位 | `<config>/skills/<slug>/` ✅（S4 V6 Linux + 2026-08-27 Windows 同域加载+语义触发双证） | `<config>/skills/`（CC 同构 ⏳） | **`<config>/skills/` ✅实测**（skills list 枚举 Location 指向域内 + 显式点名触发完整标记；`.agents/skills` 域内形态**不**识别）。**注**：用户级 skills 真身是 HOME 下 `~/.agents/skills/`，**不随 --config-dir 隔离**——员工会话 skills 面 = 域内 + 用户全局叠加（P-19 同构现象） |
| hooks | `<config>/settings.json` hooks 键——**❌ 实测负面结论：CC `-p`（headless）模式 hooks 整体不执行**（user 级/项目级均 registry 0、hook 事件流空；同一文件 env 键却生效=「半生效」；2026-08-27 Windows 实测，报告 §4）。**编排驱动（-p spawn）场景员工 hooks 在 CC 侧无执行面**——红线编译产物在 CC 会话须退化为 AGENTS.md 指令层承载，hooks 面仅在交互式会话有效（决策注记随 D-059 留档） | 同左 ⏳（CB 是否同 CC 行为待验） | **❌ 同 CC 负面结论**（1.1.32 实测：项目层+域内 `<config>/settings.json`+`<config>/.qoder/settings.json` 三落位 × 两种命令形态 × 有无 matcher × 两种权限模式全组合不触发——**v0.1 V-4（1.1.26 项目层触发）已过时，版本行为变更**）。CC 系两底座 headless hooks 整体关闭（报告 §4） |
| MCP（员工连接器） | `<config>/.mcp.json` ⏳（域内位置实测核） | ⏳ | ⏳；注：qodercli 全局消费 `~/.qoder/settings.json` 的 mcpServers 键（spike §2.4，D-034 细化）——域隔离时为 `<config>/settings.json` mcpServers ⏳ |
| 命令/版本探测 | `claude --version` | `codebuddy --version`（实测基线 2.137.1；2026-08-27 在场探测 2.139.0） | **`qodercli --version`**（实测基线 1.1.26；2026-08-27 实测 **1.1.32**（login 触发自升级）；~~qoder~~ 是 IDE 启动器，D-034） |
| 支持区间断言 | `>= 实测基线`；major 跳变 WARN（PR-031）；**⚠️ 平台分档待处理：Windows 实测 2.1.226 < Linux 基线 2.1.245——单一区间在 Windows 直接误伤（/api/bases 卡片 supported:false 实录，报告 §7.2），断言区间按平台分档或下调** | 同左 | 同左 |
| auth 置备 | **按机器认证形态分档**：OAuth 文件形态=软链 `.credentials.json` 一件（S4 T0：`.claude.json` 自动重建）；**env token 形态=零置备环境继承**（本机 Windows 实测：`ANTHROPIC_AUTH_TOKEN`+`ANTHROPIC_BASE_URL` env，无凭证文件——P-12 软链对此形态不适用） | ⏳ 复验 | **P-6 清单精简为两件**（`installation_id` + `.auth/`，拷贝只读不动原件）——**1.1.32 复验通过**（真实域登录后拷两件即恢复临时域登录；~~state.json~~ 1.1.29+ 域内已无此文件） |
| base_version_tested | **2.1.226（Windows 实测 2026-08-27）**/ 2.1.245（S4 Linux）——平台分档 | 2.137.1（v0.1）/ 2.139.0（在场探测 2026-08-27） | **1.1.32（实测 2026-08-27，含身份/skills/hooks/旗标全链）** |

**provides 能力面**（PR-024 补齐；CC 同构声明 + 待验标注）：

```yaml
provides: [agent-def, fs-access, skill-def, bash-exec, slash-command, subagent-dispatch]   # L2 全集，CC 系同构预期
# 待 M2 联调首日实测回填：subagent-dispatch / slash-command（CB/Qoder 未单项实证）
```

### 5.2 身份编译规则（AGENTS.md → 域内身份文件）

1. 基线 = identity copy（AGENTS.md 六段式渲染产物直拷；模板只用交集 markdown 特性 + 授权措辞已含——模板设计 §4）。
2. 首行溯源注释：`<!-- generated by devzero from <employee_id>@<version>/AGENTS.md; do not edit by hand -->`——卸载/漂移/接管判定锚点。
3. 域内文件由本安装创建（config 域是员工私有域）→ **无用户手写冲突**；冲突仅存在于回退档 workdir 落位（备份 `<name>.bak-<ts>` 后接管，用户原文附录尾部）。
4. spec 分歧适配位：`adapters/<base>/transforms/instruction.ts` 预留，当前为空。

### 5.3 AGENTS.md 语义的域内化说明

v0.1 实证「Qoder 原生读取 workspace root AGENTS.md」（Q-006/V-1）不受影响——本设计把 AGENTS.md 放进 config 域后，qodercli 是否仍加载属待验项（S1 V-X1「常驻」是正面证据）。实测口径：M2 联调首日「Qoder config 域身份注入 ≥3 采样」。

> **CC 同构补记（2026-08-27 实测）**：CC 2.1.226 亦原生加载 workdir 根 `AGENTS.md`（③-CC 竞争边界实测：config 域身份（域内 CLAUDE.md）× workdir 用户手写 AGENTS.md（非身份项目约定）→ **互补叠加**——身份标记与项目约定标记同会话双生效，无双弃用；「双弃用」仅在双方都立身份时触发，S1 V-X1c 语义不变）。另实测发现 **CLAUDE_CONFIG_DIR 不隔离用户全局指令内容**（隔离域会话仍复述用户全局 CLAUDE.md 的工作偏好内容，来源未定位——报告 §7.1/P-19）：员工会话语义按「域内身份 + 用户全局工作原则叠加」理解；用户全局若含身份类内容存在双弃用风险。

### 5.4 回退档落位（project-file 锚；**2026-08-27 实测后不启用**——D-059 主路径通过，本节留档备查）

| 包内源 | 目标（workdir） | 动作 |
|---|---|---|
| persona（AGENTS.md） | `AGENTS.md`（全文或 @import 薄壳） | copy/薄壳（引用目标必须在根内，S1 V-X3） |
| skills/<n>/ | `.agents/skills/ds-<id>-<n>/` | copy（**前缀命名空间** `ds-<employee_id>-<slug>`：一层目录保底座 slug 发现惯例，PR-023 回退方案 a；嵌套方案 b 未被实证发现，弃） |
| hooks | `.qoder/settings.json` hooks 键 | merge（`_devzero` 撤销标记） |
| 员工 MCP | `.mcp.json` | merge（`_devzero` 标记） |

---

## 6 安装执行器（B-03；S1 §8 事务化八条全量落实）

### 6.1 执行流

```
① 协商（negotiate(spec, profile, probe)：探测修正覆盖静态声明；missing_required ≠ ∅ → 一等安装期错误，B-8）
   └ 执行期细化（2026-08-27 实现）：**能力分档**——内容性能力（agent-def/fs-access/skill-def）缺失进 missing_required 阻塞安装；编排行为能力（bash-exec/slash-command/subagent-dispatch）缺失走 reachable 降级不阻塞（UPP optional 降级映射语义，对齐 §7 降级目录 degraded-subagent「按 L1 交付」——概要 §4.1 伪代码的 missing_required 全量计法以此为准细化）
→ ② 生成落位计划（§5 档案 + manifest hash 预计算；幂等：同版本 hash 一致 → unchanged 跳过）
→ ③ 冲突预检（config 域残留漂移 / 回退档 workdir 用户文件）
→ ④ 开事务：registry 行 status=installing + 事务日志 .transaction/（计划 + 每步 phase）
→ ⑤ 逐项执行：copy/convert 先写临时文件再原子 rename（Windows：目标存在即抛 → 先删后 rename + 代际重名 .r2.*，不与句柄占用较劲，§12.1 坑2）
        merge 三件套：写前备份原文件 → 程序化深合并（禁文本拼接）→ 写后回读校验 + 捕获底座 parse error 信号（P-1/P-5：底座容忍坏 JSON 静默半生效）
→ ⑥ manifest 落盘 + 报告写入 home/reports/ + registry 转 installed
→ ✗ 任一失败 → 按事务日志逆序回滚（rename 回退/恢复备份/删已拷目录）→ registry 行标 broken + 报告 result=rolled-back（**安装期错误一等公民：完整错误对象 + 用户可读文案 + 恢复指引**）
```

### 6.2 事务化八条映射（S1 §8 → 本设计机制）

| S1 条款 | v0.2 机制 |
|---|---|
| 1. 三步落位单事务 | executor 单事务覆盖 config 域全套落位 |
| 2. merge 三件套 | §6.1 ⑤（备份/深合并/回读校验） |
| 3. 撤销标记双锚 | merge 类 `_devzero: <employee_id>` JSON 键 + copy 类溯源注释 + manifest checksum（缺一不可） |
| 4. 创建 vs 合入区分 | 目标不存在→本安装创建（卸载可整删）；已存在→按标记摘条目保文件（显式判定，不做隐式归零整删） |
| 5. 当值切换崩溃恢复 | **改写**：config-domain 模型无身份切换窗口；对应窗口 = registry installing→installed 两阶段（§6.4） |
| 6. 卸载顺序 | §8（摘 merge 条目→删 copy→清目录→归属豁免 diff→报告） |
| 7. hook 命令生成器 | Windows 转义模板（`cmd /c` + 重定向引号处理，P-4）+ 每模板预触发验证进 CI——**⚠️ 2026-08-27 实测修订：CC/qodercli `-p`（headless）模式 hooks 整体不执行（双底座实证，报告 §4/P-15），CI 预触发失去 headless 载体**；模板验证改交互式人工验证或等待底座恢复 headless hooks 支持；员工红线在编排驱动会话由 AGENTS.md 指令层承载 |
| 8. 报告记录 base_version | §7 报告字段 + registry 行同步（P-8） |

### 6.3 幂等与升级

- 同版本重装：manifest hash 逐项比对，一致跳过（unchanged）。
- 跨版本升级：重跑 adapt 编译 → 域内产物替换；memory/ 与 sessions/ **永不触碰**（用户资产豁免）。
- 域内漂移（产物被手改）：hash 不符 → plan 阶段列冲突，默认「重编译覆盖」（产物=编译物原则），报告记录 diff 摘要。

### 6.4 崩溃恢复（PR-026 裁决：双消费方）

1. **服务启动扫描**：registry 非终态行（installing/upgrading/uninstalling）→ 校验事务日志与 manifest 完整性：完整 → 补转终态（收尾）；不完整 → 标 broken + UI 提示重跑（adapt 幂等自愈）。
2. **下次同目标 plan/execute 前**：检测 broken 行 → 先自愈（重跑 adapt 修复域）再继续新计划。
3. 验收含 **kill -9 注入用例**（安装中途强杀进程 → 重启服务 → 扫描恢复 → 重装成功且域状态一致）。

---

## 7 安装报告（B-04；D-008）

**Schema 以上游概要设计 §4.3 为基线，V0.2 裁剪/修订如下**（PR-028 裁剪声明化）：

| 基线字段 | v0.2 处置 |
|---|---|
| report_version / employee_id / package_version / base / base_version / negotiation / placements / result / started_at / finished_at / log | **保留** |
| signature | **裁剪**（V0.1 无签名，D-019） |
| base_version_tested | **保留**（仅展示；探测断言用 §5 区间） |
| placements[].transform / merge_marker | **保留**（转换名/`_devzero` 标记） |
| scope | **改写** → `{type: "deployment", home: "<path>"}`（三级模型无 workspace scope；workdir 是 launch 参数非安装属性） |
| placements[].conflict.type 枚举 | `user-file-exists \| checksum-mismatch \| domain-drift`（**duty-conflict 移除**——当值退役；v0.1 三值中 other-employee 并入 domain-drift） |

- 存储：`<home>/reports/install-<ts>.json`（一份数据三处消费不变：报告展示 / 卸载反查 manifest / 版本对比 spec_version vs 员工包 version → 「版本落后」黄标，E-10）。
- 多版本历史：home 内全量保留（量小：每 deployment 独立 home，天然按员工×底座聚合；无 v0.1 的 history/ 无限增长问题）。
- UI 双入口不变：底座页按 base 聚合（registry 反查）/ 详情页员工包 tab 按员工聚合。
- **安装期错误一等公民**（B-8 红线）：报告 result=failed/rolled-back 时附完整错误对象 `{code, message, phase, recoverable, hint}`；协商 missing_required、版本断言不符、auth 置备失败均在此列，禁止静默降级。

**漂移检测**（manifest hash，B 系列内只读）：对 installed 行校验 `.devzero-manifest.json` hash → 漂移列表供 UI 展示；一键对齐 = 重跑 adapt（自愈）→ V0.2。

---

## 8 卸载（B-05）

```
输入：registry 行 + home/.devzero-manifest.json（placements 真源）+ 残留扫描
① 保护检查：sessions/ 有活动会话（进程存活）→ 阻止并提示（0.2 会话进程接管后扩展）
② registry 行 status=uninstalling（崩溃恢复锚点）
③ merge 类（仅回退档 workdir 产物）：按 _devzero 标记精确摘除；无法解析 → 报告列 manual[] + diff
④ copy/convert 类：checksum 一致 → 删；不一致（用户改过）→ 默认保留 + kept[] 记录，force 可强删
⑤ 删 home（memory/ 与 sessions/ 默认保留并迁移至 ~/digital-staff/.unclaimed/<id>-<ts>/，报告列明；用户资产不销毁）
⑥ 残留扫描：按 devzero 命名空间约定 glob（ds-* 目录 / _devzero 标记 / 溯源注释文件 / .devzero-*）→ 孤儿文件确认清理（归属豁免：第三方运行时投递如 .ieidev/ 不计「不干净」，P-5）
   └ 执行期注记（2026-08-27 实现）：config-domain 主路径产物全在 home 内随域删除，无独立 glob 扫描面——残留扫描的完整形态随 project-file 回退档启用时落地（workdir 侧 ds-*/薄壳/merge 摘除）；V0.1 主路径验收以「home 整删 + kept 备份 + memory 迁移」覆盖
⑦ registry 行删除 + 卸载报告 ~/.devzero/installs/uninstalled/<id>@<base>.json（audit 保留）
```

**验收**：卸载后 workdir 与用户全局配置 diff 干净（除用户数据与豁免项）；重复卸载幂等（无 registry 行则 no-op）。

---

## 9 底座在场探测（B-06）

| 底座 | 探测命令 | 版本断言（B-8：安装期一等错误） |
|---|---|---|
| Claude Code | `claude --version`（PATH + 常见安装位） | `>= 2.1.245`（Linux 基线）；**Windows 基线 2.1.226（2026-08-27 实测）——断言区间按平台分档**（否则本机实测即被 `/api/bases` 卡片判 `supported:false`，B-8 会误伤阻塞一切 CC 安装，报告 §7.2）；major 跳变 → WARN（PR-031） |
| CodeBuddy | `codebuddy --version` | `>= 2.137.1`（v0.1 实测基线；2026-08-27 在场探测 2.139.0） |
| Qoder | **`qodercli --version`** | `>= 1.1.26`（v0.1 实测基线；2026-08-27 实测 **1.1.32**（login 触发自升级，P-8），身份/skills/hooks/旗标全链实测通过） |

- 探测缓存 `~/.devzero/bases/<base>.json`（30 分钟 TTL + 手动刷新）。
- 在场分两档（B-Q4 维持）：**在场**（命令可用——安装唯一要求）/ **就绪**（= 底座本体已登录（用户全局凭证存在）**且** 域内凭证置备成功（CC=软链指向可达 / qodercli=auth 三件套在域内 / CB=登录态））——就绪缺失给 UI 提示不阻塞（先装后登合法；安装期不做就绪断言）。
- 「已装 skill 校验」口径：安装态枚举（manifest）or 语义探针——`skills list` 有盲区（P-7），不作为唯一判据。
- 底座页卡片（在场徽章/已装员工数=registry 计数/最近安装时间/操作）同 v0.1 §8.2；未装底座显示安装引导。

---

## 10 API 面（installs/bases 域，分域注册挂服务本体）

| 端点 | 方法 | 请求 → 响应 | 说明 |
|---|---|---|---|
| `/api/bases` | GET | → `[{id, label, present, version, version_tested, supported, employees_count, last_install_at}]` | 底座卡片 |
| `/api/bases/probe` | POST | `{base?}` → 探测结果 | 手动刷新 |
| `/api/bases/:id/models` | GET | → `ModelInfo[]` | listModels（D-046 发起表单数据源） |
| `/api/deployments/plan` | POST | `{employee_id, bases: [base_id]}` → 每底座 `{negotiation, placements, conflicts, unchanged}` | **干跑**不落盘（预览/冲突决议收集/漂移自愈检测） |
| `/api/deployments/execute` | POST | plan 请求 + `conflict_resolutions` → 每底座安装报告 | 未决议冲突 → 422（一等安装期错误） |
| `/api/deployments` | GET | `?employee_id= \| ?base=` → Deployment 列表（含报告摘要/漂移状态） | 双入口（query 过滤实现分期：Ctx 无 query 面，V0.1 返回全量由前端过滤——2026-08-27 实现注记） |
| `/api/deployments/verify` | POST | `{employee_id, base}` → 漂移明细 | hash 校验（只读） |
| `/api/uninstall/plan` | POST | `{employee_id, base, keep_user_data?}` → `{cleanup[], kept_predicted[], manual_predicted[]}` | 卸载干跑 |
| `/api/uninstall/execute` | POST | plan 请求 + `{force}` → `{cleaned[], kept[], manual[]}` | §8 |

- **launch 不设 HTTP 面**：进程内库接口，L3 spawn runner 直接消费（D-048）；对外任务发起走编排 API（L3 线）。
- 鉴权「会话+激活」（服务本体 §10.2）；`/api/bases/register` 自定义底座 → V0.2。
- v0.1 的 `/api/install/*`、`/api/duty/switch` 随模型退役（当值退役，duty 域删除）。

---

## 11 预审问题处置表（PR-023~032 逐条销项）

| # | 级别 | 处置（本设计落点） |
|---|---|---|
| PR-023 | 🔴 | 主路径消解：skills 在各员工独立 config 域天然隔离；回退档用 `ds-<id>-<slug>` 前缀命名空间（§5.4，保一层 slug 目录发现惯例）；B-07 验收文案同步改写（§12） |
| PR-024 | 🟡 | §5.1 provides 能力面补齐（CC 同构声明 + subagent-dispatch/slash-command 待 M2 实测回填） |
| PR-025 | 🟡 | 主路径消解：员工连接器落 config 域隔离（§4.5）；引擎连接器归编排器落任务工作区 .mcp.json（D-042）；R-001 的 hooks 语义随当值退役失去场景（每域 hooks 只作用于本员工会话）——留档注记进决策记录 D-059 |
| PR-026 | 🟡 | §6.4 双消费方裁决（启动扫描 + plan 前自愈）+ kill -9 验收用例 |
| PR-027 | 🟡 | **销项**：D-041/045 已裁决引擎在 service 内、账本落任务工作区 `.devzero/tasks/`（员工会话 cwd 内可读）——orchestration/ 仍不落位，理由从「引擎自读包目录」更新为「引擎管理 + 账本工作区可见」 |
| PR-028 | 🟡 | §7 裁剪声明化 + conflict 枚举改写（duty-conflict 移除、other-employee→domain-drift） |
| PR-029 | ⚪ | v0.1 开放问题 B-Q7 已回填，本设计不迁移未销项（归档文档不改动；销项状态在本文档头部输入表体现） |
| PR-030 | ⚪ | duty.json/duty 参数整体退役（D-059）；scope 概念改写为 deployment/home（§7）——同名概念三种表达问题随模型消失 |
| PR-031 | ⚪ | §9 版本断言加 major 跳变 WARN |
| PR-032 | ⚪ | 概要设计勘误注记（Qoder skills 已被 D-034 修正为 .agents/skills/、roster.json 已裁撤、base_identity 回填步骤随三层模型失效）——待本设计评审通过后随 D-059 批次在概要设计头部加勘误注记（冻结文档不改正文） |

---

## 12 测试与验收（B 系列映射）

| 功能点 | v0.2 验收（= 测试口径） |
|---|---|
| B-01/B-02 | 三 adapter adapt 链 TDD：spec→config 域编译产物逐文件断言（身份文件含溯源注释/skills 入域/manifest hash）；真机验收 M2（§12 真机清单） |
| B-03 | 事务八条单测：成功链 / 中途失败注入回滚（目标盘只读/中途删文件）/ 幂等重装 unchanged / **kill -9 崩溃恢复**（§6.4）/ 升级不触 memory |
| B-04 | 报告三处消费（展示/卸载反查/版本对比）；failed/rolled-back 报告含完整错误对象（一等公民） |
| B-05 | 卸载后 workdir+全局配置 diff 干净（归属豁免）；merge 条目按 _devzero 精确摘除；残留扫描命中 devzero 命名空间；memory 迁移不销毁 |
| B-06 | 未装底座显示引导；已装显示版本与员工数（registry 计数）；版本断言不符 → 安装期一等错误 |
| B-07 | **语义改写（D-059）**：身份互斥 = 每 deployment 独立 config 域（并发 spawn 同一 workdir 两员工互不污染，测试：两域身份文件并存、各自 launch spec 的 env/旗标指向各自域）；锚层回退档 = project-file 覆盖式落位 + 前缀命名空间并存 |

**测试形态**：spec/registry/negotiator/launch 构造纯逻辑单测（表驱动 + fixture 包目录）；executor 事务用临时目录夹具（成功/注入失败/幂等/kill-9 模拟=直接操作 registry 状态注入）；真机验证单独出报告回填 §5.1 ⏳ 项。

**M2 真机清单**（联调首日，按 S4 §3.5 判据规范：每项独立夹具、≥3 采样、标记+语义双轨断言、记录底座版本；**2026-08-27 执行完毕**——[L2 安装线真机验证报告](../../research/L2安装线真机验证报告-2026-08-27.md)）：

1. ✅ Qoder config 域身份注入 ≥3 采样 + skills 同域加载（D-059 主项）——**通过，回退不触发**
2. ✅ 「config 域身份 × workdir 用户手写 AGENTS.md」竞争边界——CC/Qoder 双测互补叠加（S4 空档补上；CB 待网络解除）
3. ⛔ CB config 域身份面复验（429/502 阻塞未测——本机云端 502 两次复现，如实记录）
4. ⛔ CB 动态注入验证（同上阻塞）
5. ✅ launch 旗标实测：prompt 中转（stdin 双底座唯一共同可靠，@file 仅 CC）/ permission（值域差异+语义）/ model（双底座生效）/ effort（三底座三样）——§4.3 全收口
6. ✅ listModels 实测（CC 无命令走静态表 / CB help 内置清单 / qodercli `--list-models` 实测清单——tier-map 漂移发现）
7. ❌ hooks Windows `cmd /c` 转义模板预触发——**双底座 headless hooks 均不执行，验证失去载体**（P-15，处置见 §6.2 条款 7）
8. ✅ Windows 实测版本基线回填 §5.1/§9（claude 2.1.226 / qodercli 1.1.32 / codebuddy 2.139.0——claude 平台分档待处理）

---

## 13 开放问题

| # | 问题 | 临时立场 |
|---|---|---|
| B-Q8 | CC/Qoder config 域内的 MCP 约定位置（CC 域内 .mcp.json? qodercli 域内 settings.json mcpServers?） | §4.5 ⏳ M2 实测核；员工连接器 V0.1 非演示必需（demo 员工只用引擎连接器，编排器落工作区） |
| B-Q9 | effort 旗标三底座支持面（CC --effort 存在性未核） | transforms 映射表 M2 收口；不支持则 effort 收敛进 model tier 语义 |
| B-Q10 | 漂移检测 UI 形态（底座页 vs 详情页） | 只读列表 + 一键对齐（重跑 adapt）→ V0.2；V0.1 只做 verify API |
| B-Q11 | 多员工同 workdir 并发 spawn 的产物写冲突（非身份冲突：两员工同时写同一文件） | 编排侧顺序推进（demo 语义）不触发；并发归 V0.2 治理（任务锁） |

---

## 变更记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-08-27 | v0.2 | S4 三层模型重构：EmployeeSpec→Deployment→Session；身份锚统一 config-domain（D-059，修订 D-043/D-013）；当值/停靠区/duty.json 退役；launch 契约（L3 消费）；registry 台账（L4 同源）；PR-023~032 逐条处置；v0.1 归档 |
| 2026-08-27 | v0.2（执行期修订） | B 系列实现收口回写：§6.1 协商能力分档细化（内容性阻塞/编排行为降级）；§8 残留扫描口径（主路径无独立扫描面，随回退档落地）；§10 deployments query 过滤分期；补验收用例 B-03「升级不触 memory」+ B-07「并发 spawn 互不污染」 |
| 2026-08-27 | v0.2（勘误） | §2 状态注记修正：i0 合流 f8f78ac 仅含设计文档与模板物料，**shared-protocol 包本体不在 main**——L1 线 A 阶段实现中（l1-create 21 任务之首）；L2 切换排期挂 L1 A 阶段合流之后 |
| 2026-08-27 | v0.2（M2 真机回填·CC 侧） | [L2 安装线真机验证报告](../../research/L2安装线真机验证报告-2026-08-27.md) CC 侧结论回填：§4.3 launch 表 CC 列收口（config 域=env 无旗标/prompt @file+stdin 双实证+坑1 复现/permission·model·effort 旗标与三底座值域差异）；§4.4 listModels（CC 无 CLI 命令走静态 tier-map）；§5.1 CC 行（hooks **-p 模式不执行**负面结论/auth 按 env-token 形态分档/Windows 版本基线 2.1.226）；§5.3 CC 原生读 workdir AGENTS.md+全局指令不隔离边界；§9 版本断言平台分档（Windows 2.1.226 < 2.1.245 误伤实录）。**Qoder 列待 qodercli 登录后二次回填（① D-059 命门未出判定）** |
| 2026-08-27 | v0.2（M2 真机回填·终版） | qodercli 补测后全量收口：**① D-059 命门通过（Qoder config 域身份 3/3 + skills `<config>/skills/` 域内双证；§4.2 待验转已验，§5.4 回退档标注不启用，`identity_anchor` 零改动）**；③ 竞争边界双底座互补叠加（§5.3）；⑤ hooks 负面结论扩至 qodercli（1.1.32 headless 不执行，v0.1 V-4 过时——§5.1/§6.2 条款 7/§12 清单 7 修订：员工红线由 AGENTS.md 指令层承载）；② Qoder 列收口（`--config-dir` 实测/stdin 唯一双底座可靠通道/@file 附件语义/permission 下划线值域/`--reasoning-effort`/`--list-models` 实测清单与 tier-map 漂移）；P-6 auth 精简两件（1.1.32 复验通过）；版本基线 claude 2.1.226 / qodercli 1.1.32 / codebuddy 2.139.0（§5.1/§9）。CB 动态面 502 阻塞未测（清单 3/4 ⛔ 如实记录） |
| 2026-08-27 | v0.2（D-062 档位配置化） | §4.4 增模型档位配置层（用户裁决）：tier-map 升级为底座级用户配置（存储 `~/.devzero/bases/tier-config.json` + GET/PUT `/api/bases/:id/tier-config` + models 端点返回合并结果 + qoder 内置默认表按实测校准）；「底座与环境」页面为配置面（I0-5 预留版面填充）；发起表单档位默认「随底座」、底座下拉接真实探测源（D-060⑤ E 条收口）；D-046 四层链第四层改「底座配置 > 内置默认」 |
