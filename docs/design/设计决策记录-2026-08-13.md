# 设计决策记录（ADR）——§4 设计阶段

> 日期：2026-08-13（夜间自主设计会话）
> 背景：按交接文档 §4 完成四大块设计。用户授权：过程决策由 AI 自主拍板，全部记录在案，供醒来后审阅/推翻。
> 状态标记：🟢 已采纳（可推翻） · 🔵 已定调引用（来自白皮书/交接文档，不在本轮可推翻范围）
> **2026-08-23 追加**：D-017~D-033（V0.1 功能点清单裁决批次，源自[套件工作台V0.1功能点清单-2026-08-23.md](概要设计/套件工作台V0.1功能点清单-2026-08-23.md) §11 的用户逐条确认；D-017~D-028 = 部署调研 §12 的 D-a~D-l）。

每条决策格式：**决策内容 / 理由 / 影响面 / 备选方案**。

---

## D-001 术语统一为「底座」，原型图"宿主/多宿主"视为同义旧称 🟢

- **决策**：四份设计文档统一使用「底座」（智能体底座）；工作台侧边栏菜单名定为「底座与环境」；原型图 workbench.html 的"多宿主"导航项在下一版原型更名。
- **理由**：CLAUDE.md、白皮书 v0.7、交接文档全部使用"底座"；"宿主"仅出现在原型图（受 QoderWaker"环境与设备"概念影响）。两词并存会让后续开发在代码命名（host vs base）上分裂。
- **影响面**：文档 B（底座管理界面）、原型图下一版。
- **备选**：统一为"宿主"（弃：与既有三份权威文档冲突面更大）。

## D-002 员工详情页 canonical tab 结构定为 10 tab 🟢

- **决策**：`首页 / 项目 / 任务 / 工作流 / 记忆 / 技能 / 知识库 / 连接器 / 权限与治理 / 员工包`。
- **理由**：交接文档 §4.1 要求按 QoderWaker 形态设计（首页/项目/任务/记忆/技能/知识库/连接器/权限 8 tab），但我们的原型 wb-detail 页有 QoderWaker 没有的技术视图（manifest/agents/skills源/node-table）。融合规则：QoderWaker 的"自动任务/对话任务"两 tab 合并为「任务」（原型任务看板已有类型筛选，复用同一套筛选）；QoderWaker 的 IM tab 并入「连接器」；新增「员工包」tab 承载 2.0 特有的包技术视图（manifest / AGENTS.md / skills 源码 / node-table / 安装报告 / 版本历史）。「工作流」保留独立 tab（QoderWaker 同样独立）。
- **影响面**：文档 A（生成完成后跳转）、文档 C（每 tab 字段全量设计）。
- **备选**：完全照抄 QoderWaker 8 tab（弃：丢失员工包/SOP 技术视图，2.0 的差异化正在于"员工=可 git 管理的包"）；完全沿用原型 6 项（弃：与交接文档 §4.1 的明确要求不符）。

## D-003 认证采用「邮箱验证码首登即注册 + JWT 双 token」🟢

> ⚠️ **2026-08-23 状态更新：本条已被实现演进取代**。实际实现为 Keycloak OIDC（Authorization Code + PKCE，Public Client）+ 工作台 BFF 会话 + enrollment 审批 + ES256 机器身份（private_key_jwt + 心跳）--平台 V0.1 已交付、workbench auth demo 已原型。登录与认证设计将按实现重写为 v0.2；本条保留为历史记录。

- **决策**：邮箱+验证码为主要登录方式（首次登录即注册），密码为可选补充；JWT access token（约 1h）+ refresh token（约 30d，滚动轮换）；token 由工作台后端进程持有与刷新，浏览器↔工作台之间用本地 session cookie 隔离；每个工作台实例注册 device_id，token 与设备绑定，支持平台侧远程吊销。
- **理由**：交接文档 §4.4 给了"邮箱+密码 / 邮箱+验证码"两个选项，验证码路线免去密码找回/强度策略等企业内不必要的复杂度；JWT 优于 session 的点在于工作台是长驻本地服务、平台无需为每台工作台维护会话状态；device 绑定为后续审计（哪台机器上报的数据）提供锚点。
- **影响面**：文档 D 全篇；文档 B 的上传/下载鉴权。
- **备选**：纯 session（弃：平台要维护跨大量本地工作台的会话，且断线重连语义差）；OAuth2 device flow（弃：无第三方 IdP 时过度设计，但文档 D 预留企业 SSO 扩展位）。

## D-004 工作名（品牌）沿用 ieidev 线 🟢（待用户确认，见 Q-001）

- **决策**：设计文档示例统一用 `@ieidev/workbench`、`.ieidev/`、skill 前缀 `ieidev-`；正文尽量用中性词"工作台包名"，避免品牌硬编码。
- **理由**：UPP v0.2 §2 员工包示例、原型图（.ieidev/ 出现 7 处）、Workshop V4（npm install @ieidev/workbench）均用 ieidev；仅白皮书附录 A 写了 `@digital-employee/workbench`。取多数派，冲突记入待讨论。
- **影响面**：全部四份文档的示例代码。

## D-005 工作台安装载体 = CLI 一行命令为主 + Windows exe 打包 🟢

- **决策**：macOS/Linux 用 `curl … | bash` 或 `npm install -g`，Windows 提供 exe 安装包；安装后 `ieidev-workbench start` 拉起本地服务，浏览器访问 `localhost:19820`。Workshop V4 提到的"IDE 插件市场安装"定位为**接入入口**（插件里一键拉起/打开工作台），不改变工作台独立应用本质。
- **理由**：白皮书 §4.4 与 CLAUDE.md 均定调 exe/CLI；QoderWaker 参照（环境与设备页的 `curl -fsSL …/install.sh | bash`）与此一致。
- **影响面**：文档 B（底座与环境页）、文档 D（首次启动登录流）。

## D-006 创建向导五步 → 员工包文件的生成映射 🟢

- **决策**：向导五步与产物的映射固定为——
  1. **模板**：选角色模板→预填后续全部表单（模板=一份完整的预置 manifest+AGENTS.md 素材）；
  2. **身份**：→ AGENTS.md（persona 五段式渲染：identity/principles/capabilities/usage_modes/constraints）+ manifest `agent:`/`persona:` 段；
  3. **能力**：→ manifest `skills:`/`mcp:` 段；从 AgentHub（Keyhub）拉取的 skill **复制进包**（skills/ 目录，锁定版本，包自包含）；MCP 连接器写 mcp.json；
  4. **约束**：→ manifest `constraints:` 段；预置红线规则库中"声明式可编译"的规则编译为 hooks/hooks.json（路径C 底座硬拦截），全部规则同时注册进 CQO（路径A 底座无关拦截）——双保险；tier-map/token 配额走路径 B（flow-driver 派发时查）；
  5. **流程+治理**：runbook → 渲染进 AGENTS.md 尾段（流程档）；node-table → 生成 orchestration/<id>.node-table.yml 骨架（编排档，可后续在 workflow 编辑器细化）；治理 → 仅写 manifest `governance:` 段（运行时不生效，上架/审批/审计时由平台消费）。
- **理由**：与扩展配置文档 v0.1 的"配置/开发分界"和三条落地路径（A: CQO / B: flow-driver / C: 底座原生）逐条对齐；skill 复制进包（而非引用）保证"三条产出路径同构 + 包可 git 管理 + 离线可安装"。
- **影响面**：文档 A 核心章节；文档 B 的落位映射输入。
- **备选**：skill 以引用形式留在包外由工作台运行时解析（弃：破坏包自包含性，离线/手写路径不同构）。

## D-007 底座适配设计优先级 = CodeBuddy、Qoder、Claude Code 写透，opencode/Cursor 引用矩阵 🟢

- **决策**：文档 B 对 CodeBuddy / Qoder / Claude Code 三个底座给出完整落位表与安装序列；opencode / Cursor 只引用 UPP §6.3 矩阵并标注差异。
- **理由**：交接文档 §4.2 明确"先适配 CodeBuddy 和 Qoder"（hooks 兼容已验证、1.0 有适配经验）；CC 是开发自用环境且 shim 已被白皮书写透（AGENTS.md→CLAUDE.md）。
- **影响面**：文档 B。

## D-008 安装报告持久化与展示 🟢

- **决策**：每次安装产出 JSON 报告，存工作台数据目录 `installs/<employee_id>@<base>.json`；UI 双入口——「底座与环境」页（按底座聚合）与详情页「员工包」tab（按员工聚合）。报告内容 = UPP §6.2 协商输出（可达 Level / 降级项清单 / 告警）+ 落位清单（每个文件源→目标路径）+ 转换记录（如 AGENTS.md→CLAUDE.md）+ 校验和。
- **理由**：落位清单+校验和是卸载残留检测与版本漂移检测的数据基础——一份数据三处消费（安装报告/卸载清理/漂移对比）。
- **影响面**：文档 B 核心章节；文档 C 员工包 tab。

## D-009 员工状态模型 = 可用性 × 工作状态两维正交 🟢

- **决策**：可用性（在线/离线，判定=所绑底座在场 && 身份文件完好）× 工作状态（空闲 / 在跑 n / gate 停等 / 失败待处理）。卡片显示聚合徽章，详情页显示全量。数据源 = .ieidev/ 的 flow-state 与 events.jsonl，工作台轮询+SSE 推送前端。
- **理由**：原型卡片已有"在跑/空闲/在线"混用的徽章，需要一个正交模型消歧；"在线"对员工而言是安装完好性而非进程存活（L0/L1 员工没有常驻进程）。
- **影响面**：文档 C 核心章节；文档 B 底座卡片。

## D-010 Keyhub 对接按「需求契约 + 现状缺口表」设计 🟢

- **决策**：文档 B 定义工作台所需的 AgentHub REST 契约（skill 搜索/详情/版本/下载、员工包上传、审核状态查询、鉴权头），并给出"Keyhub 现状 vs 契约缺口"对照表；缺口标注为对 Keyhub 的改造需求清单。
- **理由**：Keyhub 是既有平台，能力现状需实证；设计以我们的需求为锚，避免被现状绑架，同时缺口表让改造工作量透明。
- **影响面**：文档 B；文档 A 向导 Step 3 引用搜索/下载端点。

## D-011 治理分级 L1-L4 与使用深度 L0-L3 不改名，行文强制带前缀 🟢

- **决策**：保留两套 L 编号（OKR 已固化 L1-L4 提法），所有文档写"治理分级 Lx"/"使用深度 Lx"，禁止裸写 Lx。
- **理由**：改名收益（消歧）小于成本（OKR/白皮书/UPP/原型全要改）；前缀纪律可执行。
- **影响面**：全部文档行文规范。是否长期改名记 Q-003。

## D-012 四份设计文档统一章节骨架 🟢

- **决策**：统一为 `0 文档信息 → 1 定位与范围 → 2 用户旅程 → 3 信息架构与界面设计 → 4 数据模型 → 5 接口契约 → 6 与白皮书/UPP 对应关系 → 7 边界情况与降级 → 8 开放问题`。
- **理由**：四份文档将被并行评审与后续并行开发消费，同构骨架降低阅读与交叉引用成本。
- **影响面**：全部四份文档。

---

## 追加决策（设计过程中产生）

> 撰写与评审过程中新增的决策会追加到此节，编号顺延。

## D-013 安装作用域两级：员工驻地（默认）× 项目工作区，身份互斥、能力共存 🟢

- **决策**：安装目标 = 底座 × 作用域。作用域两种——**员工驻地**（工作台管理的专属目录 `homes/<employee_id>/`，对话会话以此为 cwd）与**项目工作区**（用户项目目录，员工"进驻项目"）。同一 (底座×工作区) 同时只有一名**当值员工**（身份文件 CLAUDE.md/AGENTS.md 是单一身份源，互斥）；skills/hooks/mcp 按员工命名空间目录并存（切换当值不增删能力文件）。当值切换时旧身份文件移到停靠区。
- **理由**：底座的身份加载机制决定一个目录只能有一个"我是谁"；QoderWaker 的 Waker 也是按设备+工作目录派发。驻地目录解决"不在任何项目里也能对话"；命名空间并存避免切换员工时反复重装能力文件。
- **影响面**：文档 B §2.2/§4.6；文档 C 对话界面选工作目录交互。
- **备选**：全局安装单员工（弃：一台机器只能有一个员工，与多员工产品形态冲突）；per-session CLI 注入身份（弃：白皮书 §5.3 已论证否决）。

## D-014 落位后端 v0.1 统一用 file（裸文件落位），plugin 后端留作 CC/CB 后续优化 🟢

- **决策**：安装执行器抽象为"落位计划（源→目标→动作）+ 落位后端"。v0.1 所有底座统一 **file 后端**（copy/convert/merge + 事务 + 校验和）；1.0 实证过的 plugin 后端（CC/CB 的 marketplace add + plugin install）保留为后续优化项（B-Q2）。
- **理由**：员工包核心是"身份+能力"，身份文件（CLAUDE.md）本就在插件机制之外，插件机制只覆盖 skills/hooks/commands 一半问题；单一后端让首版安装/卸载/漂移/当值切换共用同一套 placements 数据模型；plugin 后端的收益（底座自管缓存与卸载）可后续替换 file 后端的一个子集而不动数据模型。
- **影响面**：文档 B §4.2/§4.5/§4.7。
- **备选**：CC/CB 走 plugin、其他走 file 的双后端（弃：首版两套卸载/漂移逻辑，复杂度翻倍）。

## D-015 employee_id 语义分层：manifest 三元组含域后缀，工作台本地层一律用纯 id 🟢

- **决策**：manifest 身份三元组的 `employee_id` 保留白皮书 §5.2 语义（`<id>@<组织节点>`，组织节点默认取所属部门），仅用于平台身份与 AgentCard 注册；工作台本地的 API 参数、安装清单文件名（`installs/<id>@<base>.json`）、任务模型字段一律取 **manifest.id（纯 id）**。
- **理由**：评审（跨文档镜头）发现三份文档对 employee_id 用法分裂——若本地层也用带域全名，文件名会出现双 @ 且所有 API 示例不成立；白皮书三元组语义不可推翻，故做分层而非改语义。
- **影响面**：文档 A §4.3 employee_id 行、文档 B §4.3 命名约定注记、文档 C §4.2 注记。
- **备选**：全链路统一带域全名（弃：本地层冗长且与所有示例冲突）；废弃三元组（弃：违反白皮书）。

## D-016 使用深度映射修正：+编排（node-table）= 使用深度 L2，跨员工接力（使用深度 L3）归 goal/Team 层 🟢

- **决策**：向导「使用深度」选项 +编排 对应 node-table 编排档 = **使用深度 L2**；requires.level 推导为 裸用→L0、+方法论/+流程→L1、+编排→L2；使用深度 L3（跨员工接力）是 goal/群组绑定 workflow 时由工作台校验的能力门槛，**不由单员工包声明**。
- **理由**：评审（一致性镜头）发现初稿把 node-table 错挂到使用深度 L3、并把跨员工接力错误归给 flow-driver——白皮书 §3.4 明确 L2=+SOP（node-table+flow-driver）、L3=+跨员工编排（goal）；§4.2 明确 flow-driver 是"单员工 SOP 循环"。
- **影响面**：文档 A §3.3/§3.6/§4.3/§4.9；文档 B §4.1 推导表。
- **备选**：无（白皮书定义明确，属纠错）。

---

## 追加决策（2026-08-23 · V0.1 功能点清单裁决批次）

> 来源：部署调研 §12（D-a~D-l，对应 D-017~D-028）+ 2026-08-23 用户七项裁决与 Q-V6 选型（D-029~D-033 及并入各条者）。全部经用户逐条确认。

## D-017 工作台后端 = TypeScript，1.0 Python 编排引擎重写为 TS（D-a） 🟢

- **决策**：工作台后端语言为 TypeScript；1.0 的 Python 编排引擎在 L2 版本重写为 TS，不内嵌 Python 运行时。V0.1（L0/L1）无引擎，重写不在 V0.1 范围。
- **理由**：单体二进制形态（D-018）不允许随包分发第二运行时；TS 与前端同语言、类型契约（shared-protocol）全链路共享。
- **影响面**：白皮书 §4.2/§9（回写 v0.8）、MVP 规划 §5。
- **备选**：Python 后端打包分发（弃：体量与双运行时复杂度）。

## D-018 服务形态 = Bun `--compile` 单体可执行，Node SEA 回退，不用 Electron（D-b） 🟢

- **决策**：单体二进制首选 Bun `--compile`，回退路径 Node SEA；不用 Electron 承载服务。业务代码禁 Bun 专有语法（Bun API 仅限 storage/ 与 runtime/ 两目录，lint 强制）。
- **理由**：QoderWake 实证该形态可行（单文件、冷启动可控）；Electron 体量/内存/攻击面与"管理端常驻服务"定位错配。
- **影响面**：S-01、M0-T1/T3、§7.1 技术栈基线。
- **备选**：Electron（弃，壳与服务解耦后更无必要）；pkg（弃：维护停滞）。

## D-019 更新 = 检查 + 引导下载 + sha256 校验；不购签名证书、不做静默更新（D-c） 🟢

- **决策**：V0.1 更新只做检查、提示、引导下载与 sha256 校验；不购买代码签名证书；不做静默自动更新（托盘壳签名例外 0.2 再裁）。
- **理由**：V0.1 脚本安装可绕开大部分 SmartScreen 弹窗（R-2 缓解）；静默原地替换需要 bootstrapper（D-028 拒于 0.1）。
- **影响面**：U-01~U-04。
- **备选**：购证书 + 静默更新（弃：成本与复杂度，0.2 议题）。

## D-020 单实例语义 = 二次启动幂等；端口被第三方占用退码 78（D-d） 🟢

- **决策**：重复 `workbench start` 幂等（不起新进程，直接打开浏览器到现有实例）；端口被第三方占用 -> 退码 78 + 打印占用进程名；旧实例僵死 -> 检测后清 run/ 接管。
- **理由**：报错退出会让用户以为"没装上"；78 语义（配置/环境错误不徒劳重启）与 OS 守护配套。
- **影响面**：S-06；员工运行与展示设计 §7 回写（幂等而非拒绝）。
- **备选**：二次启动直接报错（弃：体验差）；端口自动换号（弃：端口漂移破坏书签/文档口径）。

## D-021 开机自启并入 OS keepalive，三平台同一份配置（D-e） 🟢

- **决策**：自启与保活合并为一份 OS 守护配置（Windows 计划任务 / systemd --user / launchd），默认开、settings 可关；不设独立自启机制。
- **理由**：自启与守护两套机制并存必然漂移（QoderWake 调研结论）；一份配置一份卸载逻辑。
- **影响面**：S-03、TR-06（托盘壳自启是另一份独立配置，属壳自身）。

## D-022 本地端口可配置，默认 19980，仅绑 127.0.0.1（D-f） 🟢

- **决策**：默认端口 **19980**（2026-08-23 由 19820 改定，本机 19820 被 QoderWake 实占；用户确认）；仅绑定 127.0.0.1、拒绝非 localhost Host 头；前端与文档以实际配置渲染。
- **理由**：端口冲突实证在先；可配置保留换号自由，默认值只是出厂口径。
- **影响面**：S-12、§6；登录设计 v0.2 与管理平台实现架构文档回写。
- **备选**：维持 19820（弃：与既有占用冲突）。

## D-023 更新源 = 平台安装包管理 API，不自建更新服务器（D-g） 🟢

- **决策**：工作台更新检查对接平台 `GET /api/v1/public/workbench-packages`（公开列表/下载/history，响应带 SHA-256；平台 V0.1 已交付，实证于 platform/backend/app/api/packages.py）。
- **理由**：管控平台本就承担分发职责；自建更新服务器是纯增量设施。
- **影响面**：U-01、A-01（平台发现端点同源）。

## D-024 Linux 不提供托盘壳（D-h） 🟢

- **决策**：托盘壳仅 Windows（V0.1）+ macOS（0.2）；Linux 不发壳，headless（CLI/服务）为一等公民形态。
- **理由**：GNOME 无原生 StatusNotifierItem 支持（需扩展），目标用户 Linux 桌面占比低；服务本体在 Linux 照常以 systemd --user 守护。
- **影响面**：§9 明确不做表。

## D-025 推翻调研 D-i：Windows 托盘壳提前进入 V0.1（D-i） 🟢

- **决策**：推翻部署调研「0.1 不含托盘」--Windows 托盘壳（TR-01~TR-09）进入 V0.1，随 M3 交付；macOS 壳仍归 0.2。
- **理由**：用户裁决 2026-08-23（V0.1 要有托盘图标）；托盘是最直观的存活指示（消解 R-8"以为没装上"），也是 keepalive 第二层（探活）的载体。
- **影响面**：§5.6、M0-T4（含托盘 spike）、M1（第一层守护）、M3（托盘全量，工期 0.5-1 周 -> 1-1.5 周）；连带 D-026/D-027。
- **备选**：维持调研原案托盘归 0.2（弃：用户裁决）。

## D-026 托盘壳 = Go + systray，VersionInfo 必填实（D-j / Q-V6） 🟢

> ⚠️ **2026-08-24 状态更新**：M0 spike 实测体量 **6.4MB，超原定 < 5MB 上限**。用户裁决：**当前可接受，上限放宽为 < 8MB**；UPX 压缩（预计 ~3MB）列为后续优化项（EDR 误报风险纳入 R-13 观察）。实证见 [M0 Spike 报告](../research/M0技术验证Spike报告-2026-08-24.md) §5。

- **决策**：壳选型 **Go + systray**（Q-V6，2026-08-23 用户确认）；原生小二进制，体量上限由调研的 < 2MB 调整为 < 5MB（2026-08-24 再放宽至 < 8MB，见上）；VersionInfo 四字段（CompanyName/FileDescription/ProductName/LegalCopyright）必填实；壳与服务仅契约耦合（HTTP /healthz + run/service.json + CLI 子命令），壳零业务逻辑。
- **理由**：一套代码 0.2 可复用出 macOS 壳；~3MB 在独立制品可接受范围（原 < 2MB 上限为纯 Win32 路线设定）；弃纯 Win32（87KB 级但开发成本高、每平台一套）。
- **影响面**：§5.6、§7.1（`workbench-tray/` 为独立 Go module，不进 pnpm workspaces）、M0-T4 spike 验证。
- **备选**：纯 Win32 原生（弃：无跨平台复用，单人工期不划算）。

## D-027 Windows keepalive 双层全量进 V0.1；托盘不得为唯一保活路径（D-k） 🟢

> ⚠️ **2026-08-24 状态更新（S2 实证）**：Windows 层「计划任务」的具体机制修正——**RestartOnFailure 实测不生效**（四组合零重试），崩溃恢复改为**重复触发器模型**（TimeTrigger 每分钟 + IgnoreNew + 幂等 daemon，实测 43s 恢复）；78 语义在 Windows 退化为低频重试（每分钟一次瞬退，Linux 侧 systemd 语义正常）。双层结论与 R-15 红线不变。详见 [S2 报告](../research/S2-Windows守护调度语义Spike报告-2026-08-24.md)。

- **决策**：计划任务（四个默认值覆写：ExecutionTimeLimit=PT0S / DisallowStartIfOnBatteries=false / StopIfGoingOnBatteries=false / StopOnIdleEnd=false）+ 托盘 health 探活，**两层都在 V0.1**（调研原定分期：0.1 只计划任务）。红线：托盘不得为唯一保活路径（R-15），杀壳进程服务必须仍被守护拉起。
- **理由**：托盘进 V0.1（D-025）后探活层自然前移；QoderWake 实证「进程在但 health 未就绪」场景需要探活层（判据 = health 就绪，非进程存活；黄态 skip 不重启，三连失败且超 30s 冷启动预算才重启）。
- **影响面**：S-03、TR-05、M1（第一层）/M3（第二层）。

## D-028 0.1 不引入 bootstrapper（D-l） 🟢

- **决策**：更新应用 = 用户手动执行新安装包；守护配置指向稳定路径（不嵌版本号），为 0.2 bootstrapper 原地替换留位。
- **理由**：V0.1 更新频率低；原地替换涉及文件锁/重启交接，复杂度不值当。
- **影响面**：U-03、S-03。

## D-029 V0.1 底座范围 = CodeBuddy CLI + Qoder CLI（用户裁决） 🟢

- **决策**：UPP 适配 V0.1 只做 CodeBuddy CLI + Qoder CLI；Claude Code -> V0.2；opencode/Cursor 更后。Qoder 两项真机验证（AGENTS.md 原生读取 Q-006、落位目录 B-Q3）为 M2 前置卡点，失败则按 UPP §6.3 矩阵重估或 Qoder 降级 V0.2。
- **理由**：用户裁决 2026-08-23；交接文档本就定「先适配 CodeBuddy 和 Qoder」（hooks 兼容已验证）；CC 是开发自用环境，可后置。
- **影响面**：B-01/B-02、员工安装与底座适配设计回写、R-19。

## D-030 工作台对话界面不进 V0.1（用户裁决） 🟢

- **决策**：会话路由/流式回显/任务建议卡/SSE 通道（员工运行与展示设计 §3.3/§4.4）整体移至 V0.2；V0.1 员工使用方式 = 在底座自带交互（CB/Qoder CLI）内直接派发。
- **理由**：用户裁决 2026-08-23；底座自带对话能力，工作台 V0.1 聚焦员工包的生产与管理闭环。
- **影响面**：§5.5 头注、§9；F-02（对话入口置灰占位，不露死入口）。

## D-031 未登录 = 无任何业务功能（用户裁决） 🟢

- **决策**：未登录仅可达接入引导页；登录 + 激活后全功能；业务 API 一律 401。
- **理由**：用户裁决 2026-08-23；工作台是企业管控体系的客户端，无身份即无授权边界，不做"游客模式"。
- **影响面**：A-08、F-02 路由守卫。

## D-032 平台断联宽限/离线降级 V0.1 不做（用户裁决） 🟢

- **决策**：平台不可达即功能不可用，UI 明示「平台不可达」；不做宽限期、不做离线降级（Q-002 宽限立场推后为 V0.2 议题）。
- **理由**：用户裁决 2026-08-23；V0.1 部署环境（单机内网 compose）平台可用性有保障，宽限/降级机制复杂度后移。
- **影响面**：A-08、F-04（断连仅顶栏常驻告警条，提示而非降级）。

## D-035 启动序列崩溃检测时序修正：crash_detected 仅在接管分支记录 🟢

- **决策**（2026-08-24，框架增量 Wave 2 质量审查 I-1）：`reliability.json` 的 `cleanStop=false` 在**运行中实例是常态**；崩溃检测仍在启动早期读取，但 `crash_detected` 事件仅在单实例判定为 **fresh/takeover（真正接管上一运行）** 时记录，idempotent/starting 分支不记。设计 §3.1 已同步修订（新增 4.5 步）。
- **理由**：原时序下重复 `start`（托盘拉起/用户重开的高频正常操作）每次误记 crash_detected 且 runId 指向健康实例——运维单源日志被噪声淹没，真崩溃信号失去区分度（冒烟断言同步失去区分度）。
- **影响面**：服务本体详细设计 §3.1；lifecycle.ts 实现；冒烟场景 6。
- **备选**：崩溃检测整体挪到判定之后（读+记一起延后）——拒绝：takeover 分支会先 clearRunDir 删掉 reliability.json，读必须在删前。故「早读、晚记」。

## D-033 AgentHub 对接以实证 CLI API 为准（用户裁决 + Keyhub 源码实证） 🟢

- **决策**：skill 获取按本地 Keyhub 源码实证的 CLI API 实现（`GET /api/cli/v1/skills/search`、`/skills/{ns}/{slug}/resolve`、`/versions/{version}/download`、Bearer token，scope skill:read）；V0.1 只消费 SKILL 资产（assetType=PLUGIN 不支持）；AgentHub 地址 + token 为工作台配置项；本地目录导入为降级路径。
- **理由**：2026-08-23 本地源码实证解除 Q-014（API 可用性不确定）；D-010 的「需求契约 + 缺口表」路线就此落地为实证路线。
- **影响面**：E-13、R-20（自托管实例部署环境未定，不影响设计）。

## D-034 Qoder/CB 落位实证修正：skills 走开放标准目录，Agent CLI 是 qodercli 🟢

- **决策**（2026-08-24 本机真机验证，qodercli 1.1.26 / codebuddy 2.137.1）：① Qoder 项目级 skills 落位 = **`.agents/skills/`**（Agent Skills 开放标准约定，非此前假设的 `.qoder/skills/`）；② Qoder 的 Agent CLI 命令 = **`qodercli`**（`qoder` 是 IDE 启动器，探测与 spawn 一律用 qodercli）；③ AGENTS.md 原生读取实测通过（零转换，R-19/Q-006/B-Q3 全解除）；hooks = `.qoder/settings.json` hooks 键（CC 同构）；MCP = 全局 `~/.qoder/mcp.json` + 项目 `.mcp.json`。
- **理由**：临时工作区实测（AGENTS.md 指令注入生效、skill 即放即发现、PreToolUse hook 实际执行）+ bundle 字符串扫描双证据；Qoder 选择开放标准目录（`.agents/skills/`）而非私有目录，反向印证 UPP v0.3 W-20「第一类适配面 = 文件系统约定」与开放标准薄封装路线。
- **影响面**：底座适配详细设计 §2.1/§3/§8/§11；功能点清单 B-02/B-06/R-19。
- **备选**：无（实证事实，属纠错）。

## 追加决策（2026-08-25 · 协同编排拉前与并行开发裁决批次）

## D-036 协同编排+任务看板拉前进当前版本；workflow 编辑器/审批工作台仍留 L2（用户裁决） 🟢

- **决策**：「编排引擎 TS 重写 + 任务看板 + **内置 team 协同编排执行**（类似 agents-team 形态，方便演示）」从 L2 拉前进当前版本；**workflow 编辑器、审批工作台不做，仍留 L2**。
- **理由**：用户裁决 2026-08-25（演示需要提前打通协同编排闭环）；1.0 `feat/demo-4stage-flow` 实证「引擎被动账本 + node-table 配置直驱 + 看板渲染」三层分离可行；内置 team 复用 E-11 向导预置模板（dev-engineer + req-architect），不引入新员工形态。
- **影响面**：功能点清单 §9（L2 行拆分，I3 回写）+ M2/M3 里程碑验收扩展 + F-02（任务看板入口解隐）；路线图 [docs/plans/2026-08-25-requirements-roadmap.md](../plans/2026-08-25-requirements-roadmap.md)。
- **备选**：workflow 编辑器一并拉前（弃：通用编辑能力，演示用不上且复杂度高）。

## D-037 协同编排运行模式先模式一（账本 + MCP 回报 + SSE 看板）；模式二后议（用户裁决） 🟢

> **修订注记（2026-08-26）**：实施路线修订——**模式二（spawn runner 自动派发）拉前进 V0.1 与模式一并行交付**（β-lite 双层驱动：L0 手动=引擎被动性免费副产品 / L1 spawn runner=交付项），依据 [D-041](#d-041)；引擎被动账本 / MCP 回报 / SSE 看板的架构定位不变，I3「模式二裁决点」消失（改双深度验收）。

- **决策**：先做**模式一**——引擎为被动账本（TS 纯库，R1/R2/R3），员工/主控会话经 **MCP 工具**回报推进（员工包 mcp.json 挂引擎工具），看板经 SSE 消费 events；**模式二**（workbench 自动 spawn 底座 CLI 派发）待模式一跑通后另行裁决。
- **理由**：用户裁决 2026-08-25；MCP 层即 UPP 四层适配第一层（员工包天然携带 mcp.json），零新增适配面；模式二依赖 D-030 移出 V0.2 的会话路由（spawn / 流式回显），拉前风险大。
- **影响面**：编排设计文档（I0 产出）；L3 引擎线范围（MCP server）；员工运行设计 §4.4 SSE 通道部分拉前（原随 D-030 移 V0.2，仅看板管线）。
- **备选**：直接上模式二（弃：依赖重，演示时间盒内容不下）。

## D-038 并行开发 = worktree 三线首批 + 两波次；契约冻结纪律；.ieidev 暂不处理（用户裁决） 🟢

- **决策**：交付组织 = 两波次五线——第一批三线 worktree 并行（员工新建 / 员工安装 / 编排引擎），第二批两线（员工运行 / 任务看板）契约稳定后加入；**并行前提 = I0 冻结契约四件套**（E-01~03 / node-table / events / SSE），**契约只在 main 变更**。`.ieidev` 工程记忆仓在 worktree 内**暂不处理**（不建 junction / 不配 sync off，hook 警告属已知行为）。
- **理由**：用户裁决 2026-08-25（三线封顶避免混乱）；五线中四线为绿地新增目录，冲突面集中在注册点与 web-dist，I0 约定化改造后可消；`.ieidev` 被 gitignore 排除故 worktree 天然无记忆目录。
- **影响面**：路线图 §4（波次表 / 端口分配 19982~19986 / web-dist 纪律 / 系统级测试只在 main）；`.gitignore`（`.worktrees/` I0 补）。
- **备选**：五线全开（弃：避免混乱，用户裁决）；单线串行（弃：与交付效率目标冲突）。

---

## 追加决策（2026-08-26 · 真安装包工程补记批次）

> 来源：真安装包工程 Task 7 落档（[计划](../plans/2026-08-25-真安装包.md) / [设计](../plans/2026-08-25-真安装包-design.md)）。D-039 = 2026-08-25 用户裁决（分发形态升级）经七项裁决设计定稿后的决策记录补登；D-040 = 同日品牌重命名裁决（Task R 执行，commit 9e22c36）当时未落决策记录的本批补记。

## D-039 分发形态 = Inno Setup 真安装包，install.ps1 退役（用户裁决） 🟢

- **决策**：正式分发路径从「绿色制品 + install.ps1 轻量安装脚本」升级为 **Inno Setup 真安装包**——setup.exe 单一路径 + 静默参数（`/VERYSILENT` 无人值守，开发者与最终用户同一路径）；卸载数据询问默认保留（`/DELETEDATA=1` 为显式清除通道）；服务恢复与用户解耦（ssPostInstall 自动拉起，安装完成页复选框仅控制「打开工作台页面」）；接受 SmartScreen 弹窗（无签名证书前提）+ signtool 挂点预留。install.ps1 全部安装语义（重装健壮性清理/计划任务注册/哨兵清除/快捷方式/per-user 形态）迁移至 `workbench-installer/workbench.iss` 后退役删除（2026-08-26）。
- **理由**：用户裁决 2026-08-25（托盘单实例修复后直接升级分发形态）；脚本安装缺卸载/静默/覆盖升级三语义，真安装包一次补齐——固定 AppId GUID = 覆盖升级原地换文件、Add/Remove Programs 卸载、静默安装；SmartScreen 为 D-019（不购证书）下的可接受代价（发布说明预告 + sha256 校验，signtool 挂点留后续）。
- **影响面**：TR-09、U-03；`workbench/workbench-installer/`（workbench.iss + build-installer.sh + installer-smoke.sh，S1–S4 冒烟）；功能点清单 TR-09 行注记。
- **备选**：维持 install.ps1 绿色制品路线（弃：无卸载语义、覆盖升级中断窗口不可控、两套落盘逻辑必漂移）；NSIS（弃：无显著收益，换血成本不划算）。

## D-040 品牌重命名 workbench → devzero：标识符级修订，协议端点保留（用户裁决） 🟢

> 补记 2026-08-26：本条裁决 2026-08-25 由 Task R 当日执行（commit 9e22c36），当时未落决策记录，本批补登。

- **决策**：品牌名 workbench → **devzero**，标识符级修订。**改**：A 本机安装标识符（exe 三兄弟 `devzero.exe`/`devzero-daemon.exe`/`devzero-tray.exe`、安装目录 `%LOCALAPPDATA%\Programs\devzero`、数据目录 `~/.devzero` profile、Run 键 `DevZeroTray`、计划任务名 `DevZeroDaemon`、单实例互斥体 `Local\devzero-tray` 及唤醒事件 `.wakeup`）；B 代码工程标识符（go module `devzero-tray`、构建产物名等）；D 显示名 **DevZero**。**不改**：C 层协议端点 `workbench-*`（工作台→平台的开发内部契约标识，含 healthz 的 `app=workbench`，用户裁决保留）；`WORKBENCH_HOME`/`WORKBENCH_SIGN_CERT` 等内部基础设施 env 名；仓内工程目录名 `workbench/`（重命名裁决范围外，见真安装包设计文档头部品牌重命名注记）。
- **理由**：用户裁决 2026-08-25（品牌定名 devzero）；C 层/env 保留 = 内部契约与基础设施标识无用户可见面，改名是纯两侧联动改造成本（裁决范围划定）。本机旧标识已清（`~/.workbench` 用户数据保留不迁移）；安装目录改名随安装器域 Task 1 落地（install.ps1 未随改名且已退役）。
- **影响面**：workbench-service / workbench-tray / workbench-web 全量标识符（commit 9e22c36，31 文件）；安装器域（devzero.ico、`Programs\devzero`、`DevZeroDaemon`、`DevZeroTray`）；后续文档与制品以 devzero 表述。
- **备选**：协议端点一并改名（弃：内部契约耦合面，纯改造无收益）；维持 workbench 名（弃：用户裁决定名）。

---

## 追加决策（2026-08-26 · 协同编排设计 I0-2 批次）

> 来源：协同编排设计 brainstorm 会话逐问对齐（2026-08-25~26，用户裁决），[设计文档](../plans/2026-08-26-协同编排-design.md) §2 十二项裁决按主题归并为 D-041~D-048；评审通过 2026-08-26。前置消险输入：[MCP-server-Bun可行性Spike报告](../research/MCP-server-Bun可行性Spike报告-2026-08-25.md)、[S4-MVP员工落地方案对比与Linux多底座Spike报告](../research/S4-MVP员工落地方案对比与Linux多底座Spike报告-2026-08-25.md)、Multica 源码读码（reference-projects/multica）、1.0 仓 `feat/demo-4stage-flow` @ 68d58ab2（员工与团队创建 v2）。

## D-041 驱动形态 = β-lite 双层（L0 手动免费 + L1 spawn runner 进 V0.1）；LLM 主控取消（编排=确定性驱动器，智能=LLM，兜底=人）（用户裁决） 🟢

- **决策**：模式二拉前——spawn runner 为 V0.1 交付项（demo 主演全自动），L0 手动驱动为引擎被动性的免费副产品（天然 fallback + 调试形态）；LLM 主控会话取消：SOP 调度由 service 内确定性驱动器（事件订阅→查表决策循环）承担，LLM 只保留在员工干活与 gate 评审会话（按需 spawn），异常兜底=人（任务 blocked + 看板黄条 + 手动接管）。**修订 D-037 实施路线**。
- **理由**：node-table 本是有限状态机，1.0 的 L0 主控 LLM 是「执行人写死剧本的贵替身」（demo 提示词.txt 即证据）——确定性工作还给代码，token 三层根因（主控长会话累积 / 子代理产出回灌 / 全表常驻）全部消除；白皮书 §4.2 编排驱动本就是运行时能力非员工；四大前置风险（MCP/spawn headless/员工落地/派单逻辑）已全部被 spike/S4/Multica 读码消险；Multica 同构（调度是 daemon 代码，LLM 只在判断位）。
- **影响面**：路线图 I3「模式二裁决点」消失（改双深度验收 U3/U5）；L3 线范围（driver + spawn runner）；内置 team 不含主控员工；员工包 AGENTS.md SOP（自报三件套）。
- **备选**：LLM 薄主控（段级激活，弃：token 与演示稳定性均劣于确定性驱动）；纯人肉桥接（弃：演示叙事弱）；LLM 主控 spawn 子会话（弃：递归复杂且 headless 会话内调度未证）。

## D-042 引擎通道 = MCP 主路挂 service `/mcp`（@hono/mcp Streamable HTTP）+ HTTP 同源降级；devzero CLI 后移（用户裁决） 🟢

- **决策**：员工回报主通道 = MCP HTTP streamable 形态挂 workbench-service `/mcp` 路由（同端口同起同停）；HTTP API 写面与 MCP 工具 1:1 同源（同一 engine 函数双协议适配）；不采用 stdio 直连（账本分裂）；devzero CLI（task 子命令组）后移 V0.2+。
- **理由**：MCP spike 四问全实证（Bun 双形态起 server / CB+qodercli 真机列调返回 / 失败不阻塞会话），路线图头号风险解除；MCP=function calling 结构化调用（LLM 可靠性最优 + 白皮书四层适配第一层自证），1.0 CLI 报错对照表三条坑在 MCP 形态整体消失；账本单点（所有底座会话连同一 service 引擎实例）；降级语义按 spike 定性——service 死→员工停跑上报不降级 curl，HTTP 回报价值场景=未来底座不支持 MCP http 形态。
- **影响面**：hono-adapter 受控例外口（/mcp 直挂，业务路由纪律不变）；员工包/工作区 mcp.json = url 型条目 `{"type":"http","url":"http://127.0.0.1:19980/mcp"}`；员工会话权限模式预配（spawn 带 -y 等，spike §3.4）；路线图 §6 风险表 MCP 行 I3 移除。
- **备选**：专用 CLI 派单（Multica 形态，弃：Windows 进程启动税 + agent 友好化工程 + 四层适配叙事缺位）；纯 HTTP+curl（弃：LLM 拼 shell 可靠性最差）；stdio MCP（弃：每底座 spawn 独立引擎进程账本分裂）。

## D-043 员工落地 = S4 三层模型 + 身份驻地分离（员工 home config 域身份 + 共享任务工作区 + 工作区项目级 .mcp.json）；底座多选=安装目标集合（用户裁决） 🟢

- **决策**：EmployeeSpec（底座无关唯一事实源）→ Deployment（spec×host×home：`~/digital-staff/<host>/<id>/`，registry 台账）→ Session（launch(deployment, workdir, task)）；身份锚 per-host 由 adapter 声明（CC=config-domain / Qoder=project-file / launcher 兜底）；任务工作区共享产物 + 一份项目级 `.mcp.json` 指回引擎；创建向导底座多选=安装目标集合声明，spawn 按 (emp, base) 查 Deployment；CC/CB/Qoder 三底座并列（全 CC 系，demo 不押注单一底座）。
- **理由**：S4 实证（CC config-domain 3/3 + skills 同域；Qoder project-file 4/4+2/2；Linux symlink 被 realpath 击穿 P-9 判死目录挂载）；身份互斥铁律天然满足（每员工独立 config 域）；人设传达与派单通道无关——身份靠安装落位、任务经派单 prompt（白皮书 §5.3，1.0 --emp prompt 注入路线已否决）。
- **影响面**：B-03 完整规格（S4 三层 + 事务化八条）；adapter 契约扩展（launch 增 permission/model/effort + listModels）；L4 花名册与 registry 统一口径；CB 身份面 429 解除后复验（开放问题）。
- **备选**：三 workspace 身份驻地分离+prompt 传产物路径（弃：产物路径传递别扭）；单 workspace 顺序切换当值（弃：切换打断演示节奏）；子目录身份叠加（弃：依赖底座未实证行为）。

## D-044 内置 team = 七员工（1.0 新 demo 花名册口径，req-architect 拆分）；demo-flow = 五阶段 12 节点，三角色位全部引擎化（用户裁决） 🟢

- **决策**：内置 team 七员工——req-clarifier（需求澄清师）/ sys-engineer（系统工程师）（二者由 req-architect 拆分）/ dev-engineer / reviewer-expert / sec-compliance（安全合规审核员·secretgate）/ sec-design（安全设计审核员·sec-scan-design）/ sec-code（代码安全审核员·sec-scan-code）；测试工程师不预置。demo-flow = 准入-需求-设计-开发-准出五阶段 12 节点：1.0 v2 三角色位全部引擎化——主责=action 节点 emp、评审位/安全位=gate 节点 reviewer；准入/准出=安全合规审核员 action 节点对称（准出检查点占位，具体内容后定）。
- **理由**：对齐 1.0 新 demo（员工与团队创建 v2：五卡链 + 三安全员必选 + 安全位定死）；1.0 的三角色位是 HUD 演示态台账未接引擎（spec 明确「不启动 flow」），本设计完成引擎化翻译且引擎零特殊语义（R2/R3 原生 gate）；「安全不降档」由拓扑保证（安全 gate 永在表内），比 1.0 运行时铁律更硬；1.0 花名册 8 员工中测试工程师不在五阶段链故不预置。
- **影响面**：E-11 模板库按 7 员工扩编（L1 回写）；三安全 skill 素材（reference-projects/安全skill/ 四 zip + 1.0 仓 secretgate.py，TS 移植）；sec-scan-code .venv 依赖装员工 home 不进包；V0.2 reviewer 体系演进路径保留。
- **备选**：三人 team（req/dev/sec-reviewer，弃：用户指定按 1.0 新 demo 七员形态）；准出=人工闸 gate 锁死（弃：用户指定改 action 与准入对称）；八员工全预置（弃：测试工程师不在链）。

## D-045 契约三件套定稿：node-table（+stage/emp/prompt/model_tier + 表快照配置直驱）/ events（六类 + OTLP span 基因 + 热冷分层）/ SSE（seq 重放 + 看板零硬编码）（用户裁决） 🟢

- **决策**：**node-table**：1.0 字段面 + 四新字段（stage 叙事分组 / emp 主责员工 / prompt 派发指令模板带变量插值 / model_tier 阶段内置档位）；run 级表快照（在跑任务不受改表影响，下个任务用新表）+ 配置直驱红线（改表零代码生效，1.0「CLI 每次传 --table 中途改表」脏点修复）。**events**：六类（1.0 transition/gate/dispatch 原生 + run.created/completed/aborted）+ OTLP span 基因（trace_id=task_id / parent_seq 因果链 / seq=行号）——全量 span 格式与导出器 V0.2+，团队共享/平台上报走消费侧导出不改本体；**账本热冷分层**：活动任务账本在 `<workspace>/.devzero/tasks/<id>/`（员工会话零成本可见，对标 1.0 .ieidev）+ 完成归档 `~/.devzero/archive/`。**SSE**：Last-Event-ID=seq 重放（跨 service 重启从文件恢复）+ 看板零硬编码消费契约（按表快照渲染，换表自动跟随）。
- **理由**：员工会话在 cwd 树内即得全上下文（表/状态/handoff），L0 手动驱动自洽（1.0 核心体验）；历史不随工作区存亡；引擎与 SSE 同进程故账本位置不影响推送（早先「零 watcher」分离理由不成立的修正）；OTLP 基因现在埋（丢了补不回来）、格式不做（V0.1 无消费方且 attributes 嵌套破坏 cat/grep）；单级 task 模型（砍 1.0 feature 两级台账）对齐看板任务模型。
- **影响面**：I0-4 契约冻结落仓真源（node-table/events/SSE schema 文件）；L3/L5 消费（L5 fixture 先行）；L4 SSE 通道关系（分立 I3 评估合流）；zod 校验三层（结构/跨字段/运行时）。
- **备选**：纯 OTLP span 格式（弃：无消费方 + 破坏人类可读）；账本只在 service 目录（弃：员工看不到任务上下文）；1.0 两级 feature 台账照搬（弃：story/reconcile 留 L2，单级对齐任务模型）。

## D-046 发起操作面（mode team/solo + 底座 + 模型四层解析链 + 内置档位复选框）；人工闸 = 对话式放行（底座会话即 V0.1 对话界面）（用户裁决） 🟢

- **决策**：发起任务表单 = 执行主体（team 选 flow 表 / solo 选员工）+ 底座 + 模型/努力档位（数据源=底座配置经 adapter listModels）+「使用流程阶段内置档位」复选框 + 工作区 + 需求文本；模型解析四层链：节点 model_tier（勾选生效）> 任务级选择 > 员工 manifest 档位（tier-map，1.0 五档语义）> 底座默认；solo 模式引擎动态生成单节点表（一切任务皆 flow，单员工任务同有事件与看板进度）。人工闸：advance 进入 human_gate 节点自动 gate_paused，**放行主通道=底座会话对话式**（工作区 TASK.md/AGENTS.md 可发现性 → 人说「批准」→ engine_confirm_gate），看板按钮为辅；底座会话 = V0.1 的「对话界面」（白皮书 §4.1⑥ V0.2 能力零成本先行）。
- **理由**：模型选择从员工级移任务级（同一员工不同任务不同档，比 1.0 更灵活且与其 v2「去档位」方向一致）；1.0 solo-demo 模板语义的 2.0 等价物即动态 solo 表；对话式操作是产品主叙事且零成本（.mcp.json + 可发现性文件已在工作区）。
- **影响面**：看板发起页表单（L5）；flow-state 状态集 +gate_paused；API 面 +confirmGate（11 操作）；createTask 落 TASK.md + AGENTS.md 引用行。
- **备选**：单员工任务绕引擎直 spawn（弃：两套任务模型）；人工闸仅看板按钮（弃：对话式体验与演示效果更优）；effort 直接透传底座旗标（弃：映射收敛 adapter，编排层传抽象值）。

## D-047 skill 获取 V0.1 = 本地 zip 上传，AgentHub 拉取后移（用户裁决） 🟢

- **决策**：E-13 的 V0.1 主路径改为本地 zip 包上传（解包 → SKILL.md frontmatter 校验 → 复制进员工包 skills/ 锁版本）；AgentHub 真平台拉取后移。
- **理由**：用户裁决；sec-scan-design / sec-scan-code 两个 zip 正是首批素材形态（reference-projects/安全skill/）；1.0 演示的「假拉取」教训——2.0 不做假的，真对接非 V0.1 演示必需。
- **影响面**：E-13 V0.1 主路径（L1 回写）；sec-scan-code 的 .venv（40MB）经 install 脚本装员工 home 不进包分发。
- **备选**：AgentHub 拉取做进 V0.1（弃：非演示必需，后移）。

## D-048 「引擎接入 service（HTTP API+SSE+MCP+driver）+ spawn runner(mock launch)」从 I2 提前并入 I1/L3 线（用户裁决） 🟢

- **决策**：路线图任务顺序优化——引擎接入 service 的全部进程内模块（HTTP API / SSE / MCP 挂载 / 确定性驱动器 / spawn runner 以 mock launch 先行 TDD）随 I1 的 L3 线交付；I2 只剩 spawn runner 真机联调（接 L2 adapter 的 launch() 实现）+ 内置 team 端到端 demo + M2 验收。
- **理由**：该组模块只依赖 L3 引擎纯库（routes/engine.ts 新文件，与 L1/L2 零冲突面，I0-5 分域注册已就位）；MCP 可行性已 spike 消险；I2 关键路径缩短，spawn runner 真机联调窗口前移更早暴露底座差异；L3 范围扩容不新增 worktree，符合 D-038 三线封顶。
- **影响面**：路线图 §5 任务表（I1 L3 行 + I2 行，I3 回写）；L3 线 plan 范围。
- **备选**：维持原 I2 排期（弃：对 L1/L2 的串行等待无必要）。

## D-049 平台地址未配置 = 开发环境：免登录全功能（用户裁决） 🟢

- **决策**（2026-08-27）：config.json `platform.baseUrl` 默认改**空串**（原 T8 默认 `http://127.0.0.1:18000` 废止）；**空串 = 开发环境语义**——session 域新增 `GET /api/state` 注入开发态（authenticated + ACTIVE + 开发用户「开发模式」），web 守卫/顶栏零改动全放行；已配置平台地址 = 生产语义 → `/api/state` 401（与「端点不存在」的现行行为等价，登录链路随 A 系列迁移提供）。PUT `/api/config/platform` 允许空串（清除配置回开发环境），GET/PUT 响应增 `devEnvironment` 标志。单一判据 = `schema.isDevPlatformBaseUrl`，A 系列落地后服务端会话 guard 沿用同一判据注入开发身份。
- **理由**：用户裁决 2026-08-27（本地开发免登录方便调试）；判据复用 T8 平台地址配置（D-13）不引入新环境变量，配置面单一真相源；开发态桥接使 A 系列未迁移期间前端全链路可调试（此前 /api/state 不存在，守卫一律按未认证只能看接入页）。
- **影响面**：config schema / config 路由 / routes/session（新域，D-049 桥接态，A 系列迁移时原地升级为真实会话端点）/ web 零改动；A-08/D-031 注记为**生产语义**（开发环境为显式例外）；S-12 Host 白名单与 127.0.0.1 绑定不松动；T8 设计文档 baseUrl 默认值注记待回写。
- **备选**：环境变量 `DEVZERO_DEV_NO_AUTH`（弃：判据分裂，与平台地址配置形成双真相源）；本地 compose 平台 + 测试账号真实登录（保留为集成验收路径，不替代日常调试旁路）。
## D-050 看板 SSE 消费层可注入 + 连接状态机 + seq 按 task_id 分域去重 🟢

- **决策**：workbench-web 的 SSE 消费（`api/engine-stream.ts`）对 `EventSourceLike` 最小面编程（addEventListener/readyState/onopen/onerror/close），工厂可注入——jsdom 无 EventSource 的测试前提 + 网络层 mock 的结构保证；连接状态机 connecting/live/reconnecting/closed（晚注册回放初态）；**seq 去重按 task_id 分域**（seq 是 per-task 事件文件行号，§7.3——全局累计会把新任务同号帧误判重放吞掉，走查实捕修复含回归锚）。
- **理由**：契约消费侧必须先于引擎可测（fixture 先行策略的结构基础）；分域去重是 events.jsonl 逐任务文件形态的直接推论。
- **影响面**：L5 全部 SSE 测试；引擎线联调时真实 EventSource 直接换入（零代码改动）。
- **备选**：封装 reconnect 逻辑自研（弃：原生 EventSource 自动重连 + Last-Event-ID 已覆盖 §8 契约）。

## D-051 看板归并层 applyEvent 纯函数 + 剧本重放测试 = 行为规格 🟢

- **决策**：事件流→看板状态（tasks/feed）的唯一映射点为纯函数 `applyEvent`（stores/kanban.ts）；四剧本（正常推进/闸位暂停/reflow/终止）全量重放断言即 KB-01 行为规格；混流按事件自带 task_id 分拣（未知任务建占位卡）；错误常驻（dispatch error / abort reason 记 blockedReason，非 toast）。
- **理由**：单点映射使 UI 组件零事件逻辑（只消费 store 态）；剧本即规格的 TDD 锚，引擎线真实事件流跑同一套断言即联调。
- **影响面**：看板全部组件的数据源；剧本生成器（fixtures/scenarios.ts）成为契约消费者侧锚。
- **备选**：组件内各自响应事件（弃：状态散落多处，重放测试无从下手）。

## D-052 表快照与员工 display 映射经 getTask 随任务下发 🟢

- **决策**：看板的表快照（阶段/节点渲染源）与员工 display 名映射经 `GET /api/engine/tasks/:id` 响应携带（`{task, table, employees}`），前端在表未到时渲染骨架态。
- **理由**：run.created 事件载荷不含表（§7.3）；零硬编码纪律①要求节点/阶段全部按表渲染，表必须先于渲染到达。
- **影响面**：引擎线 HTTP getTask 响应形状（契约歧义 A/B 的先行口径，联调时对齐修订）；L4 registry 查询面待定。
- **备选**：表随 run.created 事件内嵌（弃：事件载荷膨胀且破坏六类契约）；前端内置表（弃：违背零硬编码）。

## D-053 mock 三分法与硬约束：页面纯真实接线，mock 绝不自动启动（v0.1 页面内演出形态被推翻） 🟢

- **决策**：用户裁决（2026-08-27）——「演示是真实的，不做页面内 fixture 演出」。三分法：①页面代码零 fixture 分支（原生 EventSource + httpEngineApi，EventSource 缺失优雅降级）②mock 测试替身（MockEventSource/剧本）只存在于 vitest 进程③本地 mock（scripts/mock-engine-server.ts，state+engine+SSE 全套）**只能手动启动**。**硬约束三重防回归锚**（test/dependency-audit.test.ts）：src/ 产品代码零 import fixtures/scripts、npm scripts 零 mock 命令、vite dev 默认代理锁 19980（真实 service）；构建期 bundle grep 实证为第四道闸。
- **理由**：页面内演出给人造假感且与「引擎真实推进是 demo 红线」冲突；mock 下沉网络层后页面即真实形态（引擎通了零改动）；机器化验证而非口头承诺（用户二次强调后固化为审计测试）。
- **影响面**：v0.1 的 use-kanban-runtime/FixtureControls/fixture-service 删除；开发调试需手动起 mock + 显式 VITE_PROXY_TARGET。
- **备选**：保留 ?fixture=1 隐藏开关（弃：仍有假数据通道）；mock service 挂 npm script（弃：违背绝不自动启动硬约束）。

## D-054 闸位停靠看板辅按钮实做 confirmGate；对话式放行为主通道 🟢

- **决策**：gate_paused 常驻 amber 告警条（含通过/驳回辅按钮 → POST confirm-gate）+ 引导文案指向底座会话说「批准」（主通道）；人工 confirm 进评审流水（1.0 需求讨论 6 教训）。
- **理由**：§5.3 对话式放行为主叙事；辅按钮保底演示可操作性；人工行进流水是审计完整性要求。
- **影响面**：AlertPanel 组件；引擎线 confirmGate HTTP 端点（已按 §9.3 消费）。
- **备选**：仅对话式无按钮（弃：演示可操作性）；仅按钮（弃：主通道是对话式）。

## D-055 任务看板 UI 对齐 1.0 demo 任务看板形态（左工作区树 + stageband + 事件观战） 🟢

- **决策**：用户裁决（2026-08-27）——看板样式参照 1.0 demo 分支（agents-team feat/demo-4stage-flow ieidev_hud/frontend.py）：左列 330px **工作区目录**分组任务树（可折叠，状态图标/迷你进度）+ 右侧单任务详情（topbar/empband 员工横条/**stageband 阶段步进横幅**（表 stage 驱动零硬编码保留，done 绿底/active 呼吸光垒/gate_paused amber + gate 末格只报数不给进度条）/**黑底事件观战**（六类事件中文摘要 tail 式滚底，真 stdout 观战留引擎线）/告警卡/gtl 评审流水）；多任务大卡堆叠与泳道形态废弃。
- **理由**：1.0 形态经实战验证的观战心智（单任务聚焦 + 全景阶段横幅）；事件观战等价替代 procwatch 动作流（V0.1 事件流有数据、stdout 无）。
- **影响面**：看板组件层重写（数据层/消费层不动）；左列分组键 = 事件 task.workspace 字段。
- **备选**：保留 v0.1 多任务大卡+泳道（弃：用户裁决对齐 1.0）；照搬 1.0 四阶段（弃：我们是五阶段属表内容差异，零硬编码下自动成立）。---

## 追加决策（2026-08-27 · L3 引擎线执行期裁决批次——用户追认）

> 来源：L3 通宵执行（[执行记录](../plans/2026-08-26-l3-engine-execution.md) §2 占位裁决，用户 2026-08-27 追认）；夜间全自主授权条款「遇执行期裁决落占位号不停下」。
> **编号勘误（合流时）**：占位 D-049~051 与 main 并行线撞号（D-049 已被「平台地址开发环境」占用、D-050~055 为 L5 看板线批次）——本批顺延为 D-056~058（worktree 分支内全部引用已同步修订；各 commit message 中历史编号不回改，以本注记为准）。

## D-056 SSE 事件 id 采用复合形式 `<task_id>:<seq>`（执行期裁决，用户追认） 🟢

- **决策**：SSE 帧的 `id:` 行用 `<task_id>:<seq>` 复合形式；断线重连 Last-Event-ID 解析回「指定任务 seq 之后」的存量回放再接增量。
- **理由**：设计 §8 的 `id:<seq>` 是单任务视角字面——seq 为 per-task 计数（各任务从 1 起），多任务订阅（看板默认全量）下跨任务不可比，单一 Last-Event-ID 无法定位。1.0 demo 的 SSE 实为「变更通知信号 + 全量重拉 collab.json」弱形态（无事件载荷/无 id/无重放），无此问题也无参照可抄；2.0 设计已裁决升级为事件推送模式，复合 id 是该模式在多任务下的唯一可行解。
- **影响面**：events.jsonl 本体与 L5 fixture 零影响；仅 L5 消费 SSE 时重放解析按 `task:seq` 格式；帧格式/心跳/重放语义与设计 §8 其余条款一致。
- **备选**：退回 1.0 通知+全量重拉（弃：架构倒退，废 §8 契约与 L5 fixture）；全局 seq（弃：需改 events.jsonl 结构，影响面大）。

## D-057 spawn 失败挂起语义 = 驱动器侧停派（执行期裁决，用户追认） 🟢

- **决策**：驱动器 spawn 失败重试耗尽后：挂起记录于 `Driver.suspendedTasks()`（含原因，看板黄条/日志消费位），任务状态保持 `in_progress` 停在当前节点——L0 人接管或驱动器无状态重启天然重试。
- **理由**：引擎 11 操作契约无 setBlocked（未列）；挂起不做引擎级状态污染，人工兜底通道天然存在（设计 §5.1「重试 1 次→挂起」原意）；重启续推由驱动器无状态语义保证（M3 崩溃恢复锚）。
- **影响面**：驱动器实现与测试锁定（driver.test.ts 挂起/重启续推两用例）；若未来需引擎级 blocked 走契约演进。
- **备选**：引擎加 setBlocked 操作（弃：契约未列，扩操作面）；挂起即 aborted（弃：误杀可恢复任务）。

## D-058 账本布局回归 D-045 + tasks-index.json 索引件（执行期裁决，用户追认） 🟢

- **决策**：T4 首版实现的 dataDir 侧布局回退为 D-045 工作区布局（活动账本 `<workspace>/.devzero/tasks/<id>/`、完成归档搬 `<dataDir>/archive/tasks/<id>`）；新增 `<dataDir>/tasks-index.json` 轻量索引（workspace/flow/title/status/archived/archive_path）作为定位与列表之源——工作区布局下活动目录分散各 workspace 的必要反查件。
- **理由**：D-045 布局的核心收益=员工底座会话 cwd 树内零成本可见账本（L0 手动驱动自洽）；索引是原裁决布局下漏设计的必要补充（list/getTask 定位）；索引原子读写+坏索引显式 LedgerError 不静默清空。
- **影响面**：设计 §7.1 补索引件说明（随本批回写）；ledger 实现+18 测试锁定；归档三级占用兜底（rename→重试→copy+rm，rm 失败→索引已切归档侧+LedgerError 留人工清理）。
- **备选**：维持 dataDir 布局（弃：架构裁决不可静默推翻——T4 首版系任务书错误指令，reviewer 已拦截）。