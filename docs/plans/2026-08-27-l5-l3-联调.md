# L5×L3 看板×引擎联调记录（2026-08-27 · feat/l5-integration worktree）

> 任务：看板接真实引擎端到端跑通 + 契约歧义六条对齐落档 + UI 按 1.0 demo 协同编排形态补强
> 环境：service 19980（临时 WORKBENCH_HOME）+ web dev 19986（Vite 代理→真实引擎面）+ Playwright 浏览器实测
> 裁决批次：**D-060 / D-061**（[决策记录](../design/设计决策记录-2026-08-13.md)；任务书「D-059 起」过时——L2 线已占 D-059）
> 基线 → 终态：engine 115→**116** / service 232→**233** / web 350→**379**（+机检/泳道/守卫桩等）全绿 + 三包 typecheck 0

---

## 1. 端到端跑通实证（真实环境，非 mock）

| # | 场景 | 结果 |
|---|------|------|
| E-1 | 浏览器表单发起任务（KB-02）→ createTask 202 → SSE run.created 实时出卡 | ✅ 标题/flow/display_name/workspace/五阶段 StageBand 实时渲染（表快照经新端点 :id/table） |
| E-2 | curl 手动推进全链（照 demo-run-events.jsonl 序列：dispatch-start→advance→record-gate×7→FAIL 回流→重派→complete） | ✅ 看板实时跟随：阶段进度条 100% 逐段推进、员工卡出现/消散（中文名映射）、闸 4/5→5/5、评审流水 7 条（含 FAIL+issues 展示）、事件观战 35 条滚动 |
| E-3 | g-sec-code FAIL 回流注入（OWASP A03 issue） | ✅ 回流 transition 渲染 + 返工视图（重访重置后） |
| E-4 | 页面重载 | ✅ 初值拉取事件溯源式重建——5 任务全量回来（含已归档任务表快照，0 console errors） |
| E-5 | 泳道页（T4）：抽屉建需求 → 拖入待办池（HTML5 DnD）→ 真实 createTask | ✅ toast「已发起编排」+ 草稿出池 + 待办池出卡（SSE 补全） |
| E-6 | 泳道点卡 → /kanban?task=<id> 双层衔接 | ✅ URL query 初始选中（evaluate 断言） |
| E-7 | SSE 空闲存活 | ✅ :ping 15s 心跳持续、连接态常驻「实时连接」 |

## 2. 契约歧义六条落定（D-060，详见 L5 设计 §10 结论表）

| # | 结论 | 实现 |
|---|------|------|
| A 表快照通道 | 引擎补 `GET :id/table` 只读端点（归档任务可读——Ledger 暴露 taskDirOf 经索引 resolveTask）；getTask（TaskView）不含表；前端双调用装配（表失败容错 undefined 骨架态） | routes-engine 红绿 + engine getTable 归档红绿 |
| B display 映射 | 前端 `STATIC_EMPLOYEES` 静态七员工映射（设计 §4.1 花名册）；**登记 L4 待办**（registry 查询面就位后替换） | engine-api.ts |
| C solo 清单 | 同 B 静态映射；同 L4 待办 | CreateTaskModal 员工下拉 |
| D gate node 语义 | **引擎为准：node=闸位节点 id**（recordGate 记 state.current_node；covers 目标不进事件）。UI 零消费影响；fixture 修订对齐 | scenarios.ts runGate |
| E 模型下拉源 | 跳过；**登记 L2 待办**（listModels 未交付） | — |
| F dispatch status | **引擎为准：'done'\|'blocked'**（zod enum 同源）；fixture 'ok'\|'error' 废除 | store/观战摘要/scenarios 契约锚同步 |

## 3. 联调新发现（六条之外——D-061）

| # | 发现（main 实测） | 修复 |
|---|------|------|
| G 事件载荷无 task_id | 引擎事件只带 trace_id（设计 §7.3 本义；fixture 冗余自带致守卫误判必填）→ **全部真实帧被静默丢弃**（SSE live 零事件到达——本会话最重实锤） | parseEngineEvent 过检后 trace_id 兜底注入；display_name/final_node 可选化（引擎条件展开） |
| H R3 回流 reflow=false | 引擎 R3 gate 回流的 transition.reflow=false（标志只属 R2 机械回流）→ 看板原单口径不重置 doneNodes，返工期进度虚高 100% | applyEvent「重访即返工」：落点已在 done 集即重置 |
| I 初值拉取缺失 | stream.ts 契约注释「首屏经 GET tasks 拉取」未实现 → 重载即空板 | store.hydrate：清单+逐任务事件重放（事件溯源式）+ applyIncoming per-task seq 幂等 |
| 工程修复 | Bun.serve 默认 idleTimeout 10s < 心跳 15s → SSE 空闲流被杀，EventSource 经代理收 500→「已断开」 | main.ts `idleTimeout: 255`（mock 侧同款修法补齐真实 service） |

**其他实捕**：getFlows/getTask 未拆 {ok,…} 信封（前端拿整包当数据用——流程下拉空/表快照崩）；StageBand 闸计数按事件计数超分母（6/5）；归档任务表快照 404；jsdom 无 scrollTo（unhandled rejection 退出码 1——main 既有）。

## 4. T4 泳道任务列表层（1.0 协同编排形态·抄形不抄管线）

- **抄（形态/交互语义）**：五列泳道（彩点+名+计数徽章）、仅需求池→待办池可拖（=发起编排；派单失败留池红块可重拖）、任务卡六件套（标题/tag 组/错误块/停靠提示/决策按钮组·驳回必填 note/进度条+阶段链 pill 三态/卡脚员工）、创建需求抽屉（40% 宽滑入）
- **不抄（数据通道）**：1.0 的 SSE tick+2s 全量重拉 collab.json → 2.0 SSE 事件推送（D-056 复合 id）+ hydrate 事件重放；1.0 的 POST /collab/* → 2.0 /api/engine/*（createTask/confirmGate）
- **双层互补**：泳道=任务间全景（/kanban/board），点卡进任务看板详情（/kanban?task=）；侧栏「协同编排」一等菜单与「任务看板」同级（用户裁决 2026-08-27）
- D-036 澄清：协同编排=任务执行全景页 ≠ workflow 编辑器/审批工作台（仍留 L2）——side-nav 断言措辞已同步

## 5. 遗留与待办登记

| 项 | 归宿 |
|----|------|
| 员工清单查询面（B/C 真实源） | L4 registry |
| 模型下拉真实源 listModels（E） | L2 adapter |
| 引擎 main.ts 未装配 Driver——自动驱动留 I2（本联调用手动操作通道 curl/evaluate 推进，语义已全验证） | I2 |
| MCP /mcp 真机底座连接（I2）；SDK client 冒烟本轮未跑（HTTP 面 12 端点全实证） | I2 |
| Playwright MCP drag 工具 ref 失效问题（页面异步重挂载窗口）——evaluate 派发 DnD 事件替代 | 工具经验 |
| bash curl GBK 编码创建中文任务致乱码（探测数据已清理；浏览器路径中文正常）——演示一律走表单/拖拽 | 操作纪律 |
| service 直连 19980 的 SPA 深链 404（/kanban、/employees 同 404——shell 只在 / 伺服 index.html，深链回退未实现；dev 走 19986 Vite 代理无此问题） | I0-5 shell 域演进（非本线回归，实测 / 与深链统一行为） |

截图存证：`screenshots/2026-08-27-l5l3-board-swimlanes.png`（泳道全景，五列 + 干净任务数据）。

## 6. 提交清单（feat/l5-integration，未合流）

| commit | 内容 |
|--------|------|
| 1ec2c28 | fix(web) EventWatchPanel scrollTo 方法级可选（jsdom，main 既有） |
| 7affb29 | fix(service) Bun.serve idleTimeout 255——SSE 空闲掐断 |
| acdf4ad | fix(web) getFlows 拆 {ok,flows} 信封（T2 拦路 bug） |
| d204e74 | fix(web) 事件守卫对齐引擎真源——task_id trace_id 兜底（含 T5 机检锚 engine-real-events.test.ts） |
| 7f90af9 | feat(l5x3) 歧义 A 落定：引擎 :id/table 端点 + 前端双调用装配 + 重访重置 + STATIC_EMPLOYEES |
| 400cdd4 | feat(l5x3) 初值拉取 hydrate + 闸计数去重 + 引擎 getTable 兼归档 |
| 4c5654b | docs(l5x3) 契约六条落定回写（D-060/D-061 + 设计文档三处） |
| 9d3ca48 | feat(l5x3) T4 泳道任务列表层（含 D/F 契约修订——gate node 引擎为准 + dispatch status done\|blocked 的 scenarios/store/摘要同步） |
| （本提交） | 联调记录 + 回归收口 |
