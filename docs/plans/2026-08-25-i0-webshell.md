# I0-5 Web 壳补全实施计划

> 日期：2026-08-25
> 状态：✅ 执行完毕（2026-08-26 收官；六任务全 commit 于 i0-webshell，全量测试绿 + 三段冒烟过；G-1~G-5 待用户裁决不受影响）
> 设计：[2026-08-25-i0-webshell-design.md](2026-08-25-i0-webshell-design.md)（结构决策 D-1~D-12、缺口 G-1~G-5 以设计文档为准，本文不重复）
> 执行方式：subagent 真红绿 TDD（每任务先写失败测试→红→最小实现→绿→重构）；单任务完成即 commit 到 `i0-webshell`
> 基线：service 101 + web 11 全绿（开工前已验证）；每任务收尾跑全量测试，红则不得提交
> 纪律：端口 19982；web-dist 不提交；不碰 A 系列服务端；main 上安装器会话的未提交文件与本线无交集

---

## 任务表

| # | 任务 | 包 | 产出 | 提交 |
|---|------|----|------|------|
| T1 | 注册约定化 | service + web | `src/server/routes/{infra,shell,index}.ts`；`src/router/routes/access.ts` + index 改造 | `refactor(server,web): 路由注册分域约定化` |
| T2 | F-03 登录与接入页 | web | `api/access.ts` + `views/AccessView.vue` + `components/access/*` + session store 骨架 | `feat(web): F-03 登录与接入页` |
| T3 | F-02 导航骨架 + 守卫 | web | `router/routes/{employees,bases,kanban}.ts` + `Layout.vue` + `components/nav/SideNav.vue` + 守卫 + 占位页 ×3 | `feat(web): F-02 导航骨架与登录态路由守卫` |
| T4 | F-04 顶栏全局态 | web | `components/nav/TopBar.vue` + `api/access.ts` 扩展 interpretPlatformStatus + 检查更新占位 | `feat(web): F-04 顶栏全局态` |
| T5 | engine 空壳 | engine | `workbench/workbench-engine/*`（@devzero/engine）+ bun.lock | `chore(engine): L3 编排引擎包空壳` |
| T6 | 验收收尾 | — | dev 冒烟（19982）+ 全量测试 + 验收记录 + 文档对齐 | 视情况（冒烟脚本如产生配置变更随 T6 提交） |

---

## T1 注册约定化（地基，先行）

**service 侧**（先测后改）：
1. 红：新增 `test/routes-registry.test.ts`——① `collectRoutes()` 注册产物 = 期望路由表（GET /、GET /healthz、GET /api/events、GET /api/activity），method+path 全唯一；② 分域导出（infra/shell）各自注册函数只注册自己的端点（域间无交叉污染）。
2. 绿：建 `src/server/routes/infra.ts`（healthz/activity/events 三 handler + `registerInfraRoutes(reg, deps)`）、`src/server/routes/shell.ts`（root + `registerShellRoutes(reg, deps)`）、`src/server/routes/index.ts`（静态汇总表 + 唯一性断言函数）；`endpoints.ts` 删除，其导出的 handler 函数随域迁移（`server.test.ts`/`contracts.test.ts` 的 import 路径同步改，断言不动）；`main.ts` 装配改 `registerAllRoutes(registry, deps)`。
3. 回归：101 全绿（行为逐字节等价由既有契约测试佐证）。

**web 侧**（同构）：
4. 红：`test/router.test.ts`——路由表结构断言（`/` 在 access 域文件导出中、router.routes 长度与汇总一致）。
5. 绿：建 `src/router/routes/access.ts`（导出 `accessRoutes: RouteRecordRaw[]`，暂装 Home 组件——组件替换是 T2 的事）；`router/index.ts` 改为汇总各域数组（一行一域注释标明「I1 各线在此追加域」）。
6. 回归：web 11 全绿。

**验收**：全量绿；grep 确认 endpoints.ts 已删、main.ts 无直接域注册；routes/index.ts 每域一行。

## T2 F-03 登录与接入页

**测试先行**（vitest environment=jsdom + @vue/test-utils，先加 devDeps 与 vitest.config 调整——纯函数测试文件不动 include 规则，环境按文件级 `// @vitest-environment jsdom` 注释分流，避免拖慢既有 11 个纯逻辑测试）：
1. 纯函数红→绿：`api/access.ts` 的 `statusLabel`（demo 八状态中文映射全集）、`statusBadgeClass`（ACTIVE 绿 / PENDING 系黄 / REJECTED 系红）、`parseStateJson`（外部对象不可信容错——字段缺失/类型错归一为安全默认）。
2. 组件红→绿：`AccessStatusCard`——传入 fixture 各状态（NEW/PENDING_REVIEW/APPROVED/ACTIVE/REJECTED/REVOKED/ERROR + 未登录 + 登录未激活）断言徽章文案/类名、拒绝原因块、锁定提示块、心跳行格式；`AccessActions`——显隐矩阵（demo 布尔式照搬：登录按钮 !authenticated、重提 NEW|REJECTED|ERROR、心跳 ACTIVE、重置 REJECTED|ERROR、登出 authenticated）+ 各按钮触发对应 API 调用（fetch mock 断言 method/path）。
3. 视图红→绿：`AccessView` 挂载 → 拉取 `/api/state`（mock）渲染状态卡；PENDING_REVIEW 态 5s 轮询触发（vi.useFakeTimers）/ unmount 停止；轮询失败降级文案。登录按钮点击 → `window.location.href` 置 `/auth/login`（jsdom 下断言赋值）。
4. session store 骨架：`stores/session.ts`（state 形状 + fetchState 动作 + authenticated getter）——T3 守卫消费它，本任务先立骨架（含 store 单测）。
5. 组装：`router/routes/access.ts` 的 `/` 换挂 AccessView；Home.vue 删除（其 healthz 消费由 AccessView 顶部小徽章承接 + T4 顶栏承接，纯函数与 11 测试保留）。
6. 回归：web 全绿（11 + 新增）。

**验收**：`bun run --cwd workbench/workbench-web test` 全绿；手动 dev 抽查（vite 起 + 代理目标无服务时显示不可达徽章而非白屏）。

## T3 F-02 导航骨架 + 路由守卫

1. 守卫判定纯函数红→绿：`resolveRedirect(authenticated, toPath)`——未认证且 to≠'/' → '/'；其余 null（放行）。
2. 路由域红→绿：`routes/{employees,bases,kanban}.ts` 各导出占位路由（views/Placeholder.vue 参数化标题）；kanban 域注释标 D-036 解隐口径。
3. 守卫集成红→绿：`router/index.ts` `beforeEach`——首次导航先 `sessionStore.fetchState()`（fetch mock），未认证重定向断言（push '/employees' 落 '/'；认证后放行）；fetch 失败按未认证。
4. 布局红→绿：`Layout.vue`（侧栏 + 顶栏槽 + router-view）；`SideNav`——可点项（我的员工默认选中态/底座与环境/任务看板）+ 置灰项（我的群组与对话，无路由、disabled、title 提示「即将上线」）；workflow 编排不渲染。业务域路由挂 Layout 嵌套；`/` 保持独立全屏。
5. 占位页断言：三占位路由渲染对应标题。
6. 回归：web 全绿。

**验收**：未登录四路由（/employees /bases /kanban 及深链）全落 `/`；登录态（mock）可达；侧栏置灰项不可聚焦可 hover 提示。

## T4 F-04 顶栏全局态

1. 纯函数红→绿：`interpretPlatformStatus(stateJson, stateFetchOk)`——ACTIVE+心跳新鲜（阈值 90s，常量）→ 'ok'；ACTIVE 但心跳陈旧 → 'stale'；REVOKED → 'revoked'；未激活各态 → 'inactive'（中性不告警）；fetch 失败 → 'unreachable'。`alertBanner(status)`——仅 unreachable/revoked 出告警条文案（D-032 提示不降级）。
2. 组件红→绿：`TopBar`——用户区（首字母圆徽 + name/email，未登录显示「未登录」）；平台状态徽章；版本行（复用 versionLineGated + fetchHealthz，接入页同款 2s 轮询迁此）；检查更新占位按钮（点击 → 提示条「检查更新功能即将上线」）；告警条条件渲染断言（unreachable/revoked 两态 + 正常态不渲染）。
3. 组装：Layout 顶栏换 TopBar；session store 扩展心跳刷新动作（沿 /api/state 轮询，节奏与顶栏健康轮询合并为一个 interval）。
4. 回归：web 全绿。

**验收**：mock ACTIVE fixture 顶栏绿徽章无告警条；mock fetch 失败 → 告警条常驻；版本行非健康态「版本未知」。

## T5 engine 空壳

1. 红：`workbench/workbench-engine/test/smoke.test.ts`——`import * as engine from '../src/index'` 不炸 + 导出存在性（空入口导出 `ENGINE_VERSION` 常量占位）。
2. 绿：`package.json`（name `@devzero/engine`、scripts test/typecheck 对齐兄弟包）、`tsconfig.json`（复用 service 的形态）、`vitest.config.ts`（最简）、`src/index.ts`（`export const ENGINE_VERSION = '0.0.1'`）、`README.md`（一句话：L3 编排引擎线在此实现，契约见路线图 §3.3）。
3. `bun install` 刷 lock（I0 预期内变更）；全量测试（service 101 + web + engine smoke）绿。

**验收**：根 `bun test`（或分包跑）发现 engine smoke；`bun pm ls` 可见 @devzero/engine。

## T6 验收收尾

1. 全量测试：service + web + engine 全绿，基线 101+11 不破且新增计数记录在案。
2. dev 冒烟（19982）：
   a. service `--foreground` 起 19982（本线端口），`curl /healthz` 200；vite dev（VITE_PROXY_TARGET=19982）开页：未登录态接入页可用、服务徽章绿。
   b. 停 service → demo server `WORKBENCH_PORT=19982 WORKBENCH_PUBLIC_URL=http://localhost:5173` 起 19982，vite dev 开页：接入页拉到 demo 的 /api/state（未登录 fixture）、点登录发起 /auth/login 302（Keycloak 可达则全链登录回状态卡，不可达则记录到达层级）。
   c. 冒烟结果与平台环境可达性记入本文件执行记录节。
3. 文档对齐：design/plan 变更记录补执行状态；G-1~G-5 缺口表状态核对（未裁决项保持「待用户裁决」）。
4. 收尾检查：`git status` 无 web-dist；分支提交序列完整；不合流 main。

## T7 视觉对齐轮（用户裁决 2026-08-26：完全按原型风格修订）

> 背景：T2~T4 交付时必读材料未含 `docs/prototype/workbench.html`，视觉按 demo ui.ts 绿色系做了简朴版；用户裁决后以原型为视觉权威源全量修订（功能/语义/路由/测试断言不动，纯视觉层）。

1. `src/styles/tokens.css`：原型 `:root` 变量原样搬入（blue-950~50 全谱 + g 系中性 + 功能色 bg 对）+ body 全局（原型字体栈含 PingFang/雅黑、bg #f4f7fd、ink 文字）；main.ts 引入。
2. SideNav 重构为原型形态：78px 窄图标栏（蓝纵向渐变 950→900→800 + sticky）+ logo 44px 蓝渐变（原型人形 SVG）+ nav-item 62px 竖排 stroke 图标+10.5px 微字（图标取自原型：员工=脉冲线、底座与环境=多宿主显示器、任务看板=三列柱、群组置灰=对话气泡；workflow 编排仍不渲染）+ hover/active 内阴影样式（RouterLink 保持路由语义，样式对齐 button 形态）+ 置灰项同形态 disabled。
3. Layout：`.app` flex 结构 + main padding 26/34/60。
4. TopBar 蓝系化（原型无顶栏原型，按 token 延展）：白卡片条（card 语言）；用户区 avatar av-blue 渐变圆徽；平台状态 tag 体系映射（ok→tag-green / stale→tag-amber / revoked·unreachable→tag-red / inactive→tag-gray）；版本行 g500 小字；检查更新 btn-ghost btn-sm；告警条 red-bg 底 red 字全宽。
5. AccessView 蓝系化：头部品牌区沿 sidebar 渐变语言；状态卡/安全边界卡 card 语言（白底 g200 边框 14px 圆角 + 蓝调阴影）；状态徽章 statusBadgeClass 映射原型 tag 体系（ACTIVE 绿/PENDING 系 amber/REJECTED 系红/NEW 灰）；按钮 btn 体系（登录 btn-primary / 次要 btn-ghost / 破坏性 ghost 红字变体）。
6. Placeholder：page-head（h1 24px/700 + sub g500 13px）+ 空态卡。
7. 约束：路由结构/守卫/store/api 层/props 与 emit 协议零触碰；测试语义断言不动（文字/title/to 锚点不变），全绿为门。

## T8 平台地址配置（方案 A，设计 D-13~D-18）

1. **service 红**：config.test 增 `platform.baseUrl` 默认值与覆盖解析用例；新增 routes-config.test（GET 返回当前值 / PUT 合法 URL 持久化且 GET 立即反映 / PUT 非法 URL 400 带错误信息 / PUT 写入保留既有键不覆盖 / Host 白名单照常生效）。
2. **service 绿**：schema 增 `platform.baseUrl`（z.string().url()，默认 `http://127.0.0.1:18000`）+ `src/config/` 增合并写函数（读现文件→合并覆盖键→原子写）+ `src/server/routes/config.ts`（GET/PUT，鉴权注记 D-15）+ routes/index 汇总加一行 + main.ts 注入 profileDir。
3. **web 红**：api/platform-config.ts 测试（GET/PUT 形状、错误归一）+ PlatformConfigCard 组件测试（显示当前地址 / 编辑保存成功反馈 / 非法输入前端校验拦截 + 后端 400 错误展示）。
4. **web 绿**：实现 + AccessView hero 区挂卡（D-16 位置）。
5. **验收配套**：mock-server.ts 增 GET/PUT /api/config/platform（内存态即可）；端到端验收 = 5173 口改址 → 保存成功 → service 侧重启 → 值仍在（真实 config.json 持久化）。

## 风险与对策

| 风险 | 对策 |
|------|------|
| server.test.ts 直接 import endpoints.ts 导出符号，拆分破坏 | T1 步骤 2 已含 import 路径同步改；跑全量守底线 |
| jsdom 环境拖慢既有测试 | 环境按文件注释分流（纯逻辑默认 node 环境） |
| demo server 与 service 抢 19982 | 冒烟脚本串行（先 service 后 demo，互斥） |
| /api/progress POST/GET 矛盾（G-2） | 前端调用集中在 api/access.ts 单点，A 系列裁决后一行改 |
| Keycloak 平台环境不可达 | 冒烟覆盖到「跳转发起」层级并如实记录；组件测试已覆盖全状态渲染（不依赖平台） |

## 执行记录

| 任务 | commit | 结果 |
|------|--------|------|
| 文档对 | 8987460 | design（D-1~D-12 + G-1~G-5）+ plan 初版 |
| T1 注册约定化 | 1b9cc27 | service **106**（101 基线 + 5 新增 routes-registry）/ web **14**（11 基线 + 3 新增 router）/ typecheck 双过 / tray Go contract ok（绊网随迁 routes/infra.ts）。偏差 3 项均已审：① tray contract_test.go 第三处消费点（任务已知事实未覆盖）随迁路径；② server.test.ts 架构纪律测试文件清单字面量更新（断言逻辑不动，覆盖面扩至全部 routes 文件）；③ web router 测试以 createMemoryHistory 顶替 createWebHistory（vue-router 4.6.4 模块加载即访问 window，测装配事实不测 history 策略）。单审加固：纪律测试清单改 routes/ 目录整目录扫描（I1 新增域文件自动纳入 hono 纪律覆盖）。 |
| T2 F-03 登录与接入页 | a6237d5 | web **110**（14 基线 + 96 新增：access 39/session 4/AccessStatusCard 24/AccessActions 20/AccessView 9）/ typecheck 过 / service 零触碰。红→绿四层证据齐（模块不存在→绿）。偏差均已审：① vitest 挂 @vitejs/plugin-vue（无插件实证报错，注释完整）；② parseStateJson 容错两档（installationId 缺失整包拒绝 null / status 非法降级 NEW，粒度差异测试注明）；③ logout 不整页 reload（SPA 重拉 /api/state 等价回未登录态，注释说明）；④ 动作网络异常归一失败不抛出（demo 隐患修正）；⑤ jsdom 登录跳转以 Object.defineProperty 桩 location（探针实证）。vite.config /auth 代理键未动（D-9 属 T6 冒烟装配，范围外）。 |
| T3 F-02 导航骨架+守卫 | d6d3c3f | web **128**（110 基线 + 18 新增：guard 4/集成 4/SideNav 4/Placeholder 4/router 净增 2）/ typecheck 过 / service 106 无回归 / 零新依赖。Layout 嵌套方案选定：vue-router 4.6.4 源码 findInsertionIndex + 探针双向实证「同分记录先注册者胜」，access 行必须在 Layout 父记录前，router.test.ts resolve('/') 断言为顺序回归保险。偏差均已审：① employees 占位文案双线标注 L1/L4（E-10 属 L4、E-11 属 L1，单标 L1 与路线图波次表不符）；② 集成测试 AccessView 桩化隔离守卫 fetch 计数。已知既有事实留 T4：真 app 启动接入页存在守卫+AccessView 各一次 /api/state 拉取（T4 store 轮询合并时收敛）。 |
| T5 engine 空壳 | 5e93bf9 | @devzero/engine 入 workspace（bun.lock 预期内变更）；smoke 红绿真过（先 collection 失败→入口落地 1/1 绿）+ typecheck 过；service 106 / web 128 无回归。偏差：package.json devDeps（vitest/typescript/@types/bun 对齐兄弟包）——首版漏声明致 vitest 不可发现，install 后红绿照常。 |
| T4 F-04 顶栏全局态 | 0160b45 | web **166**（128 基线 + 38 新增：platform-status 24 / top-bar 14）/ typecheck 过。useHealthPolling composable 抽取（TopBar/AccessView 共享，既有 9 测试证明等价）；TopBar 挂载不立即拉取（与守卫「只拉一次」错开约定，注释互锁 + guard-integration 真挂载复验绿）。已审偏差：plan 原文「合并为一个 interval」与 30s/2s 双节奏矛盾（plan 疏忽，按双 interval+composable 落）；**守卫+AccessView 首屏双拉取裁决不收敛**（本地幂等 GET 无功能损害；AccessView 状态管理重构超薄骨架范围；A 系列落地 /api/state 服务端时前端重对齐一并收敛——G-1 关联）。 |
| T6 验收收尾 | 8c1d3c1 | **全量**：service 106 / web 166 / engine 1 / tray Go 9 包全绿零 FAIL + typecheck 双过。**冒烟三段（19982 纪律，临时 profile `WORKBENCH_HOME` 隔离）**：① service 段——`{"network":{"port":19982}}` 起前台进程，healthz（app=workbench/uid 在场）/api/activity 硬值/嵌入页/api/events 204 四端点行为等价实证（注册约定化后）；② vite 段——`VITE_PROXY_TARGET=19982` 起 5173，/healthz 经代理命中同一 service 实例（pid 一致）；③ demo 段（F-03 链路）——demo server 起 19982（`WORKBENCH_PUBLIC_URL=http://localhost:5173`），vite 代理 `/api/state` 返回真实未登录 state；**浏览器级验证（Playwright）**：AccessView 全要素渲染（Installation ID 真值/空值 `-`/「请先登录」提示/登录按钮/安全边界卡），健康徽章「服务不可用·版本未知」= D-11 门控语义在 demo（无 healthz 端点）下的正确表现；**登录按钮点击 → 整页跳转 /auth/login → 代理转发 → demo 平台发现（18000 不可达）→ 500**——OIDC 全链止于「跳转发起+平台发现」，Keycloak 真实登录需 compose 平台环境（未起，按风险表预案记录；组件测试已覆盖全状态渲染）。**冒烟环境事件**：19982 被 M0 spike 残留进程（m0-spike/t1-hono/spike-sea.exe，34h 前遗留）占用，已确认身份后清理；config schema 实形 `{"network":{"port":...}}`（两轮试错）。vite proxy D-9 装配（VITE_PROXY_TARGET 可配默认 19980 + /auth 键）随本 commit 提交。demo 冒烟产物（workbench-demo/node_modules 本地留存被 ignore、bun.lock 已删）。**main 并行进度**：安装器线已提交 Task 5-7 共 8+ commits（installer/docs 域），与本线零代码交集，合流冲突面极小；合流时机归用户裁决。 |
| T7 视觉对齐原型 | 28f76c6 | 用户裁决 2026-08-26「完全按照原型风格来修订」+「登录页面也改蓝色系」——视觉权威源 = `docs/prototype/workbench.html`（蓝色系）。纯视觉层：tokens.css（原型 :root 原样 + body 字体栈/底色/盒模型重置）+ SideNav 重构 78px 窄图标栏（蓝纵向渐变 950→900→800 + logo + 原型 SVG 图标逐枚搬用）+ Layout .app/main + TopBar 白卡片条（avatar av-blue/tag 体系映射/red-bg 告警条）+ AccessView 蓝系化（头部 90deg 渐变/page-head 规格/btn 体系/tag 徽章）+ Placeholder page-head 形态。**166/166 零测试同步**（语义断言全部原样通过）+ typecheck + vite build（tokens 内联实证）。subagent 冒烟发现并修复真实 CSS 特异度 bug（`.row span` 后代选择器压过 `.tag-*` 单类，改直接子选择器带注释）。主会话复核（computed style 亲自核对，非只信报告）：业务页 sidebar 78px 蓝渐变/active `rgba(96,165,250,.28)`/tag-green `#dcfce7+#16a34a`/avatar `blue-600→400` 渐变/body `#f4f7fd`、接入页登录态头部 `90deg blue-950→800`+徽章「已激活」tag-green+登出 btn-danger、未登录态登录按钮 `blue-600` 白字 9px 圆角——全部命中原型精确值；首轮探针「未找到」为探针自身缺 waitForSelector 的时序问题（非代码缺陷，二轮带 waitForSelector 全中）。复核截图 4 张存 `screenshots/`（gitignore 覆盖不提交）。devDeps 未新增（图标为内联 SVG 非图标库）。 |
| T8 平台地址配置 | 5e2584f | 用户需求 2026-08-26（方案 A，设计 D-13~D-18）。service：schema `platform.baseUrl`（默认 `http://127.0.0.1:18000`，zod url+refine 限 http(s) 判据单源）+ `writeConfigOverride`（深合并+原子写+坏 JSON 拒绝覆盖）+ config 域 `GET/PUT /api/config/platform`（GET 重读、PUT 400 INVALID_PLATFORM_URL、错误形状沿 demo 同形）+ registry/adapter 最小扩 PUT（非 GET 才读 body 保 SSE 安全）。web：api/platform-config.ts + PlatformConfigCard「平台连接」卡 + AccessView hero 挂卡（既有 9 测试零改动全绿）。**126/126 + 184/184 + engine 1 全绿** + typecheck 双过（既有测试 4 文件字面量更新沿 T1 偏差②先例）。**service 端到端验收五步全过**：①GET 默认值 ②PUT 改址 `http://192.168.45.50:18000` ③PUT ftp:// 400 中文错误 ④落盘 config.json 保留既有 network.port 键 ⑤杀进程重启后 GET 值仍在（持久化）。消费边界 D-18：登录链路读该地址归 A-01。 |
| VM 平台部署（需求一） | 环境操作（无仓内 commit） | 用户授权 SSH `lyadmin@192.168.45.50`（Ubuntu 24.04 + Docker 29.4）。过程：仓更新至 a2bb316（origin/main，安装器线已推送）→ `.env` 从模板补全（PUBLIC_HOST=192.168.45.50 + DB_PASSWORD 等九变量——首写只留一行致 postgres 空密码风险，已修）→ **镜像源三坑**：quay.io TLS timeout（registry-mirrors 不代理 quay）→ 公开代理全败 → **用户配 VPN 解决**；VM 磁盘满（98G 用尽）→ 清 build 缓存/snap 旧版/纯工具缓存（go-build/pip 等 2.3G，未动用户项目镜像与数据卷）→ `compose up` 四服务（postgres/keycloak/platform-api/platform-web；**不起 workbench 容器**——VM 本机 qoderwake 占 19820）→ 全部就绪（OIDC discovery 200 + platform health 200，postgres healthy）。**本机跨机验证全通**：`http://192.168.45.50:18000/.well-known/workbench-configuration` 完整 JSON + Keycloak discovery 200。**平台侧部署缺口（待用户裁决，均为容器内临时补丁、重建即丢）**：① platform-web nginx 不反代 `.well-known/`（e2e 时代工作台在 compose 网内走 internal 直连，外部工作台场景未覆盖）——容器内已补 `location = /.well-known/workbench-configuration`；② `/oauth2/workbench/token`（机器 Token 端点）同样不在反代列表——已补 `location /oauth2/`；③ Keycloak `workbench-desktop` client redirectUris 仅含 compose 内回调（`192.168.45.50:19820`）——已加 localhost/127.0.0.1 的 5173/5174 精确回调（通配符不支持端口位置）。**测试账号（已建）**：Keycloak `test`/`test1234`（普通员工）+ `system.admin`/`Horse~test@2026`（平台 SYSTEM_ADMIN——经 platform auth.py L78 首登 bootstrap 机制自动赋权）。**全链验收实证（playwright）**：5174 点登录 → VM Keycloak 表单 → test 登录 → 回调 → 状态卡 PENDING_REVIEW（自动提交 enrollment）→ system.admin 平台审批「通过」→ 5s 轮询自动推进 APPROVED → challenge/proof/complete 换取 workbench ID → machine token 心跳 → **ACTIVE（真实心跳时间戳在场）**；真实登录态业务页顶栏「平台已连接」绿徽章 + 首字母头像（F-04 真数据实证）。截图两张存 `screenshots/`。 |
