# L1 员工新建线实施计划（shared-protocol + E-12 管线 + E-11 向导 + E-13 zip + 预置物化 + 物料批）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 spec 交付员工域「从无到有」全链——schema 真源、构建管线、七步向导、skill zip 上传、预置物化、物料实施批，验收锚为「向导一路默认下一步产出过 schema 校验的员工包并落员工库（花名册可见）」。

**Architecture:** shared-protocol（zod schema，workspace 包，service/web 双消费）→ service `src/employees/` 域（builder/renderer/hooks-compiler/store/templates 模块，模板物料经 codegen 内联进单体）→ web `/employees/new` 七步向导（Vue3，原型视觉 × demo 交互语义）。生成即保存：原子落盘 `~/.devzero/employees/<id>/`，花名册为扫描派生视图。

**Tech Stack:** TypeScript / zod v3 / Hono（service，框架无关 registry + 单点 adapter）/ bun workspace（`workbench/*`）/ vitest / Vue3 + Pinia + vue-router / js-yaml / fflate（zip 解包）。

**Spec:** [2026-08-27-L1员工新建-design.md](2026-08-27-L1员工新建-design.md)（W1~W5 裁决与六块任务设计——执行者必须先读 spec 再读本计划）

## Global Constraints（每任务隐含遵守）

- **目录边界红线**：写域止于 `<profileDir>/employees/`、`<profileDir>/templates/custom/`、`<profileDir>/tmp/`——🔴 禁写 `~/digital-staff/`（profileDir = `WORKBENCH_HOME` > `~/.devzero`，main.ts 既有约定）
- **schema 单一真相源**：manifest 结构**逐字段照 v0.2 设计稿 §2/§3 落**（含 template_id optional、frontmatter vendored_from/license），不重新设计；前端校验徽章与后端管线用同一份 shared-protocol
- **hooks 命令格式（W3 规范锚）**：`"${DEVZERO_HOOKS_ROOT}/redlines/run-hook.cmd" <rule_id>.py`
- **向导禁词**：向导 UI 全程不得出现「底座」「安装」「AgentHub」字样（完成态显式「安装到底座」动作除外）；「上传到 AgentHub」按钮不实现
- **命名（CLAUDE.md §4）**：Web 正式名称「研发零处数字员工终端」；DevZero 仅内部代号；「终端」替代「工作台」
- **TDD 纪律**：每任务先写失败测试跑红，再实现跑绿；每任务一提交
- **契约冻结**：shared-protocol 在本线开发、合流进 main 即冻结；开发期间不改其他线契约文件（协同编排设计/node-table schema 不碰）
- **测试运行**：bash 内 `cd "/d/Works/AI Coding/digital-employees/.worktrees/l1-create"` 后按包跑 `bun run test`；新包测试前先 `bun install`（workspace 根安装）
- **快照语义**：模板 skills 物化=版本定格拷贝；预置物化幂等（已存在跳过，不比较内容）

## 技术决策（plan 阶段定案，关闭 spec Q2/Q4/Q7）

| # | 决策 |
|---|------|
| P1（Q7） | 模板物料分发 = **codegen 内联**：`workbench-service/scripts/gen-templates-assets.ts` 扫 `workbench/templates/` 全文本文件 → 生成 `src/assets/templates.gen.ts`（`export const builtinTemplates: Record<相对路径, 内容>`），**提交进仓**（沿 web-dist/index.html 先例）；`pretest`/`prebuild` 挂链重生成 + drift 测试防漏跑。运行时 builtin 模板走内存 Record，custom 模板走 fs（TemplatesProvider 统一接口）——dev/exe 行为完全一致，无释放步骤 |
| P2（Q2） | zip skill 版本：frontmatter 可选 `version` 键优先，缺省 `'0.1.0'` |
| P3（Q4） | shared-protocol 接线：`"@devzero/shared-protocol": "workspace:*"`；package.json `exports: { ".": "./src/index.ts" }`（TS 源直出，bun/vite 双端消费） |
| P4 | zip 上传 multipart 通道：hono-adapter 扩 `Ctx.bodyRaw?: ArrayBuffer`（content-type 非 application/json 的非 GET 请求读 raw）——adapter 单点改，域文件不碰框架类型 |
| P5 | secretgate 移植产物 = `secretgate.mjs`（纯 JS 零依赖 node 直跑；正则规则引擎从 1.0 `secretgate.py` 移植） |
| P6 | sec-scan-design zip 的 GBK 文件名：解包时 `new TextDecoder('gbk')` 转码重命名为 UTF-8（物料实施批一次性转换落仓；上传服务的通用 zip 同样用此通道） |

---

## 阶段 A：shared-protocol 真源（A1~A5）

### Task 1 (A1): 包骨架 + manifestSchema 基础结构

**Files:**
- Create: `workbench/shared-protocol/package.json`、`workbench/shared-protocol/tsconfig.json`、`workbench/shared-protocol/vitest.config.ts`
- Create: `workbench/shared-protocol/src/manifest.ts`、`workbench/shared-protocol/src/index.ts`
- Test: `workbench/shared-protocol/test/manifest.test.ts`

**Interfaces:**
- Produces: `manifestSchema: z.ZodType<Manifest>`（v0.2 八类全字段）；`type Manifest`；后续任务在其上加 superRefine

- [ ] **Step 1: 包骨架**

`package.json`（对齐 engine 包风格）：
```json
{
  "name": "@devzero/shared-protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "test": "vitest run", "typecheck": "tsc --noEmit" },
  "dependencies": { "zod": "^3" },
  "devDependencies": { "typescript": "^5", "vitest": "^2", "js-yaml": "^4", "@types/js-yaml": "^4" }
}
```
`tsconfig.json` 抄 `workbench-workspace 兄弟包`（engine）的配置改 include `src test`。`vitest.config.ts` 最简（`test: { include: ['test/**/*.test.ts'] }`）。workspace 根跑 `bun install`。

- [ ] **Step 2: 写失败测试（合法 manifest 全字段通过 + 非法值拒绝）**

`test/manifest.test.ts` 用 dev-engineer 物料最小化裁剪为 fixture（读 `../../templates/dev-engineer/manifest.yml` 经 js-yaml parse 后 `manifestSchema.parse`——**注意 yaml 的 `requires: {level: L2}` 流式写法 parse 出 `{level: 'L2'}` 正常**）：

```ts
import { describe, it, expect } from 'vitest'
import { readFileSync } from 'node:fs'
import yaml from 'js-yaml'
import { manifestSchema } from '../src/manifest'

const load = (rel: string) =>
  yaml.load(readFileSync(new URL(rel, import.meta.url), 'utf8')) as unknown

describe('manifestSchema 基础结构', () => {
  it('dev-engineer 物料过 schema', () => {
    const m = manifestSchema.parse(load('../../templates/dev-engineer/manifest.yml'))
    expect(m.id).toBe('dev-engineer')
    expect(m.kind).toBe('flow-owner')
    expect(m.agent.persona.usage_modes).toContain('+编排')
  })
  it('未知字段拒绝（strict）', () => {
    const base = load('../../templates/req-clarifier/manifest.yml') as Record<string, unknown>
    expect(() => manifestSchema.parse({ ...base, schema_version: '2.1' })).toThrow()
  })
  it('id 非法格式拒绝', () => {
    const base = load('../../templates/req-clarifier/manifest.yml') as Record<string, unknown>
    expect(() => manifestSchema.parse({ ...base, id: 'Bad_Id' })).toThrow()
  })
})
```

- [ ] **Step 3: 跑红**

Run: `cd workbench/shared-protocol && bun run test`
Expected: FAIL（`src/manifest.ts` 不存在）

- [ ] **Step 4: 实现 manifest.ts（逐字段照 v0.2 设计稿 §2.1）**

```ts
import { z } from 'zod'
import { skillEntrySchema } from './skill'   // A3 实装前先在此文件内用占位 z.unknown()？——不：
// 本任务先内联一个最小 skillEntry（z.object({name/version/source_type}).passthrough()），
// A3 任务再抽到 skill.ts 并 import（小步可编译）。

const semver = /^\d+\.\d+\.\d+$/

export const manifestSchema = z.object({
  // ── 元数据（10 项）──
  id: z.string().regex(/^[a-z0-9][a-z0-9-]*$/),
  display: z.string().min(1),
  brief: z.string().max(30),
  avatar: z.string().default(''),
  version: z.string().regex(semver),
  upp_version: z.literal('2.1'),
  kind: z.enum(['flow-owner', 'callee']),
  org: z.string().min(1).default('local'),
  operator: z.string().email(),
  requires: z.object({ level: z.enum(['L0', 'L1', 'L2']) }),
  // ── 八件套（1+7）──
  agent: z.object({
    persona: z.object({
      role: z.string(),
      identity: z.string().min(10),
      principles: z.array(z.string()).default([]),
      usage_modes: z.array(z.enum(['裸用', '+方法论', '+流程', '+编排'])).min(1),
    }),
  }),
  skills: z.array(skillEntrySchema).default([]),
  hooks: z.object({
    redlines: z.array(z.object({
      rule_id: z.enum(['no-push-to-main', 'high-risk-via-gate', 'no-devzero-state',
        'no-external-request', 'no-production-access', 'no-db-schema', 'custom']),
      compiled: z.boolean().default(false),
    })).default([]),
  }).default({ redlines: [] }),
  tools: z.object({ deny: z.array(z.string()).default([]) }).default({ deny: [] }),
  commands: z.literal('commands/'),
  knowledge: z.literal('knowledge/'),
  connectors: z.array(z.object({
    name: z.string(),
    type: z.enum(['stdio', 'http']),
    command: z.string().optional(),
    args: z.array(z.string()).default([]),
    url: z.string().url().optional(),
    env: z.record(z.string()).default({}),
    access: z.enum(['read', 'read-write']).default('read-write'),
  })).default([]),
  custom: z.record(z.unknown()).default({}),
  // ── 管理面（3 段）──
  constraints: z.object({
    tier: z.enum(['评审安全档', '设计档', '探索档', '编码档', '执行档']).default('编码档'),
    token_quota: z.object({
      per_task: z.number().int().positive().optional(),
      monthly: z.number().int().positive().optional(),
    }).optional(),
  }).default({}),
  governance: z.object({
    level: z.enum(['L1', 'L2', 'L3', 'L4']),
    visibility: z.enum(['private', 'team', 'department', 'company']),
    audit: z.enum(['full', 'exceptions-only']).default('exceptions-only'),
  }),
  orchestration: z.object({
    node_table: z.string().regex(/^orchestration\/.+\.node-table\.yml$/),
  }).optional(),
}).strict()

export type Manifest = z.infer<typeof manifestSchema>
```

注意：`.strict()` 要求物料 manifest 的键与 schema 完全一致——**跑测试若物料有过/缺键，以 schema 为准报错并核对 v0.2 设计稿**；物料键序（`requires: {level: L2}` inline 对象）js-yaml parse 无问题。`src/index.ts` 先 `export * from './manifest'`。

- [ ] **Step 5: 跑绿 + typecheck + 提交**

Run: `bun run test && bun run typecheck`（全绿）
```bash
git add workbench/shared-protocol && git commit -m "feat(shared-protocol): 包骨架 + manifestSchema 八类 v0.2 基础结构（strict + 活样例首测 dev-engineer 过）"
```

### Task 2 (A2): 六跨字段规则（superRefine）

**Files:**
- Modify: `workbench/shared-protocol/src/manifest.ts`
- Test: `workbench/shared-protocol/test/cross-field.test.ts`

**Interfaces:**
- Consumes: A1 的 `manifestSchema`
- Produces: `manifestSchema` 变为带 `.superRefine` 的完整版（六规则）；`validateManifest` 的 issue 形态由 A4 消费

- [ ] **Step 1: 写失败测试（六规则逐条 + 断言 issue path）**

```ts
import { describe, it, expect } from 'vitest'
import { manifestSchema } from '../src/manifest'
import type { Manifest } from '../src/manifest'

/** 以 dev-engineer 物料语义手写一份合法基底（不读文件，跨字段用例要精确控制） */
const base: Manifest = manifestSchema.parse({
  id: 'dev-engineer', display: '开发工程师', brief: '承接需求完成代码实现', avatar: '🧑‍💻',
  version: '0.1.0', upp_version: '2.1', kind: 'flow-owner', org: 'local',
  operator: 'demo@devzero.local', requires: { level: 'L2' },
  agent: { persona: { role: '数字员工·开发岗', identity: '负责把实现计划落成代码的数字员工。', principles: [], usage_modes: ['裸用', '+编排'] } },
  skills: [], hooks: { redlines: [] }, tools: { deny: [] },
  commands: 'commands/', knowledge: 'knowledge/', connectors: [], custom: {},
  constraints: {}, governance: { level: 'L3', visibility: 'team' },
  orchestration: { node_table: 'orchestration/dev-engineer.node-table.yml' },
})

describe('跨字段六规则', () => {
  it('R1: usage_modes 含 +编排 但 level≠L2 → 拒', () => {
    const r = manifestSchema.safeParse({ ...base, requires: { level: 'L1' } })
    expect(r.success).toBe(false)
    expect(!r.success && r.error.issues.some((i) => JSON.stringify(i.path) === '["requires","level"]')).toBe(true)
  })
  it('R1: +编排 但无 orchestration → 拒', () => {
    const { orchestration: _o, ...noOrch } = base
    expect(manifestSchema.safeParse(noOrch).success).toBe(false)
  })
  it('R2: kind=callee 带 orchestration → 拒', () => {
    const r = manifestSchema.safeParse({ ...base, kind: 'callee' })
    expect(r.success).toBe(false)
    expect(!r.success && r.error.issues.some((i) => i.path[0] === 'orchestration')).toBe(true)
  })
  it('R3: skills name 重复 → 拒', () => {
    const dup = [...base.skills, ...base.skills] // skills 空——改用手填两条同名
    const withDup = { ...base, skills: [
      { name: 'tdd', version: '1.0.0', source_type: 'template' },
      { name: 'tdd', version: '1.0.0', source_type: 'template' }] }
    const r = manifestSchema.safeParse(withDup)
    expect(r.success).toBe(false)
    expect(!r.success && r.error.issues.some((i) => i.path[0] === 'skills')).toBe(true)
  })
  it('R4: compiled 红线存在但缺 hooks 字段一致性——schema 层只做声明一致性：redlines 非空+deny 空+无 hooks.json 属安装期校验，schema 不拒', () => {
    expect(manifestSchema.safeParse({ ...base, hooks: { redlines: [{ rule_id: 'no-push-to-main', compiled: true }] } }).success).toBe(true)
  })
  it('R5: connectors stdio 缺 command → 拒；http 缺 url → 拒', () => {
    expect(manifestSchema.safeParse({ ...base, connectors: [{ name: 'x', type: 'stdio' }] }).success).toBe(false)
    expect(manifestSchema.safeParse({ ...base, connectors: [{ name: 'x', type: 'http' }] }).success).toBe(false)
    expect(manifestSchema.safeParse({ ...base, connectors: [{ name: 'x', type: 'stdio', command: 'npx foo' }] }).success).toBe(true)
  })
  it('R6: deny 与 connectors 并存不拒（deny 优先 warning 属运行时语义）', () => {
    expect(manifestSchema.safeParse({ ...base, tools: { deny: ['Bash'] } }).success).toBe(true)
  })
})
```

- [ ] **Step 2: 跑红** — `bun run test`（R1/R2/R3/R5 用例 FAIL：无 superRefine）

- [ ] **Step 3: 实现 superRefine**（`manifestSchema = baseObject.strict().superRefine((m, ctx) => { ... })` 六规则逐条 `ctx.addIssue({ code: 'custom', path: [...], message })`；R4/R6 是「不拒」语义测试，无实现动作）

```ts
.superRefine((m, ctx) => {
  const hasOrch = m.usage_modes 包含 '+编排'  // m.agent.persona.usage_modes
  if (hasOrch) {
    if (m.requires.level !== 'L2') ctx.addIssue({ code: z.ZodIssueCode.custom, path: ['requires', 'level'], message: 'usage_modes 含 +编排 时 requires.level 必须为 L2' })
    if (!m.orchestration) ctx.addIssue({ code: z.ZodIssueCode.custom, path: ['orchestration'], message: 'usage_modes 含 +编排 时 orchestration.node_table 必填' })
  }
  if (m.kind === 'callee' && m.orchestration) ctx.addIssue({ code: z.ZodIssueCode.custom, path: ['orchestration'], message: 'kind=callee 不得有 orchestration' })
  const names = m.skills.map((s) => s.name)
  const dup = names.find((n, i) => names.indexOf(n) !== i)
  if (dup) ctx.addIssue({ code: z.ZodIssueCode.custom, path: ['skills'], message: `skills name 重复：${dup}` })
  for (const [i, c] of m.connectors.entries()) {
    if (c.type === 'stdio' && !c.command) ctx.addIssue({ code: z.ZodIssueCode.custom, path: ['connectors', i, 'command'], message: 'stdio 连接器 command 必填' })
    if (c.type === 'http' && !c.url) ctx.addIssue({ code: z.ZodIssueCode.custom, path: ['connectors', i, 'url'], message: 'http 连接器 url 必填' })
  }
})
```
（实现时写成完整 TS；上面的伪码仅示意逐条 addIssue 的 path 取值，注意 superRefine 挂在 `.strict()` 之后链式调用。）

- [ ] **Step 4: 跑绿 + 提交** — `bun run test` 全绿；`git commit -m "feat(shared-protocol): 跨字段六规则 superRefine（+编排→L2+表必填/callee 禁表/skills 重名/stdio-http 互斥必填）"`

### Task 3 (A3): skill.ts + identity.ts + level.ts

**Files:**
- Create: `workbench/shared-protocol/src/skill.ts`、`src/identity.ts`、`src/level.ts`
- Modify: `src/manifest.ts`（skillEntry 改 import）、`src/index.ts`
- Test: `test/skill.test.ts`、`test/identity-level.test.ts`

**Interfaces:**
- Produces: `skillFrontmatterSchema`（strict：name/description/vendored_from?/license?/version?——P2 决策的 version 扩展键）；`skillEntrySchema`（discriminatedUnion template|local，template 分支含 `template_id?: string`）；`employeeId(id, org): string`；`parseOrg(employeeId): string`；`deriveLevel(usage_modes): 'L0'|'L1'|'L2'`

- [ ] **Step 1: 失败测试**

```ts
// test/skill.test.ts
describe('skillFrontmatterSchema', () => {
  it('合法：name+description（10 字+）', () => {
    expect(skillFrontmatterSchema.safeParse({ name: 'tdd-methodology', description: '测试驱动开发方法论，红绿重构循环。' }).success).toBe(true)
  })
  it('vendored_from/license/version 可选键过', () => {
    expect(skillFrontmatterSchema.safeParse({ name: 'a-skill', description: '描述十个字以上确保过校验。', vendored_from: 'superpowers@6.1.1', license: 'MIT', version: '1.2.0' }).success).toBe(true)
  })
  it('未知键拒（strict）；name 非 slug 拒；description <10 字拒', () => {
    expect(skillFrontmatterSchema.safeParse({ name: 'a-skill', description: '描述十个字以上确保过校验。', extra: 1 }).success).toBe(false)
    expect(skillFrontmatterSchema.safeParse({ name: 'Bad Name', description: '描述十个字以上确保过校验。' }).success).toBe(false)
    expect(skillFrontmatterSchema.safeParse({ name: 'a-skill', description: '太短' }).success).toBe(false)
  })
})
describe('skillEntrySchema', () => {
  it('template 分支：无 template_id 过（fable #1 optional）', () => {
    expect(skillEntrySchema.safeParse({ name: 'x', version: '1.0.0', source_type: 'template' }).success).toBe(true)
  })
  it('local 分支带 origin；agenthub 拒（V0.2 预留）', () => {
    expect(skillEntrySchema.safeParse({ name: 'x', version: '1.0.0', source_type: 'local', origin: 'foo.zip' }).success).toBe(true)
    expect(skillEntrySchema.safeParse({ name: 'x', version: '1.0.0', source_type: 'agenthub' }).success).toBe(false)
  })
})
// test/identity-level.test.ts
describe('identity/level', () => {
  it('employeeId 拼装与 parseOrg 往返', () => {
    expect(employeeId('dev-engineer', 'local')).toBe('dev-engineer@local')
    expect(parseOrg('dev-engineer@local')).toBe('local')
  })
  it('deriveLevel：裸用→L0；+方法论|+流程→L1；+编排→L2', () => {
    expect(deriveLevel(['裸用'])).toBe('L0')
    expect(deriveLevel(['裸用', '+方法论'])).toBe('L1')
    expect(deriveLevel(['裸用', '+流程'])).toBe('L1')
    expect(deriveLevel(['裸用', '+编排'])).toBe('L2')
  })
})
```

- [ ] **Step 2: 跑红** — 新测试 FAIL（模块不存在）

- [ ] **Step 3: 实现三模块**（skill.ts：`z.discriminatedUnion('source_type', [...])` 两分支 + frontmatter strict 对象；identity.ts：模板串拼接 + `split('@')` 取尾段——org 含 `@` 场景取最后一个 @ 后段；level.ts：按 usage_modes 包含性归档）；manifest.ts 的内联 skillEntry 删除改 `import { skillEntrySchema } from './skill'`；index.ts 补导出

- [ ] **Step 4: 跑绿 + 提交** — `git commit -m "feat(shared-protocol): skill/identity/level 三模块（frontmatter strict 含 vendored_from+license+version 扩展键；entry discriminatedUnion template|local；employeeId 拼装；deriveLevel）"`

### Task 4 (A4): derive.ts + errors.ts（validateManifest）

**Files:**
- Create: `workbench/shared-protocol/src/derive.ts`、`src/errors.ts`
- Modify: `src/index.ts`
- Test: `test/derive.test.ts`、`test/errors.test.ts`

**Interfaces:**
- Consumes: `Manifest` 类型、skill 素材 description（deriveCapabilities 入参是 `{name, description}[]` 技能清单——不读文件）
- Produces: `validateManifest(input: unknown): { valid: boolean; issues: Array<{ path: string; code: string; message: string }> }`（path 用点连字符串如 `requires.level`）；`deriveCapabilities(skills: Array<{name: string; description: string}>): string[]`（每 skill description 首句）；`deriveRequires(manifest: Manifest): { capabilities: string[]; optional: string[] }`（规则：恒有 `agent-def`+`fs-access`；skills 非空 +`skill-def`；有个人表 +`bash-exec`+`slash-command`+`subagent-dispatch`；hooks 非空 +`event:PreToolUse`；connectors 非空 +`mcp`）；`aggregateTools(builtin: string[], connectorTools: string[], engineTools: string[], deny: string[]): string[]`（并集减 deny）

- [ ] **Step 1: 失败测试**（表驱动：deriveRequires 五条规则逐条断言；aggregateTools `['Bash','Edit'] ∪ ['mcp__x__y'] ∪ ['engine_advance'] − deny['Edit'] → ['Bash','mcp__x__y','engine_advance']`；validateManifest 对 A2 的四个反例断言 `valid:false` 且 `issues[0].path` 点连格式正确、`code` 为 zod issue code）
- [ ] **Step 2: 跑红**
- [ ] **Step 3: 实现**（errors.ts：`safeParse` 失败时 `error.issues.map(i => ({ path: i.path.join('.'), code: i.code, message: i.message }))`；成功 `{valid:true, issues:[]}`）
- [ ] **Step 4: 跑绿 + 提交** — `git commit -m "feat(shared-protocol): derive 派生函数（capabilities/requires/aggregateTools）+ validateManifest 结构化错误（path 点连）"`

### Task 5 (A5): 活样例 CI + workspace 接线

**Files:**
- Create: `workbench/shared-protocol/test/live-fixtures.test.ts`
- Modify: `workbench/workbench-service/package.json`（dependencies 加 `"@devzero/shared-protocol": "workspace:*"`）、`workbench/workbench-web/package.json`（同）
- Test: 上述 live-fixtures.test.ts 即交付物

**Interfaces:**
- Produces: CI 活样例断言（7 manifest + 全部 SKILL.md frontmatter 过 schema）；service/web 可 import shared-protocol

- [ ] **Step 1: 写活样例测试（先跑——sec 三员 skills 为 [] 时不扫其 SKILL.md，当前全绿才对；若有 FAIL 即物料与 schema 不一致，**以 schema 为准修物料**并单独提交说明）**

```ts
import { describe, it, expect } from 'vitest'
import { readdirSync, readFileSync, existsSync, statSync } from 'node:fs'
import { join } from 'node:path'
import yaml from 'js-yaml'
import { manifestSchema, skillFrontmatterSchema } from '../src'

const TPL_ROOT = join(__dirname, '..', '..', 'templates')  // workbench/templates

describe('活样例：模板物料过 v0.2 schema', () => {
  const dirs = readdirSync(TPL_ROOT).filter((d) => statSync(join(TPL_ROOT, d)).isDirectory())
  it('七模板目录在位', () => expect(dirs.length).toBe(7))
  for (const dir of dirs) {
    it(`${dir}/manifest.yml 过 schema`, () => {
      const m = manifestSchema.parse(yaml.load(readFileSync(join(TPL_ROOT, dir, 'manifest.yml'), 'utf8')))
      // skills 声明-实物一致：manifest.skills 每条在 skills/<name>/SKILL.md 有实物（sec 三员 [] 状态天然过）
      for (const s of m.skills) {
        expect(existsSync(join(TPL_ROOT, dir, 'skills', s.name, 'SKILL.md'))).toBe(true)
      }
    })
    it(`${dir} 的每份 SKILL.md frontmatter 过 schema`, () => {
      const skillsDir = join(TPL_ROOT, dir, 'skills')
      if (!existsSync(skillsDir)) return
      for (const s of readdirSync(skillsDir)) {
        const raw = readFileSync(join(skillsDir, s, 'SKILL.md'), 'utf8')
        const fm = raw.split(/^---\s*$/m)[1] ?? ''
        const parsed = yaml.load(fm) as unknown
        const r = skillFrontmatterSchema.safeParse(parsed)
        if (!r.success) throw new Error(`${dir}/${s}: ${r.error.issues.map((i) => `${i.path}: ${i.message}`).join('; ')}`)
      }
    })
  }
})
```
（注意 frontmatter 解析：SKILL.md 以 `---` 开始，取第一段 `---` 与第二段 `---` 之间 yaml。sr/ar 两份多行折叠 description 若校验失败，读物料确认内容是否 ≥10 字——台账记载内容合格仅脚本误报，zod+yaml 正常解析应过。）

- [ ] **Step 2: 跑测试**（预期全绿；不绿则修物料/修解析，逐条解决）

- [ ] **Step 3: workspace 接线** — service/web package.json 加依赖后在 workspace 根 `bun install`；各自测试套跑一遍确认无破坏（此时还没代码 import，只是依赖就位）

- [ ] **Step 4: 提交** — `git commit -m "test(shared-protocol): 活样例 CI——7 manifest+全 SKILL.md frontmatter 过 schema（README 修改纪律 #1 落地）；service/web workspace 接线"`

---

## 阶段 B：E-12 构建管线（B1~B6）

### Task 6 (B1): codegen 模板资产模块

**Files:**
- Create: `workbench/workbench-service/scripts/gen-templates-assets.ts`、`workbench/workbench-service/src/assets/templates.gen.ts`（生成产物，提交）
- Modify: `workbench/workbench-service/package.json`（scripts 加 `"gen:templates": "bun run scripts/gen-templates-assets.ts"`、`"pretest": "bun run gen:templates"`、`"prebuild": "bun run gen:templates"`——注意 build.sh 是否已有前置，若冲突并入 build.sh）
- Test: `workbench/workbench-service/test/templates-assets.test.ts`

**Interfaces:**
- Produces: `builtinTemplates: Record<string, string>`（key = 模板内相对路径如 `dev-engineer/manifest.yml`、`dev-engineer/skills/tdd-methodology/SKILL.md`；value = 文件文本内容）

- [ ] **Step 1: 写失败测试**（drift 守卫 + 内容抽查）

```ts
import { it, expect } from 'vitest'
import { readFileSync, readdirSync, statSync, existsSync } from 'node:fs'
import { join, relative } from 'node:path'
import { builtinTemplates } from '../src/assets/templates.gen'

const TPL_ROOT = join(__dirname, '..', '..', '..', 'templates')  // workbench/templates（相对 service/test）

it('gen 与 templates/ 实际文件零 drift（改物料未重跑 gen 即红）', () => {
  const actual = new Set<string>()
  const walk = (dir: string) => {
    for (const e of readdirSync(dir)) {
      const p = join(dir, e)
      statSync(p).isDirectory() ? walk(p) : actual.add(relative(TPL_ROOT, p).split('\\').join('/'))
    }
  }
  walk(TPL_ROOT)
  expect(new Set(Object.keys(builtinTemplates))).toEqual(actual)
  for (const [k, v] of Object.entries(builtinTemplates)) {
    expect(v).toBe(readFileSync(join(TPL_ROOT, k), 'utf8'))
  }
})
it('七个模板目录齐 + manifest 在位', () => {
  expect(Object.keys(builtinTemplates).filter((k) => k.endsWith('manifest.yml')).length).toBe(7)
})
```

- [ ] **Step 2: 跑红**（templates.gen.ts 不存在）

- [ ] **Step 3: 实现 gen 脚本**（walk `../templates/`（脚本相对自身定位）→ 每 UTF-8 文件 `JSON.stringify(content)` 拼 `export const builtinTemplates: Record<string, string> = { "dev-engineer/manifest.yml": "...", ... }\n`；头部注释「生成产物：bun run gen:templates 重新生成，勿手改」）；跑 `bun run gen:templates` 产出 gen 文件

- [ ] **Step 4: 跑绿**（service 全套测试仍绿——pretest 自动重跑 gen 后 drift 测试过）+ **Step 5: 提交** — `git commit -m "feat(service): 模板资产 codegen 内联（templates.gen.ts 提交沿 web-dist 先例；pretest/prebuild 挂链 + drift 守卫测试）"`

### Task 7 (B2): templates 域模块 + GET /api/templates、GET /api/skills

**Files:**
- Create: `workbench/workbench-service/src/templates/provider.ts`
- Create: `workbench/workbench-service/src/server/routes/templates.ts`
- Modify: `workbench/workbench-service/src/server/routes/index.ts`（汇总表加一行 + RouteDeps 扩）
- Test: `workbench/workbench-service/test/templates-provider.test.ts`、`test/routes-templates.test.ts`

**Interfaces:**
- Consumes: `builtinTemplates`（B1）；Ctx/Res/RouteRegistry（既有）
- Produces:
  - `interface TemplatesProvider { list(): TemplateMeta[]; read(relPath: string): string; listSkills(): SkillMeta[]; customRoot: string }`
  - `interface TemplateMeta { id: string; display: string; brief: string; avatar: string; kind: 'flow-owner' | 'callee'; level: string; skillsCount: number; builtin: boolean }`
  - `interface SkillMeta { name: string; version: string; description: string; templateId: string; builtin: boolean }`
  - `createTemplatesProvider(builtin: Record<string, string>, customRoot: string): TemplatesProvider`（builtin 从内存 Record 读 manifest（js-yaml parse）；custom 扫 `customRoot/*/manifest.yml` fs 读——service 依赖加 `js-yaml`）
  - 路由：`GET /api/templates` → `{ items: TemplateMeta[] }`（builtin 先、custom 后）；`GET /api/skills` → `{ items: SkillMeta[] }`（builtin 全集跨模板聚合 + custom 模板的 skills 一并聚合，去重按 name 首见）

- [ ] **Step 1: 失败测试**（provider：注入真 builtinTemplates + tmp customRoot——写一个假 custom 模板目录断言聚合 builtin:false；read 相对路径返回内容；路由：构造 registry 注册后 app.request 断言 200 JSON 结构）
- [ ] **Step 2: 跑红 → Step 3: 实现**（provider + 路由域文件按 config.ts 域文件模式：`registerTemplatesRoutes(reg, deps)` + `TemplatesRouteDeps { templates: TemplatesProvider }`；index.ts 汇总表加行）→ **Step 4: 跑绿 + 提交** `feat(service): templates 域——provider（builtin 内存+custom fs 聚合）+ GET /api/templates /api/skills`

### Task 8 (B3): 员工库 store（原子落盘/冲突/回滚）

**Files:**
- Create: `workbench/workbench-service/src/employees/store.ts`
- Test: `workbench/workbench-service/test/employees-store.test.ts`

**Interfaces:**
- Produces:
  - `createEmployeeStore(employeesRoot: string, tmpRoot: string)`（两根目录注入——测试用 tmp，生产 profileDir 派生）
  - `store.exists(id: string): boolean`
  - `store.materialize(id: string, files: Array<{ path: string; content: string }>): Promise<{ packagePath: string }>`——实现：`files` 全量写入 `tmpRoot/<uuid>/`（mkdir recursive）→ 校验 `employeesRoot/<id>` 不存在（存在抛 `EmployeeIdConflictError`）→ `renameSync` 到目标 → 返回路径；**任何一步失败清理 temp 并抛带具体路径的错误**（Windows rename 撞占用：重试 3 次 100ms 间隔，仍败则报错不静默——沿 12.1 坑表教训，但「代际新文件名」策略对「目录整体落位」场景不适用，用重试+明确报错）
  - `store.list(): Array<{ id: string; manifest: unknown }>`（扫描 `employeesRoot/*/manifest.yml`，yaml parse 失败的目录跳过并收集到 `store.invalid: string[]`）

- [ ] **Step 1: 失败测试**（注入 tmp：①materialize 后目录与文件在位 ②同 id 二次 materialize 抛 EmployeeIdConflictError 且首份完好 ③files 写中途失败（用非法路径字符注入）→ temp 清理无残留 ④list 返回扫描结果）
- [ ] **Step 2: 跑红 → Step 3: 实现 → Step 4: 跑绿 + 提交** `feat(service): 员工库 store——原子落盘（temp 写全→rename）+ id 冲突预检 + 失败回滚`

### Task 9 (B4): AGENTS.md 渲染器（六段式双变体 + 物料对齐）

**Files:**
- Create: `workbench/workbench-service/src/employees/renderer.ts`
- Test: `workbench/workbench-service/test/employees-renderer.test.ts`

**Interfaces:**
- Consumes: `Manifest`（shared-protocol）；`deriveCapabilities`（技能 description 入参由调用方组装——本模块收 `skills: Array<{name, version, description}>`）
- Produces: `renderAgentsMd(manifest: Manifest, skills: Array<{name: string; version: string; description: string}>): string`

- [ ] **Step 1: 对齐基线先行（关键步骤）**——手写渲染器前，先写「7 模板物料 AGENTS.md == 渲染器输出」的快照测试骨架；然后实现渲染器到全部通过。渲染规则（spec §5.1 + 员工模板设计 §4）：
  - 标题 `# {display}（{id}）`；六段标题固定文案
  - 「我是谁」：`{role}。{identity 全文}` + S4 授权措辞句「本角色为用户配置的数字员工岗位」（以物料现有文案为准——**先读 1~2 份物料 AGENTS.md 逐段对照，物料文案是排版基准**）
  - 「我的原则」principles 逐条 `- `；「我的能力」capabilities 汇总 + 技能清单 `- {name}@{version}`；「我的边界」红线自然语言（rule_id → 描述表，写在本模块 `REDLINE_DESCRIPTIONS: Record<rule_id, string>`，描述文案从物料 AGENTS.md 边界段提取）；「我的工作方式」usage_modes + 有个人表则一句指引 `orchestration/`；末段按 kind 分派（执行位/评审位三件套文案**逐字对照物料**）
- [ ] **Step 2: 跑红（渲染器不存在）→ Step 3: 实现到 7/7 快照对齐**（不一致时：渲染器逻辑错误改渲染器；物料个别文案漂移改物料 AGENTS.md 并在提交信息注明）→ **Step 4: 跑绿 + 提交** `feat(service): AGENTS.md 六段式渲染器（kind 分派双变体；7 模板物料快照对齐测试）`

### Task 10 (B5): hooks 编译器（W3 命令格式 + 物料重生成）

**Files:**
- Create: `workbench/workbench-service/src/employees/hooks-compiler.ts`
- Modify: `workbench/templates/*/hooks/hooks.json`（7 份——命令格式 W3 化重生成）
- Test: `workbench/workbench-service/test/hooks-compiler.test.ts`

**Interfaces:**
- Produces: `compileHooks(manifest: Manifest): string | null`（null = 无 compiled 红线且 deny 空）；输出 JSON 结构 = 物料 hooks.json 既有结构（description + hooks.PreToolUse[]），命令 `"${DEVZERO_HOOKS_ROOT}/redlines/run-hook.cmd" <rule_id>.py`、matcher 按规则映射：`no-push-to-main→'Bash'`、`no-devzero-state→'Write|Edit|MultiEdit|Bash'`、`no-external-request→'Bash'`、`no-production-access→'Bash'`、`no-db-schema→'Write|Edit|MultiEdit'`；tools.deny 每项生成独立条目 matcher=该工具名；description 文案「由 devzero 从红线声明编译生成，请勿手改（重新生成会覆盖）。规则：{compiled 规则 id 列表}{T10 注记}」

- [ ] **Step 1: 失败测试**（①dev-engineer manifest → 编译输出与重生成后的物料 hooks.json 全等 ②无红线无 deny → null ③deny 非空 → 条目出现 ④命令格式断言含 `run-hook.cmd`）
- [ ] **Step 2: 跑红 → Step 3: 实现编译器 → 跑绿 → 用编译器重生成 7 份物料 hooks.json**（写一次性脚本或 bun repl 调 compileHooks 覆盖写；B1 的 gen 也要重跑——`bun run gen:templates`）→ 全套测试绿
- [ ] **Step 4: 提交** `feat(service): hooks 编译器（redlines compiled+tools.deny→PreToolUse，W3 polyglot 命令格式）+ 7 模板 hooks.json 重生成对齐`

### Task 11 (B6): builder 管线八步 + generate/validate-id 端点

**Files:**
- Create: `workbench/workbench-service/src/employees/builder.ts`
- Create: `workbench/workbench-service/src/server/routes/employees.ts`
- Modify: `workbench/workbench-service/src/server/routes/index.ts`、`src/main.ts`（装配 provider/store/builder 并传 deps）
- Test: `workbench/workbench-service/test/employees-builder.test.ts`、`test/routes-employees.test.ts`

**Interfaces:**
- Consumes: A 系列 schema/validateManifest；B2 provider；B3 store；B4 renderer；B5 compiler
- Produces:
  - `interface EmployeeDraft`（向导 draft 契约——与 Manifest 同形但允许半成品？**否**：draft 即完整 manifest 值 + skills 附带素材描述 `{ name, version, source_type, template_id?, origin?, description }[]`；服务端以 validateManifest 收口）
  - `class EmployeeBuilder { constructor(deps: { provider: TemplatesProvider; store: EmployeeStore; tmpRoot: string }) ; async generate(draft: EmployeeDraft): Promise<GenerateResult> }`——八步：validate → renderer → manifest yaml dump（key 顺序按 v0.2 三层；`operator` 缺省 'demo@devzero.local'；`template_id` 注入素材所属模板）→ skills 物化（template→provider.read 逐文件（`<tplId>/skills/<name>/` 前缀遍历 builtinTemplates + custom fs）；local→`tmpRoot/skills/<name>/` 拷贝，缺失抛 `SkillMissingError(name)`）→ hooks（compiler + redlines 脚本本体从素材模板位拷贝——**D3 未物化前该拷贝步骤跳过并留 TODO 标记**？不：留接口 `copyRedlineScripts` 依赖 provider 有 `hooks/redlines/` 文件，D3 完成后自然生效，本任务测试不覆盖脚本内容）→ mcp.json（connectors 非空才生成：`{ mcpServers: { [name]: { type, command?, args?, url?, env } } }`）→ orchestration 拷贝（manifest.orchestration 存在→从模板位拷该 yml）→ store.materialize
  - `GenerateResult = { package_path: string; files: string[]; manifest: Manifest }`
  - 路由：`POST /api/employees/generate`（body `{draft}` → 200 GenerateResult / 422 `{code:'VALIDATION_FAILED', field_errors:[{step,field,message}]}`（issues 的 path 前段映射 step：agent.*/org/operator/display→'agent'；skills.*→'skills'；hooks.*/tools.*→'hooks-tools'；connectors.*→'connectors'；其余→'base'）/ 409 `{code:'ID_CONFLICT'}`）；`GET /api/employees/validate-id?id=` → `{available, suggestion?}`（冲突时 suggestion=`<id>-2` 递增后缀）

- [ ] **Step 1: 失败测试**（builder 集成：注入隔离 tmp 的 provider/store——①dev-engineer draft（从真 builtinTemplates 读）→ generate → 断言 employees/<id>/ 目录树（manifest.yml+AGENTS.md+skills/5 份+hooks/hooks.json+orchestration/…）、manifest 复验过 schema、无 mcp.json（connectors 空）②local skill 缺 temp → SkillMissingError ③id 冲突 → 409 语义 ④坏 draft → 422 逐项+step 定位；路由测试：registry 注册 + app.request 三态）
- [ ] **Step 2: 跑红 → Step 3: 实现 builder + 路由 + main 装配**（main.ts：`const provider = createTemplatesProvider(builtinTemplates, join(profileDir, 'templates', 'custom'))`；`const store = createEmployeeStore(join(profileDir, 'employees'), join(profileDir, 'tmp'))`；deps 注入 routes——沿 index.ts RouteDeps 聚合模式）
- [ ] **Step 4: 跑绿（service 全套）+ 提交** `feat(service): E-12 builder 管线八步 + generate/validate-id 端点（draft 驱动重建/原子落盘/422 逐项 step 定位）`

---

## 阶段 C：E-13 zip 上传 + E-11 向导（C1~C5）

### Task 12 (C1): adapter bodyRaw 扩展 + zip 上传服务

**Files:**
- Modify: `workbench/workbench-service/src/server/hono-adapter.ts`（bodyRaw 通道）、`src/server/registry.ts`（Ctx 加 `bodyRaw?: ArrayBuffer`）
- Create: `workbench/workbench-service/src/employees/skill-upload.ts`
- Create: `workbench/workbench-service/src/server/routes/skills.ts`（`POST /api/skills/upload`）
- Modify: `src/server/routes/index.ts`、`src/main.ts`
- Test: `workbench/workbench-service/test/skill-upload.test.ts`

**Interfaces:**
- Produces: `uploadSkillZip(zipBytes: Uint8Array, fileName: string, tmpRoot: string): Promise<UploadedSkill>`（`UploadedSkill = { name: string; version: string; source_type: 'local'; origin: string; sha256: string; files: string[] }`）；实现要点：
  - `fflate.unzipSync` 解包（service dependencies 加 `fflate`）
  - **zip-slip**：每个 entry 名 `normalize` 后不得含 `..` 段且不得以 `/` 开头，违者抛
  - **GBK 文件名**：fflate 出的文件名若含 CP437 替换符或 zip 未标 UTF-8 flag，用 `new TextDecoder('gbk')` 重解码 `Uint8Array.from(name, c=>c.charCodeAt(0))`（Bun/Node ICU 支持；测试实证，不支持则 fallback 保留原名并 warning）
  - **布局**：根有 SKILL.md → 直接用；否则唯一顶层目录且其下有 SKILL.md → 剥一层；都不满足 → 抛 `SkillLayoutError`
  - frontmatter 过 `skillFrontmatterSchema`（复用 A3 的分割解析逻辑——抽 `parseSkillFrontmatter(mdText)` 到 shared-protocol skill.ts 导出，本任务 Modify）
  - 版本：frontmatter `version` 键优先，缺省 `'0.1.0'`（P2）
  - 物化 `tmpRoot/skills/<name>/`（目录名 = frontmatter name；与 zip 目录名不一致时**以 frontmatter name 为准**重命名）；sha256 = 原始 zip bytes 的 hash（`Bun.CryptoHasher`）
  - 限额：解压后总大小 >50MB 或文件数 >2000 拒
- 路由：multipart 解析（`new Response(ctx.bodyRaw)` + `ctx.req.formData()`？——**不**：adapter 层直读 raw，域内用 `Web APIs` 的 `Request` 包一层读 formData，或简单手解析 multipart 边界——**采用**：adapter 把 content-type 含 `multipart/form-data` 的 bodyRaw 交给域，域用 `new Request('http://l', { method: 'POST', body: bodyRaw, headers: { 'content-type': ct } }).formData()` 标准 API 取 File）

- [ ] **Step 1: 失败测试**（fixture zip 用 bun 现场造：`fflate.zipSync` 生成「根 SKILL.md + references/x.md」「顶层目录 sec-x/SKILL.md」「无 SKILL.md」「路径穿越 entry」四种 + GBK 名 fixture 用 Buffer 手构 zip entry；断言：合法两形态返回 UploadedSkill 且 tmp 落盘；穿越/无 SKILL/坏 frontmatter 各抛对应错；同名二次上传覆盖幂等）
- [ ] **Step 2: 跑红 → Step 3: 实现**（adapter：`dispatch` 中 `route.method !== 'GET' && !ct.includes('application/json')` 时 `bodyRaw: await c.req.raw.arrayBuffer()`；registry Ctx 类型加字段）→ **Step 4: 跑绿（含 server.test.ts 既有套不破坏）+ 提交** `feat(service): zip skill 上传——adapter bodyRaw 通道 + 解包防护（zip-slip/GBK/布局兼容/frontmatter 校验/temp 物化/sha256）`

### Task 13 (C2): web 向导骨架（路由/布局/模板卡/步骤条）

**Files:**
- Create: `workbench/workbench-web/src/views/CreateWizard.vue`、`src/components/wizard/TplGrid.vue`、`src/components/wizard/StepBar.vue`
- Modify: `workbench/workbench-web/src/router/routes/employees.ts`（`/employees/new` → CreateWizard；确认守卫 session+active）
- Create: `workbench/workbench-web/src/api/templates.ts`（GET /api/templates、/api/skills 类型化客户端——类型从 `@devzero/shared-protocol` import TemplateMeta 形状在 web 侧定义（API DTO 不直接暴露内部类型，字段同 B2 TemplateMeta））
- Test: `workbench/workbench-web/test/create-wizard.test.ts`（首切片：路由可达 + TplGrid 渲染 8 卡（7+Custom）+ 选中态）

**Interfaces:**
- Produces: 向导页骨架（page-head 返回按钮 +「员工创建」标题；左栏「1 · 选择角色模板」卡片网格 +「2 · 配置向导」卡（StepBar 七步 + router-view 式步骤容器）；右栏 sticky 预览面板占位组件 `<PreviewPanel>`）；`wizardStore`（Pinia：draft 状态、currentStep、模板选中）——本任务建 store 骨架（state: draft/currentStep/selectedTemplate；actions: selectTemplate（预填 draft））

- [ ] **Step 1: 失败测试**（mount CreateWizard + mock api client：8 卡渲染（id/display/brief/kind 徽章）；点卡片 → 选中态 ✓ + store.selectedTemplate 变更；Custom 卡零预填 selectTemplate 后 draft 仍空）
- [ ] **Step 2: 跑红 → Step 3: 实现**（视觉沿原型类名/结构：tpl-grid/tpl-card/check/tpl-head/tpl-name/tpl-desc/tpl-foot + tokens.css 变量；组件 scoped 样式自持——沿既有 web 组件模式）→ **Step 4: 跑绿 + 提交** `feat(web): 创建向导骨架——路由/布局（layout-2col）/模板卡网格（数据驱动 7+Custom）/七步步骤条/向导 store`

### Task 14 (C3): 七步表单组件 + slug 联动 + 草稿

**Files:**
- Create: `workbench/workbench-web/src/components/wizard/steps/StepAgent.vue`、`StepSkills.vue`、`StepHooksTools.vue`、`StepCommandsFlow.vue`、`StepKnowledge.vue`、`StepConnectors.vue`
- Create: `workbench/workbench-web/src/composables/useWizardDraft.ts`（草稿 localStorage + slug 工具）
- Test: `workbench/workbench-web/test/wizard-steps.test.ts`、`test/wizard-draft.test.ts`

**Interfaces:**
- Consumes: wizardStore.draft（结构 = manifest 值 + skills 附 description/勾选态）
- Produces:
  - `useWizardDraft()`：`slugify(display)`（小写、`[^a-z0-9]+→-`、去首尾、空兜底 `employee`、≤32）、`idTouched` 停跟逻辑（watch display → 未 touched 才同步 draft.id）、`saveDraft()/restoreDraft()/clearDraft()`（localStorage 键 `devzero:wizard-draft`，防抖 1s 落键；restore 时 local skills 标 `需重新上传`）
  - Step 组件各自读写 store.draft 对应字段；StepSkills 消费 `GET /api/skills` 全集网格（模板默认徽章=选中模板 skills 含该 name；勾选/取消/搜索 name+description 过滤 + 上传按钮占位（C1 已有后端，upload 调用本任务接通：选文件 → POST → 已选清单加 [本地上传] 行））
  - StepHooksTools：红线 chip 勾选（候选 = spec §7 表④的 7 rule_id + 自然语言描述）+ 折叠区（tier 五档 radio / token 两个 number / governance level+visibility+audit）
  - StepCommandsFlow：个人表只读（选中模板有 orchestration → 从 `GET /api/templates` 的 meta 附带？——**补 B2 TemplateMeta 加 `hasFlow: boolean` 与 flowYaml?: string`**（provider 读模板位 yml 文本），本任务 Modify provider+路由补字段）
  - StepKnowledge：占位卡「暂未开放」；StepConnectors：模板 connectors 只读列表
- 验收细节：`下一步` 按钮步级必填校验（Step②：display/id 非空；其余步无硬必填）

- [ ] **Step 1: 失败测试**（slugify 表驱动含 CJK 兜底；idTouched 停跟；StepAgent 双向绑定 draft；StepSkills mock api 渲染全集+勾选变更+搜索过滤；草稿 save/restore 往返；上传 mock 返回进已选）
- [ ] **Step 2: 跑红 → Step 3: 实现 → Step 4: 跑绿 + 提交** `feat(web): 向导七步表单（Agent 定义 slug 联动停跟/Skills 全集网格+zip 上传/HooksTools 红线+管理面折叠/CommandsFlow 个人表只读/Knowledge 占位/Connectors 只读）+ 草稿 localStorage`

### Task 15 (C4): 校验徽章 + 预览面板

**Files:**
- Create: `workbench/workbench-web/src/components/wizard/PreviewPanel.vue`、`src/composables/useManifestValidation.ts`
- Modify: `workbench/workbench-web/src/stores/wizard.ts`（或 composable 内聚——徽章状态局部于面板）
- Test: `workbench/workbench-web/test/wizard-preview.test.ts`

**Interfaces:**
- Consumes: `@devzero/shared-protocol` 的 `validateManifest`（P3 接线验证点——vite 对 workspace TS 源包直编译；若 vite 预打包报错，`optimizeDeps.exclude: ['@devzero/shared-protocol']` 处理）
- Produces: PreviewPanel（校验徽章「校验通过」/「失败 n 项」红绿 + 点击 issue 列表逐项 `{step, field}` 跳转（映射表：path 前段→step，同 B6 step 映射规则，前端常量表）；manifest YAML 渲染（js-yaml dump draft，web dependencies 加 `js-yaml`；高亮 = 简易 key/value 着色 span，无需语法库）；目录树动态显隐（skills 勾选→`skills/<name>/SKILL.md` 行；红线 compiled→hooks/ 行；connectors→mcp.json 行；orchestration→表行；commands/knowledge 恒在）；300ms 防抖 watch draft）

- [ ] **Step 1: 失败测试**（防抖：vi.useFakeTimers 改 draft → 299ms 不调 300ms 调；坏 draft（缺 display）→ 徽章红 + issue 点击 emit `{step:'agent', field:'display'}`；好 draft → 绿；目录树行随勾选增减）
- [ ] **Step 2: 跑红 → Step 3: 实现 → Step 4: 跑绿 + 提交** `feat(web): 向导预览面板——300ms 防抖 schema 校验徽章（shared-protocol 前端同源）+ 逐项跳转 + manifest YAML 渲染 + 目录树动态显隐`

### Task 16 (C5): 生成动作 + 完成态三动作 + 安装弹层

**Files:**
- Create: `workbench/workbench-web/src/components/wizard/CompletionPanel.vue`、`src/components/wizard/InstallModal.vue`
- Create: `workbench/workbench-web/src/api/employees.ts`（POST generate / GET validate-id / POST /api/templates）
- Test: `workbench/workbench-web/test/wizard-completion.test.ts`

**Interfaces:**
- Produces:
  - 最后步「生成员工包」按钮 → POST generate → 成功 → CompletionPanel（包路径+文件清单+三动作）；422 → 徽章区展开逐项；409 → id 输入区红字提示
  - InstallModal（沿 demo 三步形态：底座选择[静态数组 `[{id:'claude-code',display:'Claude Code'},{id:'codebuddy',display:'CodeBuddy'},{id:'qoder',display:'Qoder'}]` 全可选]→确认→「开始安装」→ 调 `POST /api/installs`（**预留路径**，fetch 404/501 catch → 步骤区显示「安装服务待接入（安装线联调后开放）」提示态——不抛错不模拟进度）
  - 「保存为角色模板」→ POST /api/templates（body=draft）→ 成功 toast；「完成离开」→ router.push('/employees') + clearDraft
  - 生成成功清草稿键

- [ ] **Step 1: 失败测试**（mock api：generate 成功 → CompletionPanel 渲染 files 清单 + 三动作在位；安装点击 → fetch 404 → 显示待接入文案且无 `/digital-staff` 相关字样；保存模板调用 payload 断言；离开清 draft 键）
- [ ] **Step 2: 跑红 → Step 3: 实现 → Step 4: 跑绿 + 提交** `feat(web): 向导生成与完成态——generate 三态处理/安装弹层（静态三底座+API 预留+待接入提示）/保存为模板/完成离开/草稿清理`

---

## 阶段 D：花名册 + 预置物化 + 物料批 + 收口（D1~D6）

### Task 17 (D1): 花名册（scanner API + web 列表页）

**Files:**
- Modify: `workbench/workbench-service/src/employees/store.ts`（list 已有——本任务补路由）、`src/server/routes/employees.ts`（加 GET /api/employees）
- Create: `workbench/workbench-web/src/views/EmployeesView.vue`、`src/api/employees.ts` 补 GET、router routes（`/employees` 主页——已存在则改 Placeholder 为真视图）
- Test: `workbench/workbench-service/test/routes-employees.test.ts` 补用例、`workbench/workbench-web/test/employees-view.test.ts`

**Interfaces:**
- Produces: `GET /api/employees` → `{ items: Array<{ id, display, brief, avatar, kind, version }>, invalid: string[] }`（store.list 派生；无安装数据字段——完整 E-10 后续批）；web 列表页卡片 grid（头像/岗位名+id/kind 徽章/简介两行截断）+ 空态引导卡（「＋新建员工」+ 一句话）+ 「＋新建员工」入口按钮 → /employees/new

- [ ] **Step 1~4: 红→实现→绿→提交** `feat(web): 花名册最小实现——员工列表卡片 grid + 空态 + 新建入口（GET /api/employees 扫描派生）`

### Task 18 (D2): 预置物化（首启初始化）

**Files:**
- Create: `workbench/workbench-service/src/employees/seed.ts`
- Modify: `workbench/workbench-service/src/main.ts`（startRealServer 前调 `seedBuiltinEmployees(...)`）
- Test: `workbench/workbench-service/test/seed.test.ts`

**Interfaces:**
- Produces: `seedBuiltinEmployees(provider, store, builder, opts?: { operator?: string }): Promise<{ seeded: string[]; skipped: string[] }>`——遍历 provider.list() 的 builtin 模板：`store.exists(id)` → skipped；否则读模板 manifest（provider.read）→ draft（skills 附 description——从模板 SKILL.md frontmatter parse 取）→ builder.generate；operator = opts.operator ?? 'demo@devzero.local'；幂等语义 = 已存在跳过不比较（spec §9）；**同构断言**：测试里同一 draft 走「预置物化」与「向导 generate」两条调用路径产出目录树一致

- [ ] **Step 1: 失败测试**（隔离 HOME：首跑 7 目录齐 + manifest 过 schema + operator 占位；二跑全 skipped；dev-engineer 包结构与 builder 直产一致）
- [ ] **Step 2~4: 红→实现（main.ts 装配 try/catch——seed 失败不阻断服务启动，log warn）→绿→提交** `feat(service): 预置物化——首启 7 模板走同一 E-12 管线进员工库（幂等跳过/operator 占位/与向导产物同构）`

### Task 19 (D3): 红线脚本本体（polyglot + 5 规则）

**Files:**
- Create: `workbench/templates/dev-engineer/hooks/redlines/run-hook.cmd`（1.0 `run-python-hook.cmd` 原样移植，仅注释改 devzero）+ `no-push-to-main.py`、`no-devzero-state.py`、`no-external-request.py`、`no-production-access.py`、`no-db-schema.py`
- 同构拷贝到其余 6 模板 `hooks/redlines/`（7 份自带——脚本同内容；用 shell 复制）
- Modify: B1 的 gen 重跑（templates.gen.ts 更新）
- Test: `workbench/workbench-service/test/redline-scripts.test.ts`

**Interfaces:**
- Produces: 每脚本读 stdin JSON（CC hook 协议：`{tool_name, tool_input}` 形态）→ 判定 → 拒绝时 exit 2 + stderr 拒因（CC hook 协议 exit 2 = block；1.0 先例）：
  - `no-push-to-main.py`：tool_name=Bash 且 command 匹配 `git push.*(--force )?origin main|git push origin HEAD:main`（含变体）→ 拒
  - `no-devzero-state.py`：tool_name ∈ Write|Edit|MultiEdit|Bash 且 file_path/command 含 `/.devzero/`（含 `\.devzero\` Windows 形态）→ 拒（工作台状态目录不可员工改写）
  - `no-external-request.py`：Bash command 含 `curl|wget|Invoke-WebRequest` 或 tool_name=WebFetch 且域名非白名单（V0.1 空白名单=全拒外网）→ 拒
  - `no-production-access.py`：command/file_path 含 prod 环境特征（`prod-`、`/etc/production`、`3306`生产库连接）→ 拒
  - `no-db-schema.py`：command 含 `ALTER TABLE|DROP TABLE|CREATE TABLE` 且非 migration 文件路径 → 拒
  - 规则内容为**候选值实现**（T10 定格红线分配的执行体；正则命中即拒，不猜语义）
- 测试：bun 测试 spawn `cmd /c run-hook.cmd <rule>.py`（Windows 开发机；stdin 喂 JSON，断言 exit code 与 stderr）+ 各模板 redlines/ 目录 7 份同构断言 + gen drift 过

- [ ] **Step 1~4: 红（脚本不存在）→ 实现（cmd 从 1.0 仓 `git show 68d58ab2:plugins/agents-team/hooks/run-python-hook.cmd` 取原文移植）→ 绿→提交** `feat(templates): 红线脚本本体——run-hook.cmd polyglot 包装器移植 + 5 规则 py（stdin JSON 判定/exit 2 拒因）×7 模板 hooks/redlines/ 同构自带（fable #16 规范锚落地）`

### Task 20 (D4): sec 两 zip 解包入模板库

**Files:**
- Create: `workbench/templates/sec-design/skills/sec-scan-design/`（SKILL.md + references/×15——**GBK 文件名转 UTF-8 重命名**，用一次性 bun 脚本 `scripts/import-sec-zips.ts`（临时，放 service scripts/ 或独立 tools/，跑完可留仓供追溯））
- Create: `workbench/templates/sec-code/skills/sec-scan-code/`（SKILL.md + bin/*.py——**排除 `.venv.rar`、`__pycache__`、`install.*`**；留 `install-note.md` 注记「.venv 装员工 home 语义归 L2 运行时（Q3）」）
- Modify: `workbench/templates/sec-design/manifest.yml`、`sec-code/manifest.yml`（skills 条目恢复：`{name, version, source_type: template}`——version 取 zip 实物 frontmatter 或 SKILL.md 头注记，无则 0.1.0）
- Modify: B1 gen 重跑
- Test: A5 活样例 CI（跑即覆盖——manifest 恢复后 skills 声明-实物一致断言生效）

**步骤：**
- [ ] **Step 1: 跑活样例 CI 预检**（当前绿——恢复条目前先确认基线）
- [ ] **Step 2: 解包脚本 + 物化**（GBK→UTF-8：`new TextDecoder('gbk')` 解码 zip entry 名；重名/非法字符安全处理：转码后 sanitize（保留中文、去路径非法字符）；sec-scan-code 的 SKILL.md 47KB frontmatter 过 schema——不合法处小修 frontmatter 键并注记）
- [ ] **Step 3: manifest 恢复 + 全套测试绿**（活样例 CI 断言 skills 实物在位）
- [ ] **Step 4: 提交** `feat(templates): sec-scan-design/sec-scan-code zip 解包入包（GBK 文件名转 UTF-8/排除 .venv.rar+__pycache__）+ 两 manifest skills 条目恢复（skills-remaining #1 #2 销账）`

### Task 21 (D5): secretgate TS 移植

**Files:**
- Create: `workbench/templates/sec-compliance/skills/secretgate/SKILL.md`（frontmatter：name=secretgate/description=A1 语义描述——「密钥泄露扫描与定性：脚本零 token 扫描命中，LLM 做真泄露 vs 测试 fixture 定性解读与处置建议」；正文：用法 + A1 分工段对齐 sec-compliance AGENTS.md 既有分工段）+ `scripts/secretgate.mjs`（1.0 `ieidev_hud/secretgate.py` 正则规则引擎移植）
- Modify: `workbench/templates/sec-compliance/manifest.yml`（skills 恢复）
- Modify: B1 gen 重跑
- Test: `workbench/workbench-service/test/secretgate.test.ts`（import secretgate.mjs 的纯函数：规则集对样例串命中——API Key 模式（`sk-`、`AKIA`、`ghp_` 等）/私钥块/连接串（`postgres://user:pass@`）/泛凭证；1.0 源正则逐条对照移植）

**步骤：**
- [ ] **Step 1: 读 1.0 源**（`git show 68d58ab2:plugins/agents-team/pyieidev/ieidev_hud/secretgate.py`）提取规则表 → **Step 2: 失败测试**（规则命中表驱动）→ **Step 3: 移植实现**（mjs 纯函数 `scan(text): Array<{rule, match, span}>` + CLI 入口 `node secretgate.mjs <file>` 输出 JSON 报告）→ **Step 4: 绿 + SKILL.md + manifest 恢复 + 活样例 CI 绿 + 提交** `feat(templates): secretgate.mjs——1.0 正则规则引擎 TS 生态移植为 sec-compliance skill 内置脚本（A1 零 token 扫描 + LLM 定性分工；skills-remaining #3 销账）`

### Task 22 (D6): 收口（边界守卫 + e2e 冒烟 + 全绿 + 销账）

**Files:**
- Create: `workbench/workbench-service/test/boundary-guard.test.ts`
- Create: `workbench/workbench-web/test/e2e/wizard-smoke.spec.ts`（Playwright——若 web 无 Playwright 配置，建最小 `playwright.config.ts` + 单 spec；环境限制跑不了则标记 env-gated 沿 1.0 模式）
- Modify: `workbench/templates/skills-remaining.md`（#1~#4 销账标注）、`workbench/templates/README.md`（sec 三员 skills 计数刷新 + redlines/ 目录结构行）

**步骤：**
- [ ] **Step 1: 边界守卫测试**（注入隔离 HOME（含假 `digital-staff/` 预置哨兵文件）→ 跑 seed + generate 全链 → 断言哨兵目录 mtime/内容零变化且无新增文件——「全程未触碰 ~/digital-staff/」的可执行证明）
- [ ] **Step 2: e2e 冒烟**（Playwright：起 service（隔离 HOME）+ web dev → 访问 /employees/new → 选 dev-engineer 模板 → 一路默认下一步 ×6 → 点生成 → 断言完成态出现 → 跳 /employees 断言新卡在位；env-gated（需双进程，CI/本地手动跑）
- [ ] **Step 3: 全量绿**（四包 `bun run test` + `bun run typecheck`：shared-protocol/service/web/engine）
- [ ] **Step 4: 台账销账 + 提交** `test+docs: L1 收口——目录边界守卫（digital-staff 零触碰断言）/向导 e2e 冒烟/全量测试绿/skills-remaining #1~#4 销账 + README 计数刷新`

---

## Self-Review 结论（写 plan 后自查）

- **Spec 覆盖**：spec §4→A1~A5；§5→B1~B6（5.1 渲染=B4、5.2 编译=B5、5.3 八步=B6）；§6→B2/B6/C1/C5（templates GET/POST——**POST /api/templates 在 C5 完成「保存为角色模板」时实现**（路由落 routes/templates.ts 扩展，body=draft 写 custom 目录复用 builder 物化逻辑减员工库落盘——见 C5 Files 的 api/employees.ts；C5 测试含 payload 断言）；§7→C2~C5；§8→C1；§9→D2；§10→D3~D5；§11→各任务测试+D6；§2 验收锚→D6 Step1~3
- **类型一致**：TemplateMeta/SkillMeta（B2 定义 C2 消费）；EmployeeDraft（B6 定义 C5 消费）；UploadedSkill（C1 定义 C3 消费）；step 映射（B6 与 C4 同规则两处常量——service `FIELD_STEP_MAP` / web `fieldStepMap`，注释互指）
- **无占位符**：所有代码块为可直接落盘的真实实现（伪码标注处仅 A2 superRefine 内联示意，Step 3 已注明写成完整 TS）
