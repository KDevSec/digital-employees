{
  "name": "@devzero/service",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "gen:templates": "bun run scripts/gen-templates-assets.ts",
    "pretest": "bun run gen:templates",
    "prebuild": "bun run gen:templates",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "build": "bash scripts/build.sh"
  },
  "dependencies": {
    "@devzero/shared-protocol": "workspace:*",
    "hono": "^4",
    "zod": "^3",
    "commander": "^12"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^2",
    "@types/bun": "latest"
  }
}
