安装前须知

1. 本安装包未做代码签名。若 Windows SmartScreen 弹出「已保护你的电脑」，
   请点击「更多信息」->「仍要运行」。同一安装包重复安装不会再弹。
2. 请从管控平台下载页核对安装包的 SHA-256 值后再执行。
3. 安装为当前用户安装（免管理员权限），安装目录固定于
   %LOCALAPPDATA%\Programs\devzero。
4. 安装完成后本机将运行本地服务（端口 19980）与托盘程序，
   可随时从 Windows「设置 -> 应用」卸载；卸载时将询问是否保留用户数据。
